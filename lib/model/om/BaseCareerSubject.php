<?php

/**
 * Base class that represents a row from the 'career_subject' table.
 *
 * Cada tupla representa una carrera, una materia y el año
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseCareerSubject extends BaseObject  implements Persistent {


  const PEER = 'CareerSubjectPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CareerSubjectPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the career_id field.
	 * @var        int
	 */
	protected $career_id;

	/**
	 * The value for the subject_id field.
	 * @var        int
	 */
	protected $subject_id;

	/**
	 * The value for the year field.
	 * @var        int
	 */
	protected $year;

	/**
	 * The value for the has_options field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $has_options;

	/**
	 * The value for the is_option field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_option;

	/**
	 * The value for the orientation_id field.
	 * @var        int
	 */
	protected $orientation_id;

	/**
	 * The value for the sub_orientation_id field.
	 * @var        int
	 */
	protected $sub_orientation_id;

	/**
	 * The value for the has_correlative_previous_year field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $has_correlative_previous_year;

	/**
	 * @var        Career
	 */
	protected $aCareer;

	/**
	 * @var        Subject
	 */
	protected $aSubject;

	/**
	 * @var        Orientation
	 */
	protected $aOrientation;

	/**
	 * @var        SubOrientation
	 */
	protected $aSubOrientation;

	/**
	 * @var        array StudentCareerSubjectAllowed[] Collection to store aggregation of StudentCareerSubjectAllowed objects.
	 */
	protected $collStudentCareerSubjectAlloweds;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentCareerSubjectAlloweds.
	 */
	private $lastStudentCareerSubjectAllowedCriteria = null;

	/**
	 * @var        array StudentApprovedCareerSubject[] Collection to store aggregation of StudentApprovedCareerSubject objects.
	 */
	protected $collStudentApprovedCareerSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentApprovedCareerSubjects.
	 */
	private $lastStudentApprovedCareerSubjectCriteria = null;

	/**
	 * @var        array ExaminationRepprovedSubject[] Collection to store aggregation of ExaminationRepprovedSubject objects.
	 */
	protected $collExaminationRepprovedSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collExaminationRepprovedSubjects.
	 */
	private $lastExaminationRepprovedSubjectCriteria = null;

	/**
	 * @var        array Correlative[] Collection to store aggregation of Correlative objects.
	 */
	protected $collCorrelativesRelatedByCareerSubjectId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCorrelativesRelatedByCareerSubjectId.
	 */
	private $lastCorrelativeRelatedByCareerSubjectIdCriteria = null;

	/**
	 * @var        array Correlative[] Collection to store aggregation of Correlative objects.
	 */
	protected $collCorrelativesRelatedByCorrelativeCareerSubjectId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCorrelativesRelatedByCorrelativeCareerSubjectId.
	 */
	private $lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria = null;

	/**
	 * @var        array CareerSubjectSchoolYear[] Collection to store aggregation of CareerSubjectSchoolYear objects.
	 */
	protected $collCareerSubjectSchoolYears;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerSubjectSchoolYears.
	 */
	private $lastCareerSubjectSchoolYearCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseCareerSubject object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->has_options = false;
		$this->is_option = false;
		$this->has_correlative_previous_year = true;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [career_id] column value.
	 * Referencia a la carrera
	 * @return     int
	 */
	public function getCareerId()
	{
		return $this->career_id;
	}

	/**
	 * Get the [subject_id] column value.
	 * Referencia a la materia
	 * @return     int
	 */
	public function getSubjectId()
	{
		return $this->subject_id;
	}

	/**
	 * Get the [year] column value.
	 * Representa un entero q indica el año de la carrera para esa materia
	 * @return     int
	 */
	public function getYear()
	{
		return $this->year;
	}

	/**
	 * Get the [has_options] column value.
	 * esta materia tiene opciones
	 * @return     boolean
	 */
	public function getHasOptions()
	{
		return $this->has_options;
	}

	/**
	 * Get the [is_option] column value.
	 * esta materia es opcional
	 * @return     boolean
	 */
	public function getIsOption()
	{
		return $this->is_option;
	}

	/**
	 * Get the [orientation_id] column value.
	 * Orientacion de la materia en el plan de estudio. No debería setearse si es opcion (solo para opcionales)
	 * @return     int
	 */
	public function getOrientationId()
	{
		return $this->orientation_id;
	}

	/**
	 * Get the [sub_orientation_id] column value.
	 * Sub orientacion de la materia en el plan de estudio. No debería setearse si es opcion (solo para opcionales)
	 * @return     int
	 */
	public function getSubOrientationId()
	{
		return $this->sub_orientation_id;
	}

	/**
	 * Get the [has_correlative_previous_year] column value.
	 * Indica si esta materia tiene como correlatividas las materias del año previo completo o deben elegirse materias
	 * @return     boolean
	 */
	public function getHasCorrelativePreviousYear()
	{
		return $this->has_correlative_previous_year;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = CareerSubjectPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Set the value of [career_id] column.
	 * Referencia a la carrera
	 * @param      int $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setCareerId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->career_id !== $v) {
			$this->career_id = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::CAREER_ID;
		}

		if ($this->aCareer !== null && $this->aCareer->getId() !== $v) {
			$this->aCareer = null;
		}

		return $this;
	} // setCareerId()

	/**
	 * Set the value of [subject_id] column.
	 * Referencia a la materia
	 * @param      int $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setSubjectId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->subject_id !== $v) {
			$this->subject_id = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::SUBJECT_ID;
		}

		if ($this->aSubject !== null && $this->aSubject->getId() !== $v) {
			$this->aSubject = null;
		}

		return $this;
	} // setSubjectId()

	/**
	 * Set the value of [year] column.
	 * Representa un entero q indica el año de la carrera para esa materia
	 * @param      int $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setYear($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->year !== $v) {
			$this->year = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::YEAR;
		}

		return $this;
	} // setYear()

	/**
	 * Set the value of [has_options] column.
	 * esta materia tiene opciones
	 * @param      boolean $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setHasOptions($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->has_options !== $v || $v === false) {
			$this->has_options = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::HAS_OPTIONS;
		}

		return $this;
	} // setHasOptions()

	/**
	 * Set the value of [is_option] column.
	 * esta materia es opcional
	 * @param      boolean $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setIsOption($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_option !== $v || $v === false) {
			$this->is_option = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::IS_OPTION;
		}

		return $this;
	} // setIsOption()

	/**
	 * Set the value of [orientation_id] column.
	 * Orientacion de la materia en el plan de estudio. No debería setearse si es opcion (solo para opcionales)
	 * @param      int $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setOrientationId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->orientation_id !== $v) {
			$this->orientation_id = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::ORIENTATION_ID;
		}

		if ($this->aOrientation !== null && $this->aOrientation->getId() !== $v) {
			$this->aOrientation = null;
		}

		return $this;
	} // setOrientationId()

	/**
	 * Set the value of [sub_orientation_id] column.
	 * Sub orientacion de la materia en el plan de estudio. No debería setearse si es opcion (solo para opcionales)
	 * @param      int $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setSubOrientationId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->sub_orientation_id !== $v) {
			$this->sub_orientation_id = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::SUB_ORIENTATION_ID;
		}

		if ($this->aSubOrientation !== null && $this->aSubOrientation->getId() !== $v) {
			$this->aSubOrientation = null;
		}

		return $this;
	} // setSubOrientationId()

	/**
	 * Set the value of [has_correlative_previous_year] column.
	 * Indica si esta materia tiene como correlatividas las materias del año previo completo o deben elegirse materias
	 * @param      boolean $v new value
	 * @return     CareerSubject The current object (for fluent API support)
	 */
	public function setHasCorrelativePreviousYear($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->has_correlative_previous_year !== $v || $v === true) {
			$this->has_correlative_previous_year = $v;
			$this->modifiedColumns[] = CareerSubjectPeer::HAS_CORRELATIVE_PREVIOUS_YEAR;
		}

		return $this;
	} // setHasCorrelativePreviousYear()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(CareerSubjectPeer::HAS_OPTIONS,CareerSubjectPeer::IS_OPTION,CareerSubjectPeer::HAS_CORRELATIVE_PREVIOUS_YEAR))) {
				return false;
			}

			if ($this->has_options !== false) {
				return false;
			}

			if ($this->is_option !== false) {
				return false;
			}

			if ($this->has_correlative_previous_year !== true) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->created_at = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->career_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->subject_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->year = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->has_options = ($row[$startcol + 5] !== null) ? (boolean) $row[$startcol + 5] : null;
			$this->is_option = ($row[$startcol + 6] !== null) ? (boolean) $row[$startcol + 6] : null;
			$this->orientation_id = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->sub_orientation_id = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->has_correlative_previous_year = ($row[$startcol + 9] !== null) ? (boolean) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = CareerSubjectPeer::NUM_COLUMNS - CareerSubjectPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating CareerSubject object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aCareer !== null && $this->career_id !== $this->aCareer->getId()) {
			$this->aCareer = null;
		}
		if ($this->aSubject !== null && $this->subject_id !== $this->aSubject->getId()) {
			$this->aSubject = null;
		}
		if ($this->aOrientation !== null && $this->orientation_id !== $this->aOrientation->getId()) {
			$this->aOrientation = null;
		}
		if ($this->aSubOrientation !== null && $this->sub_orientation_id !== $this->aSubOrientation->getId()) {
			$this->aSubOrientation = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSubjectPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CareerSubjectPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aCareer = null;
			$this->aSubject = null;
			$this->aOrientation = null;
			$this->aSubOrientation = null;
			$this->collStudentCareerSubjectAlloweds = null;
			$this->lastStudentCareerSubjectAllowedCriteria = null;

			$this->collStudentApprovedCareerSubjects = null;
			$this->lastStudentApprovedCareerSubjectCriteria = null;

			$this->collExaminationRepprovedSubjects = null;
			$this->lastExaminationRepprovedSubjectCriteria = null;

			$this->collCorrelativesRelatedByCareerSubjectId = null;
			$this->lastCorrelativeRelatedByCareerSubjectIdCriteria = null;

			$this->collCorrelativesRelatedByCorrelativeCareerSubjectId = null;
			$this->lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria = null;

			$this->collCareerSubjectSchoolYears = null;
			$this->lastCareerSubjectSchoolYearCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareerSubject:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSubjectPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			CareerSubjectPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseCareerSubject:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareerSubject:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(CareerSubjectPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSubjectPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseCareerSubject:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			CareerSubjectPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareer !== null) {
				if ($this->aCareer->isModified() || $this->aCareer->isNew()) {
					$affectedRows += $this->aCareer->save($con);
				}
				$this->setCareer($this->aCareer);
			}

			if ($this->aSubject !== null) {
				if ($this->aSubject->isModified() || $this->aSubject->isNew()) {
					$affectedRows += $this->aSubject->save($con);
				}
				$this->setSubject($this->aSubject);
			}

			if ($this->aOrientation !== null) {
				if ($this->aOrientation->isModified() || $this->aOrientation->isNew()) {
					$affectedRows += $this->aOrientation->save($con);
				}
				$this->setOrientation($this->aOrientation);
			}

			if ($this->aSubOrientation !== null) {
				if ($this->aSubOrientation->isModified() || $this->aSubOrientation->isNew()) {
					$affectedRows += $this->aSubOrientation->save($con);
				}
				$this->setSubOrientation($this->aSubOrientation);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CareerSubjectPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CareerSubjectPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CareerSubjectPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collStudentCareerSubjectAlloweds !== null) {
				foreach ($this->collStudentCareerSubjectAlloweds as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentApprovedCareerSubjects !== null) {
				foreach ($this->collStudentApprovedCareerSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collExaminationRepprovedSubjects !== null) {
				foreach ($this->collExaminationRepprovedSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCorrelativesRelatedByCareerSubjectId !== null) {
				foreach ($this->collCorrelativesRelatedByCareerSubjectId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId !== null) {
				foreach ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCareerSubjectSchoolYears !== null) {
				foreach ($this->collCareerSubjectSchoolYears as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareer !== null) {
				if (!$this->aCareer->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCareer->getValidationFailures());
				}
			}

			if ($this->aSubject !== null) {
				if (!$this->aSubject->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSubject->getValidationFailures());
				}
			}

			if ($this->aOrientation !== null) {
				if (!$this->aOrientation->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aOrientation->getValidationFailures());
				}
			}

			if ($this->aSubOrientation !== null) {
				if (!$this->aSubOrientation->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSubOrientation->getValidationFailures());
				}
			}


			if (($retval = CareerSubjectPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collStudentCareerSubjectAlloweds !== null) {
					foreach ($this->collStudentCareerSubjectAlloweds as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentApprovedCareerSubjects !== null) {
					foreach ($this->collStudentApprovedCareerSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collExaminationRepprovedSubjects !== null) {
					foreach ($this->collExaminationRepprovedSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCorrelativesRelatedByCareerSubjectId !== null) {
					foreach ($this->collCorrelativesRelatedByCareerSubjectId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId !== null) {
					foreach ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCareerSubjectSchoolYears !== null) {
					foreach ($this->collCareerSubjectSchoolYears as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerSubjectPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getCreatedAt();
				break;
			case 2:
				return $this->getCareerId();
				break;
			case 3:
				return $this->getSubjectId();
				break;
			case 4:
				return $this->getYear();
				break;
			case 5:
				return $this->getHasOptions();
				break;
			case 6:
				return $this->getIsOption();
				break;
			case 7:
				return $this->getOrientationId();
				break;
			case 8:
				return $this->getSubOrientationId();
				break;
			case 9:
				return $this->getHasCorrelativePreviousYear();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CareerSubjectPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getCreatedAt(),
			$keys[2] => $this->getCareerId(),
			$keys[3] => $this->getSubjectId(),
			$keys[4] => $this->getYear(),
			$keys[5] => $this->getHasOptions(),
			$keys[6] => $this->getIsOption(),
			$keys[7] => $this->getOrientationId(),
			$keys[8] => $this->getSubOrientationId(),
			$keys[9] => $this->getHasCorrelativePreviousYear(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerSubjectPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setCreatedAt($value);
				break;
			case 2:
				$this->setCareerId($value);
				break;
			case 3:
				$this->setSubjectId($value);
				break;
			case 4:
				$this->setYear($value);
				break;
			case 5:
				$this->setHasOptions($value);
				break;
			case 6:
				$this->setIsOption($value);
				break;
			case 7:
				$this->setOrientationId($value);
				break;
			case 8:
				$this->setSubOrientationId($value);
				break;
			case 9:
				$this->setHasCorrelativePreviousYear($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CareerSubjectPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCreatedAt($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCareerId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setSubjectId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setYear($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setHasOptions($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setIsOption($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setOrientationId($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setSubOrientationId($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setHasCorrelativePreviousYear($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);

		if ($this->isColumnModified(CareerSubjectPeer::ID)) $criteria->add(CareerSubjectPeer::ID, $this->id);
		if ($this->isColumnModified(CareerSubjectPeer::CREATED_AT)) $criteria->add(CareerSubjectPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(CareerSubjectPeer::CAREER_ID)) $criteria->add(CareerSubjectPeer::CAREER_ID, $this->career_id);
		if ($this->isColumnModified(CareerSubjectPeer::SUBJECT_ID)) $criteria->add(CareerSubjectPeer::SUBJECT_ID, $this->subject_id);
		if ($this->isColumnModified(CareerSubjectPeer::YEAR)) $criteria->add(CareerSubjectPeer::YEAR, $this->year);
		if ($this->isColumnModified(CareerSubjectPeer::HAS_OPTIONS)) $criteria->add(CareerSubjectPeer::HAS_OPTIONS, $this->has_options);
		if ($this->isColumnModified(CareerSubjectPeer::IS_OPTION)) $criteria->add(CareerSubjectPeer::IS_OPTION, $this->is_option);
		if ($this->isColumnModified(CareerSubjectPeer::ORIENTATION_ID)) $criteria->add(CareerSubjectPeer::ORIENTATION_ID, $this->orientation_id);
		if ($this->isColumnModified(CareerSubjectPeer::SUB_ORIENTATION_ID)) $criteria->add(CareerSubjectPeer::SUB_ORIENTATION_ID, $this->sub_orientation_id);
		if ($this->isColumnModified(CareerSubjectPeer::HAS_CORRELATIVE_PREVIOUS_YEAR)) $criteria->add(CareerSubjectPeer::HAS_CORRELATIVE_PREVIOUS_YEAR, $this->has_correlative_previous_year);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);

		$criteria->add(CareerSubjectPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of CareerSubject (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setCareerId($this->career_id);

		$copyObj->setSubjectId($this->subject_id);

		$copyObj->setYear($this->year);

		$copyObj->setHasOptions($this->has_options);

		$copyObj->setIsOption($this->is_option);

		$copyObj->setOrientationId($this->orientation_id);

		$copyObj->setSubOrientationId($this->sub_orientation_id);

		$copyObj->setHasCorrelativePreviousYear($this->has_correlative_previous_year);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getStudentCareerSubjectAlloweds() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentCareerSubjectAllowed($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentApprovedCareerSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentApprovedCareerSubject($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getExaminationRepprovedSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addExaminationRepprovedSubject($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCorrelativesRelatedByCareerSubjectId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCorrelativeRelatedByCareerSubjectId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCorrelativesRelatedByCorrelativeCareerSubjectId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCorrelativeRelatedByCorrelativeCareerSubjectId($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCareerSubjectSchoolYears() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerSubjectSchoolYear($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     CareerSubject Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CareerSubjectPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CareerSubjectPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Career object.
	 *
	 * @param      Career $v
	 * @return     CareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCareer(Career $v = null)
	{
		if ($v === null) {
			$this->setCareerId(NULL);
		} else {
			$this->setCareerId($v->getId());
		}

		$this->aCareer = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Career object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated Career object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Career The associated Career object.
	 * @throws     PropelException
	 */
	public function getCareer(PropelPDO $con = null)
	{
		if ($this->aCareer === null && ($this->career_id !== null)) {
			$c = new Criteria(CareerPeer::DATABASE_NAME);
			$c->add(CareerPeer::ID, $this->career_id);
			$this->aCareer = CareerPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCareer->addCareerSubjects($this);
			 */
		}
		return $this->aCareer;
	}

	/**
	 * Declares an association between this object and a Subject object.
	 *
	 * @param      Subject $v
	 * @return     CareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSubject(Subject $v = null)
	{
		if ($v === null) {
			$this->setSubjectId(NULL);
		} else {
			$this->setSubjectId($v->getId());
		}

		$this->aSubject = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Subject object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated Subject object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Subject The associated Subject object.
	 * @throws     PropelException
	 */
	public function getSubject(PropelPDO $con = null)
	{
		if ($this->aSubject === null && ($this->subject_id !== null)) {
			$c = new Criteria(SubjectPeer::DATABASE_NAME);
			$c->add(SubjectPeer::ID, $this->subject_id);
			$this->aSubject = SubjectPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSubject->addCareerSubjects($this);
			 */
		}
		return $this->aSubject;
	}

	/**
	 * Declares an association between this object and a Orientation object.
	 *
	 * @param      Orientation $v
	 * @return     CareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setOrientation(Orientation $v = null)
	{
		if ($v === null) {
			$this->setOrientationId(NULL);
		} else {
			$this->setOrientationId($v->getId());
		}

		$this->aOrientation = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Orientation object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated Orientation object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Orientation The associated Orientation object.
	 * @throws     PropelException
	 */
	public function getOrientation(PropelPDO $con = null)
	{
		if ($this->aOrientation === null && ($this->orientation_id !== null)) {
			$c = new Criteria(OrientationPeer::DATABASE_NAME);
			$c->add(OrientationPeer::ID, $this->orientation_id);
			$this->aOrientation = OrientationPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aOrientation->addCareerSubjects($this);
			 */
		}
		return $this->aOrientation;
	}

	/**
	 * Declares an association between this object and a SubOrientation object.
	 *
	 * @param      SubOrientation $v
	 * @return     CareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSubOrientation(SubOrientation $v = null)
	{
		if ($v === null) {
			$this->setSubOrientationId(NULL);
		} else {
			$this->setSubOrientationId($v->getId());
		}

		$this->aSubOrientation = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SubOrientation object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated SubOrientation object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SubOrientation The associated SubOrientation object.
	 * @throws     PropelException
	 */
	public function getSubOrientation(PropelPDO $con = null)
	{
		if ($this->aSubOrientation === null && ($this->sub_orientation_id !== null)) {
			$c = new Criteria(SubOrientationPeer::DATABASE_NAME);
			$c->add(SubOrientationPeer::ID, $this->sub_orientation_id);
			$this->aSubOrientation = SubOrientationPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSubOrientation->addCareerSubjects($this);
			 */
		}
		return $this->aSubOrientation;
	}

	/**
	 * Clears out the collStudentCareerSubjectAlloweds collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentCareerSubjectAlloweds()
	 */
	public function clearStudentCareerSubjectAlloweds()
	{
		$this->collStudentCareerSubjectAlloweds = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentCareerSubjectAlloweds collection (array).
	 *
	 * By default this just sets the collStudentCareerSubjectAlloweds collection to an empty array (like clearcollStudentCareerSubjectAlloweds());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentCareerSubjectAlloweds()
	{
		$this->collStudentCareerSubjectAlloweds = array();
	}

	/**
	 * Gets an array of StudentCareerSubjectAllowed objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSubject has previously been saved, it will retrieve
	 * related StudentCareerSubjectAlloweds from storage. If this CareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentCareerSubjectAllowed[]
	 * @throws     PropelException
	 */
	public function getStudentCareerSubjectAlloweds($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSubjectAlloweds === null) {
			if ($this->isNew()) {
			   $this->collStudentCareerSubjectAlloweds = array();
			} else {

				$criteria->add(StudentCareerSubjectAllowedPeer::CAREER_SUBJECT_ID, $this->id);

				StudentCareerSubjectAllowedPeer::addSelectColumns($criteria);
				$this->collStudentCareerSubjectAlloweds = StudentCareerSubjectAllowedPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentCareerSubjectAllowedPeer::CAREER_SUBJECT_ID, $this->id);

				StudentCareerSubjectAllowedPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentCareerSubjectAllowedCriteria) || !$this->lastStudentCareerSubjectAllowedCriteria->equals($criteria)) {
					$this->collStudentCareerSubjectAlloweds = StudentCareerSubjectAllowedPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentCareerSubjectAllowedCriteria = $criteria;
		return $this->collStudentCareerSubjectAlloweds;
	}

	/**
	 * Returns the number of related StudentCareerSubjectAllowed objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentCareerSubjectAllowed objects.
	 * @throws     PropelException
	 */
	public function countStudentCareerSubjectAlloweds(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentCareerSubjectAlloweds === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentCareerSubjectAllowedPeer::CAREER_SUBJECT_ID, $this->id);

				$count = StudentCareerSubjectAllowedPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentCareerSubjectAllowedPeer::CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastStudentCareerSubjectAllowedCriteria) || !$this->lastStudentCareerSubjectAllowedCriteria->equals($criteria)) {
					$count = StudentCareerSubjectAllowedPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentCareerSubjectAlloweds);
				}
			} else {
				$count = count($this->collStudentCareerSubjectAlloweds);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentCareerSubjectAllowed object to this object
	 * through the StudentCareerSubjectAllowed foreign key attribute.
	 *
	 * @param      StudentCareerSubjectAllowed $l StudentCareerSubjectAllowed
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentCareerSubjectAllowed(StudentCareerSubjectAllowed $l)
	{
		if ($this->collStudentCareerSubjectAlloweds === null) {
			$this->initStudentCareerSubjectAlloweds();
		}
		if (!in_array($l, $this->collStudentCareerSubjectAlloweds, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentCareerSubjectAlloweds, $l);
			$l->setCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSubject is new, it will return
	 * an empty collection; or if this CareerSubject has previously
	 * been saved, it will retrieve related StudentCareerSubjectAlloweds from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSubject.
	 */
	public function getStudentCareerSubjectAllowedsJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSubjectAlloweds === null) {
			if ($this->isNew()) {
				$this->collStudentCareerSubjectAlloweds = array();
			} else {

				$criteria->add(StudentCareerSubjectAllowedPeer::CAREER_SUBJECT_ID, $this->id);

				$this->collStudentCareerSubjectAlloweds = StudentCareerSubjectAllowedPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentCareerSubjectAllowedPeer::CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentCareerSubjectAllowedCriteria) || !$this->lastStudentCareerSubjectAllowedCriteria->equals($criteria)) {
				$this->collStudentCareerSubjectAlloweds = StudentCareerSubjectAllowedPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentCareerSubjectAllowedCriteria = $criteria;

		return $this->collStudentCareerSubjectAlloweds;
	}

	/**
	 * Clears out the collStudentApprovedCareerSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentApprovedCareerSubjects()
	 */
	public function clearStudentApprovedCareerSubjects()
	{
		$this->collStudentApprovedCareerSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentApprovedCareerSubjects collection (array).
	 *
	 * By default this just sets the collStudentApprovedCareerSubjects collection to an empty array (like clearcollStudentApprovedCareerSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentApprovedCareerSubjects()
	{
		$this->collStudentApprovedCareerSubjects = array();
	}

	/**
	 * Gets an array of StudentApprovedCareerSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSubject has previously been saved, it will retrieve
	 * related StudentApprovedCareerSubjects from storage. If this CareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentApprovedCareerSubject[]
	 * @throws     PropelException
	 */
	public function getStudentApprovedCareerSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCareerSubjects === null) {
			if ($this->isNew()) {
			   $this->collStudentApprovedCareerSubjects = array();
			} else {

				$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				StudentApprovedCareerSubjectPeer::addSelectColumns($criteria);
				$this->collStudentApprovedCareerSubjects = StudentApprovedCareerSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				StudentApprovedCareerSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentApprovedCareerSubjectCriteria) || !$this->lastStudentApprovedCareerSubjectCriteria->equals($criteria)) {
					$this->collStudentApprovedCareerSubjects = StudentApprovedCareerSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentApprovedCareerSubjectCriteria = $criteria;
		return $this->collStudentApprovedCareerSubjects;
	}

	/**
	 * Returns the number of related StudentApprovedCareerSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentApprovedCareerSubject objects.
	 * @throws     PropelException
	 */
	public function countStudentApprovedCareerSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentApprovedCareerSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				$count = StudentApprovedCareerSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastStudentApprovedCareerSubjectCriteria) || !$this->lastStudentApprovedCareerSubjectCriteria->equals($criteria)) {
					$count = StudentApprovedCareerSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentApprovedCareerSubjects);
				}
			} else {
				$count = count($this->collStudentApprovedCareerSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentApprovedCareerSubject object to this object
	 * through the StudentApprovedCareerSubject foreign key attribute.
	 *
	 * @param      StudentApprovedCareerSubject $l StudentApprovedCareerSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentApprovedCareerSubject(StudentApprovedCareerSubject $l)
	{
		if ($this->collStudentApprovedCareerSubjects === null) {
			$this->initStudentApprovedCareerSubjects();
		}
		if (!in_array($l, $this->collStudentApprovedCareerSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentApprovedCareerSubjects, $l);
			$l->setCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSubject is new, it will return
	 * an empty collection; or if this CareerSubject has previously
	 * been saved, it will retrieve related StudentApprovedCareerSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSubject.
	 */
	public function getStudentApprovedCareerSubjectsJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCareerSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentApprovedCareerSubjects = array();
			} else {

				$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				$this->collStudentApprovedCareerSubjects = StudentApprovedCareerSubjectPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentApprovedCareerSubjectCriteria) || !$this->lastStudentApprovedCareerSubjectCriteria->equals($criteria)) {
				$this->collStudentApprovedCareerSubjects = StudentApprovedCareerSubjectPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentApprovedCareerSubjectCriteria = $criteria;

		return $this->collStudentApprovedCareerSubjects;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSubject is new, it will return
	 * an empty collection; or if this CareerSubject has previously
	 * been saved, it will retrieve related StudentApprovedCareerSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSubject.
	 */
	public function getStudentApprovedCareerSubjectsJoinSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCareerSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentApprovedCareerSubjects = array();
			} else {

				$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				$this->collStudentApprovedCareerSubjects = StudentApprovedCareerSubjectPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentApprovedCareerSubjectCriteria) || !$this->lastStudentApprovedCareerSubjectCriteria->equals($criteria)) {
				$this->collStudentApprovedCareerSubjects = StudentApprovedCareerSubjectPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentApprovedCareerSubjectCriteria = $criteria;

		return $this->collStudentApprovedCareerSubjects;
	}

	/**
	 * Clears out the collExaminationRepprovedSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addExaminationRepprovedSubjects()
	 */
	public function clearExaminationRepprovedSubjects()
	{
		$this->collExaminationRepprovedSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collExaminationRepprovedSubjects collection (array).
	 *
	 * By default this just sets the collExaminationRepprovedSubjects collection to an empty array (like clearcollExaminationRepprovedSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initExaminationRepprovedSubjects()
	{
		$this->collExaminationRepprovedSubjects = array();
	}

	/**
	 * Gets an array of ExaminationRepprovedSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSubject has previously been saved, it will retrieve
	 * related ExaminationRepprovedSubjects from storage. If this CareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ExaminationRepprovedSubject[]
	 * @throws     PropelException
	 */
	public function getExaminationRepprovedSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collExaminationRepprovedSubjects === null) {
			if ($this->isNew()) {
			   $this->collExaminationRepprovedSubjects = array();
			} else {

				$criteria->add(ExaminationRepprovedSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				ExaminationRepprovedSubjectPeer::addSelectColumns($criteria);
				$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ExaminationRepprovedSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				ExaminationRepprovedSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastExaminationRepprovedSubjectCriteria) || !$this->lastExaminationRepprovedSubjectCriteria->equals($criteria)) {
					$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastExaminationRepprovedSubjectCriteria = $criteria;
		return $this->collExaminationRepprovedSubjects;
	}

	/**
	 * Returns the number of related ExaminationRepprovedSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ExaminationRepprovedSubject objects.
	 * @throws     PropelException
	 */
	public function countExaminationRepprovedSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collExaminationRepprovedSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ExaminationRepprovedSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				$count = ExaminationRepprovedSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ExaminationRepprovedSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastExaminationRepprovedSubjectCriteria) || !$this->lastExaminationRepprovedSubjectCriteria->equals($criteria)) {
					$count = ExaminationRepprovedSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collExaminationRepprovedSubjects);
				}
			} else {
				$count = count($this->collExaminationRepprovedSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ExaminationRepprovedSubject object to this object
	 * through the ExaminationRepprovedSubject foreign key attribute.
	 *
	 * @param      ExaminationRepprovedSubject $l ExaminationRepprovedSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addExaminationRepprovedSubject(ExaminationRepprovedSubject $l)
	{
		if ($this->collExaminationRepprovedSubjects === null) {
			$this->initExaminationRepprovedSubjects();
		}
		if (!in_array($l, $this->collExaminationRepprovedSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collExaminationRepprovedSubjects, $l);
			$l->setCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSubject is new, it will return
	 * an empty collection; or if this CareerSubject has previously
	 * been saved, it will retrieve related ExaminationRepprovedSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSubject.
	 */
	public function getExaminationRepprovedSubjectsJoinExaminationRepproved($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collExaminationRepprovedSubjects === null) {
			if ($this->isNew()) {
				$this->collExaminationRepprovedSubjects = array();
			} else {

				$criteria->add(ExaminationRepprovedSubjectPeer::CAREER_SUBJECT_ID, $this->id);

				$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelectJoinExaminationRepproved($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ExaminationRepprovedSubjectPeer::CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastExaminationRepprovedSubjectCriteria) || !$this->lastExaminationRepprovedSubjectCriteria->equals($criteria)) {
				$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelectJoinExaminationRepproved($criteria, $con, $join_behavior);
			}
		}
		$this->lastExaminationRepprovedSubjectCriteria = $criteria;

		return $this->collExaminationRepprovedSubjects;
	}

	/**
	 * Clears out the collCorrelativesRelatedByCareerSubjectId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCorrelativesRelatedByCareerSubjectId()
	 */
	public function clearCorrelativesRelatedByCareerSubjectId()
	{
		$this->collCorrelativesRelatedByCareerSubjectId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCorrelativesRelatedByCareerSubjectId collection (array).
	 *
	 * By default this just sets the collCorrelativesRelatedByCareerSubjectId collection to an empty array (like clearcollCorrelativesRelatedByCareerSubjectId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCorrelativesRelatedByCareerSubjectId()
	{
		$this->collCorrelativesRelatedByCareerSubjectId = array();
	}

	/**
	 * Gets an array of Correlative objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSubject has previously been saved, it will retrieve
	 * related CorrelativesRelatedByCareerSubjectId from storage. If this CareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Correlative[]
	 * @throws     PropelException
	 */
	public function getCorrelativesRelatedByCareerSubjectId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCorrelativesRelatedByCareerSubjectId === null) {
			if ($this->isNew()) {
			   $this->collCorrelativesRelatedByCareerSubjectId = array();
			} else {

				$criteria->add(CorrelativePeer::CAREER_SUBJECT_ID, $this->id);

				CorrelativePeer::addSelectColumns($criteria);
				$this->collCorrelativesRelatedByCareerSubjectId = CorrelativePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CorrelativePeer::CAREER_SUBJECT_ID, $this->id);

				CorrelativePeer::addSelectColumns($criteria);
				if (!isset($this->lastCorrelativeRelatedByCareerSubjectIdCriteria) || !$this->lastCorrelativeRelatedByCareerSubjectIdCriteria->equals($criteria)) {
					$this->collCorrelativesRelatedByCareerSubjectId = CorrelativePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCorrelativeRelatedByCareerSubjectIdCriteria = $criteria;
		return $this->collCorrelativesRelatedByCareerSubjectId;
	}

	/**
	 * Returns the number of related Correlative objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Correlative objects.
	 * @throws     PropelException
	 */
	public function countCorrelativesRelatedByCareerSubjectId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCorrelativesRelatedByCareerSubjectId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CorrelativePeer::CAREER_SUBJECT_ID, $this->id);

				$count = CorrelativePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CorrelativePeer::CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastCorrelativeRelatedByCareerSubjectIdCriteria) || !$this->lastCorrelativeRelatedByCareerSubjectIdCriteria->equals($criteria)) {
					$count = CorrelativePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCorrelativesRelatedByCareerSubjectId);
				}
			} else {
				$count = count($this->collCorrelativesRelatedByCareerSubjectId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Correlative object to this object
	 * through the Correlative foreign key attribute.
	 *
	 * @param      Correlative $l Correlative
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCorrelativeRelatedByCareerSubjectId(Correlative $l)
	{
		if ($this->collCorrelativesRelatedByCareerSubjectId === null) {
			$this->initCorrelativesRelatedByCareerSubjectId();
		}
		if (!in_array($l, $this->collCorrelativesRelatedByCareerSubjectId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCorrelativesRelatedByCareerSubjectId, $l);
			$l->setCareerSubjectRelatedByCareerSubjectId($this);
		}
	}

	/**
	 * Clears out the collCorrelativesRelatedByCorrelativeCareerSubjectId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCorrelativesRelatedByCorrelativeCareerSubjectId()
	 */
	public function clearCorrelativesRelatedByCorrelativeCareerSubjectId()
	{
		$this->collCorrelativesRelatedByCorrelativeCareerSubjectId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCorrelativesRelatedByCorrelativeCareerSubjectId collection (array).
	 *
	 * By default this just sets the collCorrelativesRelatedByCorrelativeCareerSubjectId collection to an empty array (like clearcollCorrelativesRelatedByCorrelativeCareerSubjectId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCorrelativesRelatedByCorrelativeCareerSubjectId()
	{
		$this->collCorrelativesRelatedByCorrelativeCareerSubjectId = array();
	}

	/**
	 * Gets an array of Correlative objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSubject has previously been saved, it will retrieve
	 * related CorrelativesRelatedByCorrelativeCareerSubjectId from storage. If this CareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Correlative[]
	 * @throws     PropelException
	 */
	public function getCorrelativesRelatedByCorrelativeCareerSubjectId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId === null) {
			if ($this->isNew()) {
			   $this->collCorrelativesRelatedByCorrelativeCareerSubjectId = array();
			} else {

				$criteria->add(CorrelativePeer::CORRELATIVE_CAREER_SUBJECT_ID, $this->id);

				CorrelativePeer::addSelectColumns($criteria);
				$this->collCorrelativesRelatedByCorrelativeCareerSubjectId = CorrelativePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CorrelativePeer::CORRELATIVE_CAREER_SUBJECT_ID, $this->id);

				CorrelativePeer::addSelectColumns($criteria);
				if (!isset($this->lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria) || !$this->lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria->equals($criteria)) {
					$this->collCorrelativesRelatedByCorrelativeCareerSubjectId = CorrelativePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria = $criteria;
		return $this->collCorrelativesRelatedByCorrelativeCareerSubjectId;
	}

	/**
	 * Returns the number of related Correlative objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Correlative objects.
	 * @throws     PropelException
	 */
	public function countCorrelativesRelatedByCorrelativeCareerSubjectId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CorrelativePeer::CORRELATIVE_CAREER_SUBJECT_ID, $this->id);

				$count = CorrelativePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CorrelativePeer::CORRELATIVE_CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria) || !$this->lastCorrelativeRelatedByCorrelativeCareerSubjectIdCriteria->equals($criteria)) {
					$count = CorrelativePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCorrelativesRelatedByCorrelativeCareerSubjectId);
				}
			} else {
				$count = count($this->collCorrelativesRelatedByCorrelativeCareerSubjectId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Correlative object to this object
	 * through the Correlative foreign key attribute.
	 *
	 * @param      Correlative $l Correlative
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCorrelativeRelatedByCorrelativeCareerSubjectId(Correlative $l)
	{
		if ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId === null) {
			$this->initCorrelativesRelatedByCorrelativeCareerSubjectId();
		}
		if (!in_array($l, $this->collCorrelativesRelatedByCorrelativeCareerSubjectId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCorrelativesRelatedByCorrelativeCareerSubjectId, $l);
			$l->setCareerSubjectRelatedByCorrelativeCareerSubjectId($this);
		}
	}

	/**
	 * Clears out the collCareerSubjectSchoolYears collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerSubjectSchoolYears()
	 */
	public function clearCareerSubjectSchoolYears()
	{
		$this->collCareerSubjectSchoolYears = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerSubjectSchoolYears collection (array).
	 *
	 * By default this just sets the collCareerSubjectSchoolYears collection to an empty array (like clearcollCareerSubjectSchoolYears());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerSubjectSchoolYears()
	{
		$this->collCareerSubjectSchoolYears = array();
	}

	/**
	 * Gets an array of CareerSubjectSchoolYear objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSubject has previously been saved, it will retrieve
	 * related CareerSubjectSchoolYears from storage. If this CareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerSubjectSchoolYear[]
	 * @throws     PropelException
	 */
	public function getCareerSubjectSchoolYears($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
			   $this->collCareerSubjectSchoolYears = array();
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

				CareerSubjectSchoolYearPeer::addSelectColumns($criteria);
				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

				CareerSubjectSchoolYearPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
					$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerSubjectSchoolYearCriteria = $criteria;
		return $this->collCareerSubjectSchoolYears;
	}

	/**
	 * Returns the number of related CareerSubjectSchoolYear objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerSubjectSchoolYear objects.
	 * @throws     PropelException
	 */
	public function countCareerSubjectSchoolYears(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

				$count = CareerSubjectSchoolYearPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
					$count = CareerSubjectSchoolYearPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerSubjectSchoolYears);
				}
			} else {
				$count = count($this->collCareerSubjectSchoolYears);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerSubjectSchoolYear object to this object
	 * through the CareerSubjectSchoolYear foreign key attribute.
	 *
	 * @param      CareerSubjectSchoolYear $l CareerSubjectSchoolYear
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerSubjectSchoolYear(CareerSubjectSchoolYear $l)
	{
		if ($this->collCareerSubjectSchoolYears === null) {
			$this->initCareerSubjectSchoolYears();
		}
		if (!in_array($l, $this->collCareerSubjectSchoolYears, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerSubjectSchoolYears, $l);
			$l->setCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSubject is new, it will return
	 * an empty collection; or if this CareerSubject has previously
	 * been saved, it will retrieve related CareerSubjectSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSubject.
	 */
	public function getCareerSubjectSchoolYearsJoinCareerSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
				$this->collCareerSubjectSchoolYears = array();
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinCareerSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinCareerSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectSchoolYearCriteria = $criteria;

		return $this->collCareerSubjectSchoolYears;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSubject is new, it will return
	 * an empty collection; or if this CareerSubject has previously
	 * been saved, it will retrieve related CareerSubjectSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSubject.
	 */
	public function getCareerSubjectSchoolYearsJoinSubjectConfiguration($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
				$this->collCareerSubjectSchoolYears = array();
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinSubjectConfiguration($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinSubjectConfiguration($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectSchoolYearCriteria = $criteria;

		return $this->collCareerSubjectSchoolYears;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collStudentCareerSubjectAlloweds) {
				foreach ((array) $this->collStudentCareerSubjectAlloweds as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentApprovedCareerSubjects) {
				foreach ((array) $this->collStudentApprovedCareerSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collExaminationRepprovedSubjects) {
				foreach ((array) $this->collExaminationRepprovedSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCorrelativesRelatedByCareerSubjectId) {
				foreach ((array) $this->collCorrelativesRelatedByCareerSubjectId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCorrelativesRelatedByCorrelativeCareerSubjectId) {
				foreach ((array) $this->collCorrelativesRelatedByCorrelativeCareerSubjectId as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCareerSubjectSchoolYears) {
				foreach ((array) $this->collCareerSubjectSchoolYears as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collStudentCareerSubjectAlloweds = null;
		$this->collStudentApprovedCareerSubjects = null;
		$this->collExaminationRepprovedSubjects = null;
		$this->collCorrelativesRelatedByCareerSubjectId = null;
		$this->collCorrelativesRelatedByCorrelativeCareerSubjectId = null;
		$this->collCareerSubjectSchoolYears = null;
			$this->aCareer = null;
			$this->aSubject = null;
			$this->aOrientation = null;
			$this->aSubOrientation = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseCareerSubject:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseCareerSubject::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseCareerSubject
