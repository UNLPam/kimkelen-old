<?php

/**
 * Base class that represents a row from the 'course' table.
 *
 * Representa un curso para una materia
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:08 2018
 *
 * @package    lib.model.om
 */
abstract class BaseCourse extends BaseObject  implements Persistent {


  const PEER = 'CoursePeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CoursePeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the starts_at field.
	 * @var        string
	 */
	protected $starts_at;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the quota field.
	 * @var        int
	 */
	protected $quota;

	/**
	 * The value for the school_year_id field.
	 * @var        int
	 */
	protected $school_year_id;

	/**
	 * The value for the division_id field.
	 * @var        int
	 */
	protected $division_id;

	/**
	 * The value for the related_division_id field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $related_division_id;

	/**
	 * The value for the is_closed field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_closed;

	/**
	 * The value for the current_period field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $current_period;

	/**
	 * The value for the is_pathway field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_pathway;

	/**
	 * @var        SchoolYear
	 */
	protected $aSchoolYear;

	/**
	 * @var        Division
	 */
	protected $aDivisionRelatedByDivisionId;

	/**
	 * @var        Division
	 */
	protected $aDivisionRelatedByRelatedDivisionId;

	/**
	 * @var        array CoursePreceptor[] Collection to store aggregation of CoursePreceptor objects.
	 */
	protected $collCoursePreceptors;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCoursePreceptors.
	 */
	private $lastCoursePreceptorCriteria = null;

	/**
	 * @var        array CourseSubject[] Collection to store aggregation of CourseSubject objects.
	 */
	protected $collCourseSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCourseSubjects.
	 */
	private $lastCourseSubjectCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseCourse object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->related_division_id = 0;
		$this->is_closed = false;
		$this->current_period = 1;
		$this->is_pathway = false;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [optionally formatted] temporal [starts_at] column value.
	 * Indica el dia que se empieza a cursar
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getStartsAt($format = 'Y-m-d')
	{
		if ($this->starts_at === null) {
			return null;
		}


		if ($this->starts_at === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->starts_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->starts_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [name] column value.
	 * Nombre del curso
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [quota] column value.
	 * Entero que representa la capacidad de alumnos que pueden estar inscriptos
	 * @return     int
	 */
	public function getQuota()
	{
		return $this->quota;
	}

	/**
	 * Get the [school_year_id] column value.
	 * 
	 * @return     int
	 */
	public function getSchoolYearId()
	{
		return $this->school_year_id;
	}

	/**
	 * Get the [division_id] column value.
	 * 
	 * @return     int
	 */
	public function getDivisionId()
	{
		return $this->division_id;
	}

	/**
	 * Get the [related_division_id] column value.
	 * En caso de que una comision, se quiera relacionar con una division.
	 * @return     int
	 */
	public function getRelatedDivisionId()
	{
		return $this->related_division_id;
	}

	/**
	 * Get the [is_closed] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsClosed()
	{
		return $this->is_closed;
	}

	/**
	 * Get the [current_period] column value.
	 * 
	 * @return     int
	 */
	public function getCurrentPeriod()
	{
		return $this->current_period;
	}

	/**
	 * Get the [is_pathway] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsPathway()
	{
		return $this->is_pathway;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CoursePeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Sets the value of [starts_at] column to a normalized version of the date/time value specified.
	 * Indica el dia que se empieza a cursar
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Course The current object (for fluent API support)
	 */
	public function setStartsAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->starts_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->starts_at !== null && $tmpDt = new DateTime($this->starts_at)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->starts_at = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = CoursePeer::STARTS_AT;
			}
		} // if either are not null

		return $this;
	} // setStartsAt()

	/**
	 * Set the value of [name] column.
	 * Nombre del curso
	 * @param      string $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = CoursePeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [quota] column.
	 * Entero que representa la capacidad de alumnos que pueden estar inscriptos
	 * @param      int $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setQuota($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->quota !== $v) {
			$this->quota = $v;
			$this->modifiedColumns[] = CoursePeer::QUOTA;
		}

		return $this;
	} // setQuota()

	/**
	 * Set the value of [school_year_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setSchoolYearId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->school_year_id !== $v) {
			$this->school_year_id = $v;
			$this->modifiedColumns[] = CoursePeer::SCHOOL_YEAR_ID;
		}

		if ($this->aSchoolYear !== null && $this->aSchoolYear->getId() !== $v) {
			$this->aSchoolYear = null;
		}

		return $this;
	} // setSchoolYearId()

	/**
	 * Set the value of [division_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setDivisionId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->division_id !== $v) {
			$this->division_id = $v;
			$this->modifiedColumns[] = CoursePeer::DIVISION_ID;
		}

		if ($this->aDivisionRelatedByDivisionId !== null && $this->aDivisionRelatedByDivisionId->getId() !== $v) {
			$this->aDivisionRelatedByDivisionId = null;
		}

		return $this;
	} // setDivisionId()

	/**
	 * Set the value of [related_division_id] column.
	 * En caso de que una comision, se quiera relacionar con una division.
	 * @param      int $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setRelatedDivisionId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->related_division_id !== $v || $v === 0) {
			$this->related_division_id = $v;
			$this->modifiedColumns[] = CoursePeer::RELATED_DIVISION_ID;
		}

		if ($this->aDivisionRelatedByRelatedDivisionId !== null && $this->aDivisionRelatedByRelatedDivisionId->getId() !== $v) {
			$this->aDivisionRelatedByRelatedDivisionId = null;
		}

		return $this;
	} // setRelatedDivisionId()

	/**
	 * Set the value of [is_closed] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setIsClosed($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_closed !== $v || $v === false) {
			$this->is_closed = $v;
			$this->modifiedColumns[] = CoursePeer::IS_CLOSED;
		}

		return $this;
	} // setIsClosed()

	/**
	 * Set the value of [current_period] column.
	 * 
	 * @param      int $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setCurrentPeriod($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->current_period !== $v || $v === 1) {
			$this->current_period = $v;
			$this->modifiedColumns[] = CoursePeer::CURRENT_PERIOD;
		}

		return $this;
	} // setCurrentPeriod()

	/**
	 * Set the value of [is_pathway] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Course The current object (for fluent API support)
	 */
	public function setIsPathway($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_pathway !== $v || $v === false) {
			$this->is_pathway = $v;
			$this->modifiedColumns[] = CoursePeer::IS_PATHWAY;
		}

		return $this;
	} // setIsPathway()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(CoursePeer::RELATED_DIVISION_ID,CoursePeer::IS_CLOSED,CoursePeer::CURRENT_PERIOD,CoursePeer::IS_PATHWAY))) {
				return false;
			}

			if ($this->related_division_id !== 0) {
				return false;
			}

			if ($this->is_closed !== false) {
				return false;
			}

			if ($this->current_period !== 1) {
				return false;
			}

			if ($this->is_pathway !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->starts_at = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->name = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->quota = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->school_year_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->division_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->related_division_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->is_closed = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
			$this->current_period = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->is_pathway = ($row[$startcol + 9] !== null) ? (boolean) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = CoursePeer::NUM_COLUMNS - CoursePeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Course object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSchoolYear !== null && $this->school_year_id !== $this->aSchoolYear->getId()) {
			$this->aSchoolYear = null;
		}
		if ($this->aDivisionRelatedByDivisionId !== null && $this->division_id !== $this->aDivisionRelatedByDivisionId->getId()) {
			$this->aDivisionRelatedByDivisionId = null;
		}
		if ($this->aDivisionRelatedByRelatedDivisionId !== null && $this->related_division_id !== $this->aDivisionRelatedByRelatedDivisionId->getId()) {
			$this->aDivisionRelatedByRelatedDivisionId = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CoursePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CoursePeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSchoolYear = null;
			$this->aDivisionRelatedByDivisionId = null;
			$this->aDivisionRelatedByRelatedDivisionId = null;
			$this->collCoursePreceptors = null;
			$this->lastCoursePreceptorCriteria = null;

			$this->collCourseSubjects = null;
			$this->lastCourseSubjectCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCourse:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CoursePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			CoursePeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseCourse:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCourse:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CoursePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseCourse:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			CoursePeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSchoolYear !== null) {
				if ($this->aSchoolYear->isModified() || $this->aSchoolYear->isNew()) {
					$affectedRows += $this->aSchoolYear->save($con);
				}
				$this->setSchoolYear($this->aSchoolYear);
			}

			if ($this->aDivisionRelatedByDivisionId !== null) {
				if ($this->aDivisionRelatedByDivisionId->isModified() || $this->aDivisionRelatedByDivisionId->isNew()) {
					$affectedRows += $this->aDivisionRelatedByDivisionId->save($con);
				}
				$this->setDivisionRelatedByDivisionId($this->aDivisionRelatedByDivisionId);
			}

			if ($this->aDivisionRelatedByRelatedDivisionId !== null) {
				if ($this->aDivisionRelatedByRelatedDivisionId->isModified() || $this->aDivisionRelatedByRelatedDivisionId->isNew()) {
					$affectedRows += $this->aDivisionRelatedByRelatedDivisionId->save($con);
				}
				$this->setDivisionRelatedByRelatedDivisionId($this->aDivisionRelatedByRelatedDivisionId);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CoursePeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CoursePeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CoursePeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCoursePreceptors !== null) {
				foreach ($this->collCoursePreceptors as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCourseSubjects !== null) {
				foreach ($this->collCourseSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSchoolYear !== null) {
				if (!$this->aSchoolYear->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSchoolYear->getValidationFailures());
				}
			}

			if ($this->aDivisionRelatedByDivisionId !== null) {
				if (!$this->aDivisionRelatedByDivisionId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDivisionRelatedByDivisionId->getValidationFailures());
				}
			}

			if ($this->aDivisionRelatedByRelatedDivisionId !== null) {
				if (!$this->aDivisionRelatedByRelatedDivisionId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aDivisionRelatedByRelatedDivisionId->getValidationFailures());
				}
			}


			if (($retval = CoursePeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCoursePreceptors !== null) {
					foreach ($this->collCoursePreceptors as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCourseSubjects !== null) {
					foreach ($this->collCourseSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CoursePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getStartsAt();
				break;
			case 2:
				return $this->getName();
				break;
			case 3:
				return $this->getQuota();
				break;
			case 4:
				return $this->getSchoolYearId();
				break;
			case 5:
				return $this->getDivisionId();
				break;
			case 6:
				return $this->getRelatedDivisionId();
				break;
			case 7:
				return $this->getIsClosed();
				break;
			case 8:
				return $this->getCurrentPeriod();
				break;
			case 9:
				return $this->getIsPathway();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CoursePeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getStartsAt(),
			$keys[2] => $this->getName(),
			$keys[3] => $this->getQuota(),
			$keys[4] => $this->getSchoolYearId(),
			$keys[5] => $this->getDivisionId(),
			$keys[6] => $this->getRelatedDivisionId(),
			$keys[7] => $this->getIsClosed(),
			$keys[8] => $this->getCurrentPeriod(),
			$keys[9] => $this->getIsPathway(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CoursePeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setStartsAt($value);
				break;
			case 2:
				$this->setName($value);
				break;
			case 3:
				$this->setQuota($value);
				break;
			case 4:
				$this->setSchoolYearId($value);
				break;
			case 5:
				$this->setDivisionId($value);
				break;
			case 6:
				$this->setRelatedDivisionId($value);
				break;
			case 7:
				$this->setIsClosed($value);
				break;
			case 8:
				$this->setCurrentPeriod($value);
				break;
			case 9:
				$this->setIsPathway($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CoursePeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setStartsAt($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setName($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setQuota($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setSchoolYearId($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setDivisionId($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setRelatedDivisionId($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setIsClosed($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCurrentPeriod($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setIsPathway($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CoursePeer::DATABASE_NAME);

		if ($this->isColumnModified(CoursePeer::ID)) $criteria->add(CoursePeer::ID, $this->id);
		if ($this->isColumnModified(CoursePeer::STARTS_AT)) $criteria->add(CoursePeer::STARTS_AT, $this->starts_at);
		if ($this->isColumnModified(CoursePeer::NAME)) $criteria->add(CoursePeer::NAME, $this->name);
		if ($this->isColumnModified(CoursePeer::QUOTA)) $criteria->add(CoursePeer::QUOTA, $this->quota);
		if ($this->isColumnModified(CoursePeer::SCHOOL_YEAR_ID)) $criteria->add(CoursePeer::SCHOOL_YEAR_ID, $this->school_year_id);
		if ($this->isColumnModified(CoursePeer::DIVISION_ID)) $criteria->add(CoursePeer::DIVISION_ID, $this->division_id);
		if ($this->isColumnModified(CoursePeer::RELATED_DIVISION_ID)) $criteria->add(CoursePeer::RELATED_DIVISION_ID, $this->related_division_id);
		if ($this->isColumnModified(CoursePeer::IS_CLOSED)) $criteria->add(CoursePeer::IS_CLOSED, $this->is_closed);
		if ($this->isColumnModified(CoursePeer::CURRENT_PERIOD)) $criteria->add(CoursePeer::CURRENT_PERIOD, $this->current_period);
		if ($this->isColumnModified(CoursePeer::IS_PATHWAY)) $criteria->add(CoursePeer::IS_PATHWAY, $this->is_pathway);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CoursePeer::DATABASE_NAME);

		$criteria->add(CoursePeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Course (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setStartsAt($this->starts_at);

		$copyObj->setName($this->name);

		$copyObj->setQuota($this->quota);

		$copyObj->setSchoolYearId($this->school_year_id);

		$copyObj->setDivisionId($this->division_id);

		$copyObj->setRelatedDivisionId($this->related_division_id);

		$copyObj->setIsClosed($this->is_closed);

		$copyObj->setCurrentPeriod($this->current_period);

		$copyObj->setIsPathway($this->is_pathway);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCoursePreceptors() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCoursePreceptor($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCourseSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCourseSubject($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Course Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CoursePeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CoursePeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SchoolYear object.
	 *
	 * @param      SchoolYear $v
	 * @return     Course The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSchoolYear(SchoolYear $v = null)
	{
		if ($v === null) {
			$this->setSchoolYearId(NULL);
		} else {
			$this->setSchoolYearId($v->getId());
		}

		$this->aSchoolYear = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SchoolYear object, it will not be re-added.
		if ($v !== null) {
			$v->addCourse($this);
		}

		return $this;
	}


	/**
	 * Get the associated SchoolYear object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SchoolYear The associated SchoolYear object.
	 * @throws     PropelException
	 */
	public function getSchoolYear(PropelPDO $con = null)
	{
		if ($this->aSchoolYear === null && ($this->school_year_id !== null)) {
			$c = new Criteria(SchoolYearPeer::DATABASE_NAME);
			$c->add(SchoolYearPeer::ID, $this->school_year_id);
			$this->aSchoolYear = SchoolYearPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSchoolYear->addCourses($this);
			 */
		}
		return $this->aSchoolYear;
	}

	/**
	 * Declares an association between this object and a Division object.
	 *
	 * @param      Division $v
	 * @return     Course The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDivisionRelatedByDivisionId(Division $v = null)
	{
		if ($v === null) {
			$this->setDivisionId(NULL);
		} else {
			$this->setDivisionId($v->getId());
		}

		$this->aDivisionRelatedByDivisionId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Division object, it will not be re-added.
		if ($v !== null) {
			$v->addCourseRelatedByDivisionId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Division object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Division The associated Division object.
	 * @throws     PropelException
	 */
	public function getDivisionRelatedByDivisionId(PropelPDO $con = null)
	{
		if ($this->aDivisionRelatedByDivisionId === null && ($this->division_id !== null)) {
			$c = new Criteria(DivisionPeer::DATABASE_NAME);
			$c->add(DivisionPeer::ID, $this->division_id);
			$this->aDivisionRelatedByDivisionId = DivisionPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDivisionRelatedByDivisionId->addCoursesRelatedByDivisionId($this);
			 */
		}
		return $this->aDivisionRelatedByDivisionId;
	}

	/**
	 * Declares an association between this object and a Division object.
	 *
	 * @param      Division $v
	 * @return     Course The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDivisionRelatedByRelatedDivisionId(Division $v = null)
	{
		if ($v === null) {
			$this->setRelatedDivisionId(0);
		} else {
			$this->setRelatedDivisionId($v->getId());
		}

		$this->aDivisionRelatedByRelatedDivisionId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Division object, it will not be re-added.
		if ($v !== null) {
			$v->addCourseRelatedByRelatedDivisionId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Division object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Division The associated Division object.
	 * @throws     PropelException
	 */
	public function getDivisionRelatedByRelatedDivisionId(PropelPDO $con = null)
	{
		if ($this->aDivisionRelatedByRelatedDivisionId === null && ($this->related_division_id !== null)) {
			$c = new Criteria(DivisionPeer::DATABASE_NAME);
			$c->add(DivisionPeer::ID, $this->related_division_id);
			$this->aDivisionRelatedByRelatedDivisionId = DivisionPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aDivisionRelatedByRelatedDivisionId->addCoursesRelatedByRelatedDivisionId($this);
			 */
		}
		return $this->aDivisionRelatedByRelatedDivisionId;
	}

	/**
	 * Clears out the collCoursePreceptors collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCoursePreceptors()
	 */
	public function clearCoursePreceptors()
	{
		$this->collCoursePreceptors = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCoursePreceptors collection (array).
	 *
	 * By default this just sets the collCoursePreceptors collection to an empty array (like clearcollCoursePreceptors());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCoursePreceptors()
	{
		$this->collCoursePreceptors = array();
	}

	/**
	 * Gets an array of CoursePreceptor objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Course has previously been saved, it will retrieve
	 * related CoursePreceptors from storage. If this Course is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CoursePreceptor[]
	 * @throws     PropelException
	 */
	public function getCoursePreceptors($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CoursePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCoursePreceptors === null) {
			if ($this->isNew()) {
			   $this->collCoursePreceptors = array();
			} else {

				$criteria->add(CoursePreceptorPeer::COURSE_ID, $this->id);

				CoursePreceptorPeer::addSelectColumns($criteria);
				$this->collCoursePreceptors = CoursePreceptorPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CoursePreceptorPeer::COURSE_ID, $this->id);

				CoursePreceptorPeer::addSelectColumns($criteria);
				if (!isset($this->lastCoursePreceptorCriteria) || !$this->lastCoursePreceptorCriteria->equals($criteria)) {
					$this->collCoursePreceptors = CoursePreceptorPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCoursePreceptorCriteria = $criteria;
		return $this->collCoursePreceptors;
	}

	/**
	 * Returns the number of related CoursePreceptor objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CoursePreceptor objects.
	 * @throws     PropelException
	 */
	public function countCoursePreceptors(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CoursePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCoursePreceptors === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CoursePreceptorPeer::COURSE_ID, $this->id);

				$count = CoursePreceptorPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CoursePreceptorPeer::COURSE_ID, $this->id);

				if (!isset($this->lastCoursePreceptorCriteria) || !$this->lastCoursePreceptorCriteria->equals($criteria)) {
					$count = CoursePreceptorPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCoursePreceptors);
				}
			} else {
				$count = count($this->collCoursePreceptors);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CoursePreceptor object to this object
	 * through the CoursePreceptor foreign key attribute.
	 *
	 * @param      CoursePreceptor $l CoursePreceptor
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCoursePreceptor(CoursePreceptor $l)
	{
		if ($this->collCoursePreceptors === null) {
			$this->initCoursePreceptors();
		}
		if (!in_array($l, $this->collCoursePreceptors, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCoursePreceptors, $l);
			$l->setCourse($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Course is new, it will return
	 * an empty collection; or if this Course has previously
	 * been saved, it will retrieve related CoursePreceptors from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Course.
	 */
	public function getCoursePreceptorsJoinPersonal($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CoursePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCoursePreceptors === null) {
			if ($this->isNew()) {
				$this->collCoursePreceptors = array();
			} else {

				$criteria->add(CoursePreceptorPeer::COURSE_ID, $this->id);

				$this->collCoursePreceptors = CoursePreceptorPeer::doSelectJoinPersonal($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CoursePreceptorPeer::COURSE_ID, $this->id);

			if (!isset($this->lastCoursePreceptorCriteria) || !$this->lastCoursePreceptorCriteria->equals($criteria)) {
				$this->collCoursePreceptors = CoursePreceptorPeer::doSelectJoinPersonal($criteria, $con, $join_behavior);
			}
		}
		$this->lastCoursePreceptorCriteria = $criteria;

		return $this->collCoursePreceptors;
	}

	/**
	 * Clears out the collCourseSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCourseSubjects()
	 */
	public function clearCourseSubjects()
	{
		$this->collCourseSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCourseSubjects collection (array).
	 *
	 * By default this just sets the collCourseSubjects collection to an empty array (like clearcollCourseSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCourseSubjects()
	{
		$this->collCourseSubjects = array();
	}

	/**
	 * Gets an array of CourseSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Course has previously been saved, it will retrieve
	 * related CourseSubjects from storage. If this Course is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CourseSubject[]
	 * @throws     PropelException
	 */
	public function getCourseSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CoursePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCourseSubjects === null) {
			if ($this->isNew()) {
			   $this->collCourseSubjects = array();
			} else {

				$criteria->add(CourseSubjectPeer::COURSE_ID, $this->id);

				CourseSubjectPeer::addSelectColumns($criteria);
				$this->collCourseSubjects = CourseSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CourseSubjectPeer::COURSE_ID, $this->id);

				CourseSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastCourseSubjectCriteria) || !$this->lastCourseSubjectCriteria->equals($criteria)) {
					$this->collCourseSubjects = CourseSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCourseSubjectCriteria = $criteria;
		return $this->collCourseSubjects;
	}

	/**
	 * Returns the number of related CourseSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CourseSubject objects.
	 * @throws     PropelException
	 */
	public function countCourseSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CoursePeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCourseSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CourseSubjectPeer::COURSE_ID, $this->id);

				$count = CourseSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CourseSubjectPeer::COURSE_ID, $this->id);

				if (!isset($this->lastCourseSubjectCriteria) || !$this->lastCourseSubjectCriteria->equals($criteria)) {
					$count = CourseSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCourseSubjects);
				}
			} else {
				$count = count($this->collCourseSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CourseSubject object to this object
	 * through the CourseSubject foreign key attribute.
	 *
	 * @param      CourseSubject $l CourseSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCourseSubject(CourseSubject $l)
	{
		if ($this->collCourseSubjects === null) {
			$this->initCourseSubjects();
		}
		if (!in_array($l, $this->collCourseSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCourseSubjects, $l);
			$l->setCourse($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Course is new, it will return
	 * an empty collection; or if this Course has previously
	 * been saved, it will retrieve related CourseSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Course.
	 */
	public function getCourseSubjectsJoinCareerSubjectSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CoursePeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCourseSubjects === null) {
			if ($this->isNew()) {
				$this->collCourseSubjects = array();
			} else {

				$criteria->add(CourseSubjectPeer::COURSE_ID, $this->id);

				$this->collCourseSubjects = CourseSubjectPeer::doSelectJoinCareerSubjectSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CourseSubjectPeer::COURSE_ID, $this->id);

			if (!isset($this->lastCourseSubjectCriteria) || !$this->lastCourseSubjectCriteria->equals($criteria)) {
				$this->collCourseSubjects = CourseSubjectPeer::doSelectJoinCareerSubjectSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastCourseSubjectCriteria = $criteria;

		return $this->collCourseSubjects;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCoursePreceptors) {
				foreach ((array) $this->collCoursePreceptors as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCourseSubjects) {
				foreach ((array) $this->collCourseSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCoursePreceptors = null;
		$this->collCourseSubjects = null;
			$this->aSchoolYear = null;
			$this->aDivisionRelatedByDivisionId = null;
			$this->aDivisionRelatedByRelatedDivisionId = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseCourse:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseCourse::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseCourse
