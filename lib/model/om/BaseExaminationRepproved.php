<?php

/**
 * Base class that represents a row from the 'examination_repproved' table.
 *
 * Representa una mesa de previa.
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseExaminationRepproved extends BaseObject  implements Persistent {


  const PEER = 'ExaminationRepprovedPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        ExaminationRepprovedPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the date_from field.
	 * @var        string
	 */
	protected $date_from;

	/**
	 * The value for the date_to field.
	 * @var        string
	 */
	protected $date_to;

	/**
	 * The value for the examination_number field.
	 * Note: this column has a database default value of: 1
	 * @var        int
	 */
	protected $examination_number;

	/**
	 * The value for the examination_type field.
	 * @var        int
	 */
	protected $examination_type;

	/**
	 * The value for the school_year_id field.
	 * @var        int
	 */
	protected $school_year_id;

	/**
	 * @var        SchoolYear
	 */
	protected $aSchoolYear;

	/**
	 * @var        array ExaminationRepprovedSubject[] Collection to store aggregation of ExaminationRepprovedSubject objects.
	 */
	protected $collExaminationRepprovedSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collExaminationRepprovedSubjects.
	 */
	private $lastExaminationRepprovedSubjectCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseExaminationRepproved object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->examination_number = 1;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [optionally formatted] temporal [date_from] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDateFrom($format = 'Y-m-d')
	{
		if ($this->date_from === null) {
			return null;
		}


		if ($this->date_from === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->date_from);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->date_from, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [date_to] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getDateTo($format = 'Y-m-d')
	{
		if ($this->date_to === null) {
			return null;
		}


		if ($this->date_to === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->date_to);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->date_to, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [examination_number] column value.
	 * Indica la instancia de la mesa.
	 * @return     int
	 */
	public function getExaminationNumber()
	{
		return $this->examination_number;
	}

	/**
	 * Get the [examination_type] column value.
	 * Indica el tipo de examination_repproved (Libre o previa)
	 * @return     int
	 */
	public function getExaminationType()
	{
		return $this->examination_type;
	}

	/**
	 * Get the [school_year_id] column value.
	 * año lectivo
	 * @return     int
	 */
	public function getSchoolYearId()
	{
		return $this->school_year_id;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = ExaminationRepprovedPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = ExaminationRepprovedPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Sets the value of [date_from] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setDateFrom($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->date_from !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->date_from !== null && $tmpDt = new DateTime($this->date_from)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->date_from = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = ExaminationRepprovedPeer::DATE_FROM;
			}
		} // if either are not null

		return $this;
	} // setDateFrom()

	/**
	 * Sets the value of [date_to] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setDateTo($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->date_to !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->date_to !== null && $tmpDt = new DateTime($this->date_to)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->date_to = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = ExaminationRepprovedPeer::DATE_TO;
			}
		} // if either are not null

		return $this;
	} // setDateTo()

	/**
	 * Set the value of [examination_number] column.
	 * Indica la instancia de la mesa.
	 * @param      int $v new value
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setExaminationNumber($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->examination_number !== $v || $v === 1) {
			$this->examination_number = $v;
			$this->modifiedColumns[] = ExaminationRepprovedPeer::EXAMINATION_NUMBER;
		}

		return $this;
	} // setExaminationNumber()

	/**
	 * Set the value of [examination_type] column.
	 * Indica el tipo de examination_repproved (Libre o previa)
	 * @param      int $v new value
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setExaminationType($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->examination_type !== $v) {
			$this->examination_type = $v;
			$this->modifiedColumns[] = ExaminationRepprovedPeer::EXAMINATION_TYPE;
		}

		return $this;
	} // setExaminationType()

	/**
	 * Set the value of [school_year_id] column.
	 * año lectivo
	 * @param      int $v new value
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 */
	public function setSchoolYearId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->school_year_id !== $v) {
			$this->school_year_id = $v;
			$this->modifiedColumns[] = ExaminationRepprovedPeer::SCHOOL_YEAR_ID;
		}

		if ($this->aSchoolYear !== null && $this->aSchoolYear->getId() !== $v) {
			$this->aSchoolYear = null;
		}

		return $this;
	} // setSchoolYearId()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(ExaminationRepprovedPeer::EXAMINATION_NUMBER))) {
				return false;
			}

			if ($this->examination_number !== 1) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->name = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->date_from = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->date_to = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->examination_number = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->examination_type = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->school_year_id = ($row[$startcol + 6] !== null) ? (int) $row[$startcol + 6] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 7; // 7 = ExaminationRepprovedPeer::NUM_COLUMNS - ExaminationRepprovedPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating ExaminationRepproved object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSchoolYear !== null && $this->school_year_id !== $this->aSchoolYear->getId()) {
			$this->aSchoolYear = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ExaminationRepprovedPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = ExaminationRepprovedPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSchoolYear = null;
			$this->collExaminationRepprovedSubjects = null;
			$this->lastExaminationRepprovedSubjectCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseExaminationRepproved:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ExaminationRepprovedPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			ExaminationRepprovedPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseExaminationRepproved:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseExaminationRepproved:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(ExaminationRepprovedPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseExaminationRepproved:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			ExaminationRepprovedPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSchoolYear !== null) {
				if ($this->aSchoolYear->isModified() || $this->aSchoolYear->isNew()) {
					$affectedRows += $this->aSchoolYear->save($con);
				}
				$this->setSchoolYear($this->aSchoolYear);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = ExaminationRepprovedPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = ExaminationRepprovedPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += ExaminationRepprovedPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collExaminationRepprovedSubjects !== null) {
				foreach ($this->collExaminationRepprovedSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSchoolYear !== null) {
				if (!$this->aSchoolYear->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSchoolYear->getValidationFailures());
				}
			}


			if (($retval = ExaminationRepprovedPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collExaminationRepprovedSubjects !== null) {
					foreach ($this->collExaminationRepprovedSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ExaminationRepprovedPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getName();
				break;
			case 2:
				return $this->getDateFrom();
				break;
			case 3:
				return $this->getDateTo();
				break;
			case 4:
				return $this->getExaminationNumber();
				break;
			case 5:
				return $this->getExaminationType();
				break;
			case 6:
				return $this->getSchoolYearId();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = ExaminationRepprovedPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getName(),
			$keys[2] => $this->getDateFrom(),
			$keys[3] => $this->getDateTo(),
			$keys[4] => $this->getExaminationNumber(),
			$keys[5] => $this->getExaminationType(),
			$keys[6] => $this->getSchoolYearId(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = ExaminationRepprovedPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setName($value);
				break;
			case 2:
				$this->setDateFrom($value);
				break;
			case 3:
				$this->setDateTo($value);
				break;
			case 4:
				$this->setExaminationNumber($value);
				break;
			case 5:
				$this->setExaminationType($value);
				break;
			case 6:
				$this->setSchoolYearId($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = ExaminationRepprovedPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setName($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setDateFrom($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setDateTo($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setExaminationNumber($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setExaminationType($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setSchoolYearId($arr[$keys[6]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(ExaminationRepprovedPeer::DATABASE_NAME);

		if ($this->isColumnModified(ExaminationRepprovedPeer::ID)) $criteria->add(ExaminationRepprovedPeer::ID, $this->id);
		if ($this->isColumnModified(ExaminationRepprovedPeer::NAME)) $criteria->add(ExaminationRepprovedPeer::NAME, $this->name);
		if ($this->isColumnModified(ExaminationRepprovedPeer::DATE_FROM)) $criteria->add(ExaminationRepprovedPeer::DATE_FROM, $this->date_from);
		if ($this->isColumnModified(ExaminationRepprovedPeer::DATE_TO)) $criteria->add(ExaminationRepprovedPeer::DATE_TO, $this->date_to);
		if ($this->isColumnModified(ExaminationRepprovedPeer::EXAMINATION_NUMBER)) $criteria->add(ExaminationRepprovedPeer::EXAMINATION_NUMBER, $this->examination_number);
		if ($this->isColumnModified(ExaminationRepprovedPeer::EXAMINATION_TYPE)) $criteria->add(ExaminationRepprovedPeer::EXAMINATION_TYPE, $this->examination_type);
		if ($this->isColumnModified(ExaminationRepprovedPeer::SCHOOL_YEAR_ID)) $criteria->add(ExaminationRepprovedPeer::SCHOOL_YEAR_ID, $this->school_year_id);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(ExaminationRepprovedPeer::DATABASE_NAME);

		$criteria->add(ExaminationRepprovedPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of ExaminationRepproved (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setDateFrom($this->date_from);

		$copyObj->setDateTo($this->date_to);

		$copyObj->setExaminationNumber($this->examination_number);

		$copyObj->setExaminationType($this->examination_type);

		$copyObj->setSchoolYearId($this->school_year_id);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getExaminationRepprovedSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addExaminationRepprovedSubject($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     ExaminationRepproved Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     ExaminationRepprovedPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new ExaminationRepprovedPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SchoolYear object.
	 *
	 * @param      SchoolYear $v
	 * @return     ExaminationRepproved The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSchoolYear(SchoolYear $v = null)
	{
		if ($v === null) {
			$this->setSchoolYearId(NULL);
		} else {
			$this->setSchoolYearId($v->getId());
		}

		$this->aSchoolYear = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SchoolYear object, it will not be re-added.
		if ($v !== null) {
			$v->addExaminationRepproved($this);
		}

		return $this;
	}


	/**
	 * Get the associated SchoolYear object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SchoolYear The associated SchoolYear object.
	 * @throws     PropelException
	 */
	public function getSchoolYear(PropelPDO $con = null)
	{
		if ($this->aSchoolYear === null && ($this->school_year_id !== null)) {
			$c = new Criteria(SchoolYearPeer::DATABASE_NAME);
			$c->add(SchoolYearPeer::ID, $this->school_year_id);
			$this->aSchoolYear = SchoolYearPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSchoolYear->addExaminationRepproveds($this);
			 */
		}
		return $this->aSchoolYear;
	}

	/**
	 * Clears out the collExaminationRepprovedSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addExaminationRepprovedSubjects()
	 */
	public function clearExaminationRepprovedSubjects()
	{
		$this->collExaminationRepprovedSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collExaminationRepprovedSubjects collection (array).
	 *
	 * By default this just sets the collExaminationRepprovedSubjects collection to an empty array (like clearcollExaminationRepprovedSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initExaminationRepprovedSubjects()
	{
		$this->collExaminationRepprovedSubjects = array();
	}

	/**
	 * Gets an array of ExaminationRepprovedSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this ExaminationRepproved has previously been saved, it will retrieve
	 * related ExaminationRepprovedSubjects from storage. If this ExaminationRepproved is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array ExaminationRepprovedSubject[]
	 * @throws     PropelException
	 */
	public function getExaminationRepprovedSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ExaminationRepprovedPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collExaminationRepprovedSubjects === null) {
			if ($this->isNew()) {
			   $this->collExaminationRepprovedSubjects = array();
			} else {

				$criteria->add(ExaminationRepprovedSubjectPeer::EXAMINATION_REPPROVED_ID, $this->id);

				ExaminationRepprovedSubjectPeer::addSelectColumns($criteria);
				$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(ExaminationRepprovedSubjectPeer::EXAMINATION_REPPROVED_ID, $this->id);

				ExaminationRepprovedSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastExaminationRepprovedSubjectCriteria) || !$this->lastExaminationRepprovedSubjectCriteria->equals($criteria)) {
					$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastExaminationRepprovedSubjectCriteria = $criteria;
		return $this->collExaminationRepprovedSubjects;
	}

	/**
	 * Returns the number of related ExaminationRepprovedSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related ExaminationRepprovedSubject objects.
	 * @throws     PropelException
	 */
	public function countExaminationRepprovedSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ExaminationRepprovedPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collExaminationRepprovedSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(ExaminationRepprovedSubjectPeer::EXAMINATION_REPPROVED_ID, $this->id);

				$count = ExaminationRepprovedSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(ExaminationRepprovedSubjectPeer::EXAMINATION_REPPROVED_ID, $this->id);

				if (!isset($this->lastExaminationRepprovedSubjectCriteria) || !$this->lastExaminationRepprovedSubjectCriteria->equals($criteria)) {
					$count = ExaminationRepprovedSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collExaminationRepprovedSubjects);
				}
			} else {
				$count = count($this->collExaminationRepprovedSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a ExaminationRepprovedSubject object to this object
	 * through the ExaminationRepprovedSubject foreign key attribute.
	 *
	 * @param      ExaminationRepprovedSubject $l ExaminationRepprovedSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addExaminationRepprovedSubject(ExaminationRepprovedSubject $l)
	{
		if ($this->collExaminationRepprovedSubjects === null) {
			$this->initExaminationRepprovedSubjects();
		}
		if (!in_array($l, $this->collExaminationRepprovedSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collExaminationRepprovedSubjects, $l);
			$l->setExaminationRepproved($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this ExaminationRepproved is new, it will return
	 * an empty collection; or if this ExaminationRepproved has previously
	 * been saved, it will retrieve related ExaminationRepprovedSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in ExaminationRepproved.
	 */
	public function getExaminationRepprovedSubjectsJoinCareerSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(ExaminationRepprovedPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collExaminationRepprovedSubjects === null) {
			if ($this->isNew()) {
				$this->collExaminationRepprovedSubjects = array();
			} else {

				$criteria->add(ExaminationRepprovedSubjectPeer::EXAMINATION_REPPROVED_ID, $this->id);

				$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelectJoinCareerSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(ExaminationRepprovedSubjectPeer::EXAMINATION_REPPROVED_ID, $this->id);

			if (!isset($this->lastExaminationRepprovedSubjectCriteria) || !$this->lastExaminationRepprovedSubjectCriteria->equals($criteria)) {
				$this->collExaminationRepprovedSubjects = ExaminationRepprovedSubjectPeer::doSelectJoinCareerSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastExaminationRepprovedSubjectCriteria = $criteria;

		return $this->collExaminationRepprovedSubjects;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collExaminationRepprovedSubjects) {
				foreach ((array) $this->collExaminationRepprovedSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collExaminationRepprovedSubjects = null;
			$this->aSchoolYear = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseExaminationRepproved:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseExaminationRepproved::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseExaminationRepproved
