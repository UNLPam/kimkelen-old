<?php

/**
 * Base class that represents a row from the 'career_school_year' table.
 *
 * Representa la configuracion de un plan de estudios en un año lectivo
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseCareerSchoolYear extends BaseObject  implements Persistent {


  const PEER = 'CareerSchoolYearPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CareerSchoolYearPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the school_year_id field.
	 * @var        int
	 */
	protected $school_year_id;

	/**
	 * The value for the career_id field.
	 * @var        int
	 */
	protected $career_id;

	/**
	 * The value for the subject_configuration_id field.
	 * @var        int
	 */
	protected $subject_configuration_id;

	/**
	 * The value for the is_processed field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_processed;

	/**
	 * @var        SchoolYear
	 */
	protected $aSchoolYear;

	/**
	 * @var        Career
	 */
	protected $aCareer;

	/**
	 * @var        SubjectConfiguration
	 */
	protected $aSubjectConfiguration;

	/**
	 * @var        array StudentAttendance[] Collection to store aggregation of StudentAttendance objects.
	 */
	protected $collStudentAttendances;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentAttendances.
	 */
	private $lastStudentAttendanceCriteria = null;

	/**
	 * @var        array StudentFree[] Collection to store aggregation of StudentFree objects.
	 */
	protected $collStudentFrees;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentFrees.
	 */
	private $lastStudentFreeCriteria = null;

	/**
	 * @var        array StudentCareerSchoolYear[] Collection to store aggregation of StudentCareerSchoolYear objects.
	 */
	protected $collStudentCareerSchoolYears;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentCareerSchoolYears.
	 */
	private $lastStudentCareerSchoolYearCriteria = null;

	/**
	 * @var        array CareerSubjectSchoolYear[] Collection to store aggregation of CareerSubjectSchoolYear objects.
	 */
	protected $collCareerSubjectSchoolYears;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerSubjectSchoolYears.
	 */
	private $lastCareerSubjectSchoolYearCriteria = null;

	/**
	 * @var        array Division[] Collection to store aggregation of Division objects.
	 */
	protected $collDivisions;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDivisions.
	 */
	private $lastDivisionCriteria = null;

	/**
	 * @var        array CareerSchoolYearPeriod[] Collection to store aggregation of CareerSchoolYearPeriod objects.
	 */
	protected $collCareerSchoolYearPeriods;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerSchoolYearPeriods.
	 */
	private $lastCareerSchoolYearPeriodCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseCareerSchoolYear object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_processed = false;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [school_year_id] column value.
	 * 
	 * @return     int
	 */
	public function getSchoolYearId()
	{
		return $this->school_year_id;
	}

	/**
	 * Get the [career_id] column value.
	 * 
	 * @return     int
	 */
	public function getCareerId()
	{
		return $this->career_id;
	}

	/**
	 * Get the [subject_configuration_id] column value.
	 * Referencia a la configuración global de las materias pertenecientes a esta carrera.
	 * @return     int
	 */
	public function getSubjectConfigurationId()
	{
		return $this->subject_configuration_id;
	}

	/**
	 * Get the [is_processed] column value.
	 * Indica si la carrera en este año lectivo ya fue procesado.
	 * @return     boolean
	 */
	public function getIsProcessed()
	{
		return $this->is_processed;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [school_year_id] column.
	 * 
	 * @param      int $v new value
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 */
	public function setSchoolYearId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->school_year_id !== $v) {
			$this->school_year_id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeer::SCHOOL_YEAR_ID;
		}

		if ($this->aSchoolYear !== null && $this->aSchoolYear->getId() !== $v) {
			$this->aSchoolYear = null;
		}

		return $this;
	} // setSchoolYearId()

	/**
	 * Set the value of [career_id] column.
	 * 
	 * @param      int $v new value
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 */
	public function setCareerId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->career_id !== $v) {
			$this->career_id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeer::CAREER_ID;
		}

		if ($this->aCareer !== null && $this->aCareer->getId() !== $v) {
			$this->aCareer = null;
		}

		return $this;
	} // setCareerId()

	/**
	 * Set the value of [subject_configuration_id] column.
	 * Referencia a la configuración global de las materias pertenecientes a esta carrera.
	 * @param      int $v new value
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 */
	public function setSubjectConfigurationId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->subject_configuration_id !== $v) {
			$this->subject_configuration_id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeer::SUBJECT_CONFIGURATION_ID;
		}

		if ($this->aSubjectConfiguration !== null && $this->aSubjectConfiguration->getId() !== $v) {
			$this->aSubjectConfiguration = null;
		}

		return $this;
	} // setSubjectConfigurationId()

	/**
	 * Set the value of [is_processed] column.
	 * Indica si la carrera en este año lectivo ya fue procesado.
	 * @param      boolean $v new value
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 */
	public function setIsProcessed($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_processed !== $v || $v === false) {
			$this->is_processed = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeer::IS_PROCESSED;
		}

		return $this;
	} // setIsProcessed()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(CareerSchoolYearPeer::IS_PROCESSED))) {
				return false;
			}

			if ($this->is_processed !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->school_year_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->career_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->subject_configuration_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->is_processed = ($row[$startcol + 4] !== null) ? (boolean) $row[$startcol + 4] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 5; // 5 = CareerSchoolYearPeer::NUM_COLUMNS - CareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating CareerSchoolYear object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aSchoolYear !== null && $this->school_year_id !== $this->aSchoolYear->getId()) {
			$this->aSchoolYear = null;
		}
		if ($this->aCareer !== null && $this->career_id !== $this->aCareer->getId()) {
			$this->aCareer = null;
		}
		if ($this->aSubjectConfiguration !== null && $this->subject_configuration_id !== $this->aSubjectConfiguration->getId()) {
			$this->aSubjectConfiguration = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSchoolYearPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CareerSchoolYearPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aSchoolYear = null;
			$this->aCareer = null;
			$this->aSubjectConfiguration = null;
			$this->collStudentAttendances = null;
			$this->lastStudentAttendanceCriteria = null;

			$this->collStudentFrees = null;
			$this->lastStudentFreeCriteria = null;

			$this->collStudentCareerSchoolYears = null;
			$this->lastStudentCareerSchoolYearCriteria = null;

			$this->collCareerSubjectSchoolYears = null;
			$this->lastCareerSubjectSchoolYearCriteria = null;

			$this->collDivisions = null;
			$this->lastDivisionCriteria = null;

			$this->collCareerSchoolYearPeriods = null;
			$this->lastCareerSchoolYearPeriodCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareerSchoolYear:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSchoolYearPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			CareerSchoolYearPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseCareerSchoolYear:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareerSchoolYear:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSchoolYearPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseCareerSchoolYear:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			CareerSchoolYearPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSchoolYear !== null) {
				if ($this->aSchoolYear->isModified() || $this->aSchoolYear->isNew()) {
					$affectedRows += $this->aSchoolYear->save($con);
				}
				$this->setSchoolYear($this->aSchoolYear);
			}

			if ($this->aCareer !== null) {
				if ($this->aCareer->isModified() || $this->aCareer->isNew()) {
					$affectedRows += $this->aCareer->save($con);
				}
				$this->setCareer($this->aCareer);
			}

			if ($this->aSubjectConfiguration !== null) {
				if ($this->aSubjectConfiguration->isModified() || $this->aSubjectConfiguration->isNew()) {
					$affectedRows += $this->aSubjectConfiguration->save($con);
				}
				$this->setSubjectConfiguration($this->aSubjectConfiguration);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CareerSchoolYearPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CareerSchoolYearPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CareerSchoolYearPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collStudentAttendances !== null) {
				foreach ($this->collStudentAttendances as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentFrees !== null) {
				foreach ($this->collStudentFrees as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentCareerSchoolYears !== null) {
				foreach ($this->collStudentCareerSchoolYears as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCareerSubjectSchoolYears !== null) {
				foreach ($this->collCareerSubjectSchoolYears as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDivisions !== null) {
				foreach ($this->collDivisions as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCareerSchoolYearPeriods !== null) {
				foreach ($this->collCareerSchoolYearPeriods as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aSchoolYear !== null) {
				if (!$this->aSchoolYear->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSchoolYear->getValidationFailures());
				}
			}

			if ($this->aCareer !== null) {
				if (!$this->aCareer->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCareer->getValidationFailures());
				}
			}

			if ($this->aSubjectConfiguration !== null) {
				if (!$this->aSubjectConfiguration->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSubjectConfiguration->getValidationFailures());
				}
			}


			if (($retval = CareerSchoolYearPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collStudentAttendances !== null) {
					foreach ($this->collStudentAttendances as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentFrees !== null) {
					foreach ($this->collStudentFrees as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentCareerSchoolYears !== null) {
					foreach ($this->collStudentCareerSchoolYears as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCareerSubjectSchoolYears !== null) {
					foreach ($this->collCareerSubjectSchoolYears as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDivisions !== null) {
					foreach ($this->collDivisions as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCareerSchoolYearPeriods !== null) {
					foreach ($this->collCareerSchoolYearPeriods as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerSchoolYearPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getSchoolYearId();
				break;
			case 2:
				return $this->getCareerId();
				break;
			case 3:
				return $this->getSubjectConfigurationId();
				break;
			case 4:
				return $this->getIsProcessed();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CareerSchoolYearPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getSchoolYearId(),
			$keys[2] => $this->getCareerId(),
			$keys[3] => $this->getSubjectConfigurationId(),
			$keys[4] => $this->getIsProcessed(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerSchoolYearPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setSchoolYearId($value);
				break;
			case 2:
				$this->setCareerId($value);
				break;
			case 3:
				$this->setSubjectConfigurationId($value);
				break;
			case 4:
				$this->setIsProcessed($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CareerSchoolYearPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setSchoolYearId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCareerId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setSubjectConfigurationId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setIsProcessed($arr[$keys[4]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);

		if ($this->isColumnModified(CareerSchoolYearPeer::ID)) $criteria->add(CareerSchoolYearPeer::ID, $this->id);
		if ($this->isColumnModified(CareerSchoolYearPeer::SCHOOL_YEAR_ID)) $criteria->add(CareerSchoolYearPeer::SCHOOL_YEAR_ID, $this->school_year_id);
		if ($this->isColumnModified(CareerSchoolYearPeer::CAREER_ID)) $criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->career_id);
		if ($this->isColumnModified(CareerSchoolYearPeer::SUBJECT_CONFIGURATION_ID)) $criteria->add(CareerSchoolYearPeer::SUBJECT_CONFIGURATION_ID, $this->subject_configuration_id);
		if ($this->isColumnModified(CareerSchoolYearPeer::IS_PROCESSED)) $criteria->add(CareerSchoolYearPeer::IS_PROCESSED, $this->is_processed);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);

		$criteria->add(CareerSchoolYearPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of CareerSchoolYear (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setSchoolYearId($this->school_year_id);

		$copyObj->setCareerId($this->career_id);

		$copyObj->setSubjectConfigurationId($this->subject_configuration_id);

		$copyObj->setIsProcessed($this->is_processed);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getStudentAttendances() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentAttendance($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentFrees() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentFree($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentCareerSchoolYears() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentCareerSchoolYear($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCareerSubjectSchoolYears() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerSubjectSchoolYear($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDivisions() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDivision($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCareerSchoolYearPeriods() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerSchoolYearPeriod($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     CareerSchoolYear Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CareerSchoolYearPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CareerSchoolYearPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a SchoolYear object.
	 *
	 * @param      SchoolYear $v
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSchoolYear(SchoolYear $v = null)
	{
		if ($v === null) {
			$this->setSchoolYearId(NULL);
		} else {
			$this->setSchoolYearId($v->getId());
		}

		$this->aSchoolYear = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SchoolYear object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSchoolYear($this);
		}

		return $this;
	}


	/**
	 * Get the associated SchoolYear object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SchoolYear The associated SchoolYear object.
	 * @throws     PropelException
	 */
	public function getSchoolYear(PropelPDO $con = null)
	{
		if ($this->aSchoolYear === null && ($this->school_year_id !== null)) {
			$c = new Criteria(SchoolYearPeer::DATABASE_NAME);
			$c->add(SchoolYearPeer::ID, $this->school_year_id);
			$this->aSchoolYear = SchoolYearPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSchoolYear->addCareerSchoolYears($this);
			 */
		}
		return $this->aSchoolYear;
	}

	/**
	 * Declares an association between this object and a Career object.
	 *
	 * @param      Career $v
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCareer(Career $v = null)
	{
		if ($v === null) {
			$this->setCareerId(NULL);
		} else {
			$this->setCareerId($v->getId());
		}

		$this->aCareer = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Career object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSchoolYear($this);
		}

		return $this;
	}


	/**
	 * Get the associated Career object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Career The associated Career object.
	 * @throws     PropelException
	 */
	public function getCareer(PropelPDO $con = null)
	{
		if ($this->aCareer === null && ($this->career_id !== null)) {
			$c = new Criteria(CareerPeer::DATABASE_NAME);
			$c->add(CareerPeer::ID, $this->career_id);
			$this->aCareer = CareerPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCareer->addCareerSchoolYears($this);
			 */
		}
		return $this->aCareer;
	}

	/**
	 * Declares an association between this object and a SubjectConfiguration object.
	 *
	 * @param      SubjectConfiguration $v
	 * @return     CareerSchoolYear The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSubjectConfiguration(SubjectConfiguration $v = null)
	{
		if ($v === null) {
			$this->setSubjectConfigurationId(NULL);
		} else {
			$this->setSubjectConfigurationId($v->getId());
		}

		$this->aSubjectConfiguration = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SubjectConfiguration object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSchoolYear($this);
		}

		return $this;
	}


	/**
	 * Get the associated SubjectConfiguration object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SubjectConfiguration The associated SubjectConfiguration object.
	 * @throws     PropelException
	 */
	public function getSubjectConfiguration(PropelPDO $con = null)
	{
		if ($this->aSubjectConfiguration === null && ($this->subject_configuration_id !== null)) {
			$c = new Criteria(SubjectConfigurationPeer::DATABASE_NAME);
			$c->add(SubjectConfigurationPeer::ID, $this->subject_configuration_id);
			$this->aSubjectConfiguration = SubjectConfigurationPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSubjectConfiguration->addCareerSchoolYears($this);
			 */
		}
		return $this->aSubjectConfiguration;
	}

	/**
	 * Clears out the collStudentAttendances collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentAttendances()
	 */
	public function clearStudentAttendances()
	{
		$this->collStudentAttendances = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentAttendances collection (array).
	 *
	 * By default this just sets the collStudentAttendances collection to an empty array (like clearcollStudentAttendances());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentAttendances()
	{
		$this->collStudentAttendances = array();
	}

	/**
	 * Gets an array of StudentAttendance objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear has previously been saved, it will retrieve
	 * related StudentAttendances from storage. If this CareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentAttendance[]
	 * @throws     PropelException
	 */
	public function getStudentAttendances($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentAttendances === null) {
			if ($this->isNew()) {
			   $this->collStudentAttendances = array();
			} else {

				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentAttendancePeer::addSelectColumns($criteria);
				$this->collStudentAttendances = StudentAttendancePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentAttendancePeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentAttendanceCriteria) || !$this->lastStudentAttendanceCriteria->equals($criteria)) {
					$this->collStudentAttendances = StudentAttendancePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentAttendanceCriteria = $criteria;
		return $this->collStudentAttendances;
	}

	/**
	 * Returns the number of related StudentAttendance objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentAttendance objects.
	 * @throws     PropelException
	 */
	public function countStudentAttendances(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentAttendances === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = StudentAttendancePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastStudentAttendanceCriteria) || !$this->lastStudentAttendanceCriteria->equals($criteria)) {
					$count = StudentAttendancePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentAttendances);
				}
			} else {
				$count = count($this->collStudentAttendances);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentAttendance object to this object
	 * through the StudentAttendance foreign key attribute.
	 *
	 * @param      StudentAttendance $l StudentAttendance
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentAttendance(StudentAttendance $l)
	{
		if ($this->collStudentAttendances === null) {
			$this->initStudentAttendances();
		}
		if (!in_array($l, $this->collStudentAttendances, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentAttendances, $l);
			$l->setCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentAttendances from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentAttendancesJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentAttendances === null) {
			if ($this->isNew()) {
				$this->collStudentAttendances = array();
			} else {

				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentAttendanceCriteria) || !$this->lastStudentAttendanceCriteria->equals($criteria)) {
				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentAttendanceCriteria = $criteria;

		return $this->collStudentAttendances;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentAttendances from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentAttendancesJoinAbsenceType($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentAttendances === null) {
			if ($this->isNew()) {
				$this->collStudentAttendances = array();
			} else {

				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinAbsenceType($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentAttendanceCriteria) || !$this->lastStudentAttendanceCriteria->equals($criteria)) {
				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinAbsenceType($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentAttendanceCriteria = $criteria;

		return $this->collStudentAttendances;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentAttendances from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentAttendancesJoinCourseSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentAttendances === null) {
			if ($this->isNew()) {
				$this->collStudentAttendances = array();
			} else {

				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentAttendanceCriteria) || !$this->lastStudentAttendanceCriteria->equals($criteria)) {
				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentAttendanceCriteria = $criteria;

		return $this->collStudentAttendances;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentAttendances from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentAttendancesJoinStudentAttendanceJustification($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentAttendances === null) {
			if ($this->isNew()) {
				$this->collStudentAttendances = array();
			} else {

				$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinStudentAttendanceJustification($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentAttendanceCriteria) || !$this->lastStudentAttendanceCriteria->equals($criteria)) {
				$this->collStudentAttendances = StudentAttendancePeer::doSelectJoinStudentAttendanceJustification($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentAttendanceCriteria = $criteria;

		return $this->collStudentAttendances;
	}

	/**
	 * Clears out the collStudentFrees collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentFrees()
	 */
	public function clearStudentFrees()
	{
		$this->collStudentFrees = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentFrees collection (array).
	 *
	 * By default this just sets the collStudentFrees collection to an empty array (like clearcollStudentFrees());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentFrees()
	{
		$this->collStudentFrees = array();
	}

	/**
	 * Gets an array of StudentFree objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear has previously been saved, it will retrieve
	 * related StudentFrees from storage. If this CareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentFree[]
	 * @throws     PropelException
	 */
	public function getStudentFrees($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
			   $this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentFreePeer::addSelectColumns($criteria);
				$this->collStudentFrees = StudentFreePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentFreePeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
					$this->collStudentFrees = StudentFreePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentFreeCriteria = $criteria;
		return $this->collStudentFrees;
	}

	/**
	 * Returns the number of related StudentFree objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentFree objects.
	 * @throws     PropelException
	 */
	public function countStudentFrees(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = StudentFreePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
					$count = StudentFreePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentFrees);
				}
			} else {
				$count = count($this->collStudentFrees);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentFree object to this object
	 * through the StudentFree foreign key attribute.
	 *
	 * @param      StudentFree $l StudentFree
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentFree(StudentFree $l)
	{
		if ($this->collStudentFrees === null) {
			$this->initStudentFrees();
		}
		if (!in_array($l, $this->collStudentFrees, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentFrees, $l);
			$l->setCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentFrees from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentFreesJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentFrees = StudentFreePeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
				$this->collStudentFrees = StudentFreePeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentFreeCriteria = $criteria;

		return $this->collStudentFrees;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentFrees from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentFreesJoinCareerSchoolYearPeriod($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentFrees = StudentFreePeer::doSelectJoinCareerSchoolYearPeriod($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
				$this->collStudentFrees = StudentFreePeer::doSelectJoinCareerSchoolYearPeriod($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentFreeCriteria = $criteria;

		return $this->collStudentFrees;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentFrees from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentFreesJoinCourseSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentFrees = StudentFreePeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
				$this->collStudentFrees = StudentFreePeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentFreeCriteria = $criteria;

		return $this->collStudentFrees;
	}

	/**
	 * Clears out the collStudentCareerSchoolYears collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentCareerSchoolYears()
	 */
	public function clearStudentCareerSchoolYears()
	{
		$this->collStudentCareerSchoolYears = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentCareerSchoolYears collection (array).
	 *
	 * By default this just sets the collStudentCareerSchoolYears collection to an empty array (like clearcollStudentCareerSchoolYears());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentCareerSchoolYears()
	{
		$this->collStudentCareerSchoolYears = array();
	}

	/**
	 * Gets an array of StudentCareerSchoolYear objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear has previously been saved, it will retrieve
	 * related StudentCareerSchoolYears from storage. If this CareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentCareerSchoolYear[]
	 * @throws     PropelException
	 */
	public function getStudentCareerSchoolYears($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYears === null) {
			if ($this->isNew()) {
			   $this->collStudentCareerSchoolYears = array();
			} else {

				$criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentCareerSchoolYearPeer::addSelectColumns($criteria);
				$this->collStudentCareerSchoolYears = StudentCareerSchoolYearPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentCareerSchoolYearPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentCareerSchoolYearCriteria) || !$this->lastStudentCareerSchoolYearCriteria->equals($criteria)) {
					$this->collStudentCareerSchoolYears = StudentCareerSchoolYearPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentCareerSchoolYearCriteria = $criteria;
		return $this->collStudentCareerSchoolYears;
	}

	/**
	 * Returns the number of related StudentCareerSchoolYear objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentCareerSchoolYear objects.
	 * @throws     PropelException
	 */
	public function countStudentCareerSchoolYears(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentCareerSchoolYears === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = StudentCareerSchoolYearPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastStudentCareerSchoolYearCriteria) || !$this->lastStudentCareerSchoolYearCriteria->equals($criteria)) {
					$count = StudentCareerSchoolYearPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentCareerSchoolYears);
				}
			} else {
				$count = count($this->collStudentCareerSchoolYears);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentCareerSchoolYear object to this object
	 * through the StudentCareerSchoolYear foreign key attribute.
	 *
	 * @param      StudentCareerSchoolYear $l StudentCareerSchoolYear
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentCareerSchoolYear(StudentCareerSchoolYear $l)
	{
		if ($this->collStudentCareerSchoolYears === null) {
			$this->initStudentCareerSchoolYears();
		}
		if (!in_array($l, $this->collStudentCareerSchoolYears, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentCareerSchoolYears, $l);
			$l->setCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related StudentCareerSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getStudentCareerSchoolYearsJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYears === null) {
			if ($this->isNew()) {
				$this->collStudentCareerSchoolYears = array();
			} else {

				$criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentCareerSchoolYears = StudentCareerSchoolYearPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentCareerSchoolYearCriteria) || !$this->lastStudentCareerSchoolYearCriteria->equals($criteria)) {
				$this->collStudentCareerSchoolYears = StudentCareerSchoolYearPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentCareerSchoolYearCriteria = $criteria;

		return $this->collStudentCareerSchoolYears;
	}

	/**
	 * Clears out the collCareerSubjectSchoolYears collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerSubjectSchoolYears()
	 */
	public function clearCareerSubjectSchoolYears()
	{
		$this->collCareerSubjectSchoolYears = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerSubjectSchoolYears collection (array).
	 *
	 * By default this just sets the collCareerSubjectSchoolYears collection to an empty array (like clearcollCareerSubjectSchoolYears());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerSubjectSchoolYears()
	{
		$this->collCareerSubjectSchoolYears = array();
	}

	/**
	 * Gets an array of CareerSubjectSchoolYear objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear has previously been saved, it will retrieve
	 * related CareerSubjectSchoolYears from storage. If this CareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerSubjectSchoolYear[]
	 * @throws     PropelException
	 */
	public function getCareerSubjectSchoolYears($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
			   $this->collCareerSubjectSchoolYears = array();
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				CareerSubjectSchoolYearPeer::addSelectColumns($criteria);
				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				CareerSubjectSchoolYearPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
					$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerSubjectSchoolYearCriteria = $criteria;
		return $this->collCareerSubjectSchoolYears;
	}

	/**
	 * Returns the number of related CareerSubjectSchoolYear objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerSubjectSchoolYear objects.
	 * @throws     PropelException
	 */
	public function countCareerSubjectSchoolYears(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = CareerSubjectSchoolYearPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
					$count = CareerSubjectSchoolYearPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerSubjectSchoolYears);
				}
			} else {
				$count = count($this->collCareerSubjectSchoolYears);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerSubjectSchoolYear object to this object
	 * through the CareerSubjectSchoolYear foreign key attribute.
	 *
	 * @param      CareerSubjectSchoolYear $l CareerSubjectSchoolYear
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerSubjectSchoolYear(CareerSubjectSchoolYear $l)
	{
		if ($this->collCareerSubjectSchoolYears === null) {
			$this->initCareerSubjectSchoolYears();
		}
		if (!in_array($l, $this->collCareerSubjectSchoolYears, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerSubjectSchoolYears, $l);
			$l->setCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related CareerSubjectSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getCareerSubjectSchoolYearsJoinCareerSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
				$this->collCareerSubjectSchoolYears = array();
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinCareerSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinCareerSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectSchoolYearCriteria = $criteria;

		return $this->collCareerSubjectSchoolYears;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related CareerSubjectSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getCareerSubjectSchoolYearsJoinSubjectConfiguration($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjectSchoolYears === null) {
			if ($this->isNew()) {
				$this->collCareerSubjectSchoolYears = array();
			} else {

				$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinSubjectConfiguration($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastCareerSubjectSchoolYearCriteria) || !$this->lastCareerSubjectSchoolYearCriteria->equals($criteria)) {
				$this->collCareerSubjectSchoolYears = CareerSubjectSchoolYearPeer::doSelectJoinSubjectConfiguration($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectSchoolYearCriteria = $criteria;

		return $this->collCareerSubjectSchoolYears;
	}

	/**
	 * Clears out the collDivisions collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDivisions()
	 */
	public function clearDivisions()
	{
		$this->collDivisions = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDivisions collection (array).
	 *
	 * By default this just sets the collDivisions collection to an empty array (like clearcollDivisions());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDivisions()
	{
		$this->collDivisions = array();
	}

	/**
	 * Gets an array of Division objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear has previously been saved, it will retrieve
	 * related Divisions from storage. If this CareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Division[]
	 * @throws     PropelException
	 */
	public function getDivisions($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDivisions === null) {
			if ($this->isNew()) {
			   $this->collDivisions = array();
			} else {

				$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				DivisionPeer::addSelectColumns($criteria);
				$this->collDivisions = DivisionPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				DivisionPeer::addSelectColumns($criteria);
				if (!isset($this->lastDivisionCriteria) || !$this->lastDivisionCriteria->equals($criteria)) {
					$this->collDivisions = DivisionPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDivisionCriteria = $criteria;
		return $this->collDivisions;
	}

	/**
	 * Returns the number of related Division objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Division objects.
	 * @throws     PropelException
	 */
	public function countDivisions(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDivisions === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = DivisionPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastDivisionCriteria) || !$this->lastDivisionCriteria->equals($criteria)) {
					$count = DivisionPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collDivisions);
				}
			} else {
				$count = count($this->collDivisions);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Division object to this object
	 * through the Division foreign key attribute.
	 *
	 * @param      Division $l Division
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDivision(Division $l)
	{
		if ($this->collDivisions === null) {
			$this->initDivisions();
		}
		if (!in_array($l, $this->collDivisions, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDivisions, $l);
			$l->setCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related Divisions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getDivisionsJoinDivisionTitle($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDivisions === null) {
			if ($this->isNew()) {
				$this->collDivisions = array();
			} else {

				$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collDivisions = DivisionPeer::doSelectJoinDivisionTitle($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastDivisionCriteria) || !$this->lastDivisionCriteria->equals($criteria)) {
				$this->collDivisions = DivisionPeer::doSelectJoinDivisionTitle($criteria, $con, $join_behavior);
			}
		}
		$this->lastDivisionCriteria = $criteria;

		return $this->collDivisions;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related Divisions from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getDivisionsJoinShift($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDivisions === null) {
			if ($this->isNew()) {
				$this->collDivisions = array();
			} else {

				$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collDivisions = DivisionPeer::doSelectJoinShift($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DivisionPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastDivisionCriteria) || !$this->lastDivisionCriteria->equals($criteria)) {
				$this->collDivisions = DivisionPeer::doSelectJoinShift($criteria, $con, $join_behavior);
			}
		}
		$this->lastDivisionCriteria = $criteria;

		return $this->collDivisions;
	}

	/**
	 * Clears out the collCareerSchoolYearPeriods collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerSchoolYearPeriods()
	 */
	public function clearCareerSchoolYearPeriods()
	{
		$this->collCareerSchoolYearPeriods = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerSchoolYearPeriods collection (array).
	 *
	 * By default this just sets the collCareerSchoolYearPeriods collection to an empty array (like clearcollCareerSchoolYearPeriods());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerSchoolYearPeriods()
	{
		$this->collCareerSchoolYearPeriods = array();
	}

	/**
	 * Gets an array of CareerSchoolYearPeriod objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear has previously been saved, it will retrieve
	 * related CareerSchoolYearPeriods from storage. If this CareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerSchoolYearPeriod[]
	 * @throws     PropelException
	 */
	public function getCareerSchoolYearPeriods($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYearPeriods === null) {
			if ($this->isNew()) {
			   $this->collCareerSchoolYearPeriods = array();
			} else {

				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				CareerSchoolYearPeriodPeer::addSelectColumns($criteria);
				$this->collCareerSchoolYearPeriods = CareerSchoolYearPeriodPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				CareerSchoolYearPeriodPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerSchoolYearPeriodCriteria) || !$this->lastCareerSchoolYearPeriodCriteria->equals($criteria)) {
					$this->collCareerSchoolYearPeriods = CareerSchoolYearPeriodPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerSchoolYearPeriodCriteria = $criteria;
		return $this->collCareerSchoolYearPeriods;
	}

	/**
	 * Returns the number of related CareerSchoolYearPeriod objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerSchoolYearPeriod objects.
	 * @throws     PropelException
	 */
	public function countCareerSchoolYearPeriods(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerSchoolYearPeriods === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = CareerSchoolYearPeriodPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastCareerSchoolYearPeriodCriteria) || !$this->lastCareerSchoolYearPeriodCriteria->equals($criteria)) {
					$count = CareerSchoolYearPeriodPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerSchoolYearPeriods);
				}
			} else {
				$count = count($this->collCareerSchoolYearPeriods);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerSchoolYearPeriod object to this object
	 * through the CareerSchoolYearPeriod foreign key attribute.
	 *
	 * @param      CareerSchoolYearPeriod $l CareerSchoolYearPeriod
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerSchoolYearPeriod(CareerSchoolYearPeriod $l)
	{
		if ($this->collCareerSchoolYearPeriods === null) {
			$this->initCareerSchoolYearPeriods();
		}
		if (!in_array($l, $this->collCareerSchoolYearPeriods, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerSchoolYearPeriods, $l);
			$l->setCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYear is new, it will return
	 * an empty collection; or if this CareerSchoolYear has previously
	 * been saved, it will retrieve related CareerSchoolYearPeriods from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYear.
	 */
	public function getCareerSchoolYearPeriodsJoinCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYearPeriods === null) {
			if ($this->isNew()) {
				$this->collCareerSchoolYearPeriods = array();
			} else {

				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collCareerSchoolYearPeriods = CareerSchoolYearPeriodPeer::doSelectJoinCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastCareerSchoolYearPeriodCriteria) || !$this->lastCareerSchoolYearPeriodCriteria->equals($criteria)) {
				$this->collCareerSchoolYearPeriods = CareerSchoolYearPeriodPeer::doSelectJoinCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSchoolYearPeriodCriteria = $criteria;

		return $this->collCareerSchoolYearPeriods;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collStudentAttendances) {
				foreach ((array) $this->collStudentAttendances as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentFrees) {
				foreach ((array) $this->collStudentFrees as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentCareerSchoolYears) {
				foreach ((array) $this->collStudentCareerSchoolYears as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCareerSubjectSchoolYears) {
				foreach ((array) $this->collCareerSubjectSchoolYears as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDivisions) {
				foreach ((array) $this->collDivisions as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCareerSchoolYearPeriods) {
				foreach ((array) $this->collCareerSchoolYearPeriods as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collStudentAttendances = null;
		$this->collStudentFrees = null;
		$this->collStudentCareerSchoolYears = null;
		$this->collCareerSubjectSchoolYears = null;
		$this->collDivisions = null;
		$this->collCareerSchoolYearPeriods = null;
			$this->aSchoolYear = null;
			$this->aCareer = null;
			$this->aSubjectConfiguration = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseCareerSchoolYear:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseCareerSchoolYear::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseCareerSchoolYear
