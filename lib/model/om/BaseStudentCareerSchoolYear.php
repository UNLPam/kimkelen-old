<?php

/**
 * Base class that represents a row from the 'student_career_school_year' table.
 *
 * Cada tupla representa el estado de un inscripto en una carrera en un año lectivo
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseStudentCareerSchoolYear extends BaseObject  implements Persistent {


  const PEER = 'StudentCareerSchoolYearPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        StudentCareerSchoolYearPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the career_school_year_id field.
	 * @var        int
	 */
	protected $career_school_year_id;

	/**
	 * The value for the student_id field.
	 * @var        int
	 */
	protected $student_id;

	/**
	 * The value for the year field.
	 * @var        int
	 */
	protected $year;

	/**
	 * The value for the status field.
	 * Note: this column has a database default value of: 0
	 * @var        int
	 */
	protected $status;

	/**
	 * The value for the is_processed field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_processed;

	/**
	 * @var        CareerSchoolYear
	 */
	protected $aCareerSchoolYear;

	/**
	 * @var        Student
	 */
	protected $aStudent;

	/**
	 * @var        array TentativeRepprovedStudent[] Collection to store aggregation of TentativeRepprovedStudent objects.
	 */
	protected $collTentativeRepprovedStudents;

	/**
	 * @var        Criteria The criteria used to select the current contents of collTentativeRepprovedStudents.
	 */
	private $lastTentativeRepprovedStudentCriteria = null;

	/**
	 * @var        array StudentCareerSchoolYearConduct[] Collection to store aggregation of StudentCareerSchoolYearConduct objects.
	 */
	protected $collStudentCareerSchoolYearConducts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentCareerSchoolYearConducts.
	 */
	private $lastStudentCareerSchoolYearConductCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseStudentCareerSchoolYear object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->status = 0;
		$this->is_processed = false;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [career_school_year_id] column value.
	 * Referencia a la carrera en el año lectivo
	 * @return     int
	 */
	public function getCareerSchoolYearId()
	{
		return $this->career_school_year_id;
	}

	/**
	 * Get the [student_id] column value.
	 * Referencia al estudiante
	 * @return     int
	 */
	public function getStudentId()
	{
		return $this->student_id;
	}

	/**
	 * Get the [year] column value.
	 * 
	 * @return     int
	 */
	public function getYear()
	{
		return $this->year;
	}

	/**
	 * Get the [status] column value.
	 * Campo que dice el estado del alumno en la carrera en ese año (cursando, paso de año y repitio)
	 * @return     int
	 */
	public function getStatus()
	{
		return $this->status;
	}

	/**
	 * Get the [is_processed] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsProcessed()
	{
		return $this->is_processed;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = StudentCareerSchoolYearPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = StudentCareerSchoolYearPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Set the value of [career_school_year_id] column.
	 * Referencia a la carrera en el año lectivo
	 * @param      int $v new value
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setCareerSchoolYearId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->career_school_year_id !== $v) {
			$this->career_school_year_id = $v;
			$this->modifiedColumns[] = StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID;
		}

		if ($this->aCareerSchoolYear !== null && $this->aCareerSchoolYear->getId() !== $v) {
			$this->aCareerSchoolYear = null;
		}

		return $this;
	} // setCareerSchoolYearId()

	/**
	 * Set the value of [student_id] column.
	 * Referencia al estudiante
	 * @param      int $v new value
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setStudentId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->student_id !== $v) {
			$this->student_id = $v;
			$this->modifiedColumns[] = StudentCareerSchoolYearPeer::STUDENT_ID;
		}

		if ($this->aStudent !== null && $this->aStudent->getId() !== $v) {
			$this->aStudent = null;
		}

		return $this;
	} // setStudentId()

	/**
	 * Set the value of [year] column.
	 * 
	 * @param      int $v new value
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setYear($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->year !== $v) {
			$this->year = $v;
			$this->modifiedColumns[] = StudentCareerSchoolYearPeer::YEAR;
		}

		return $this;
	} // setYear()

	/**
	 * Set the value of [status] column.
	 * Campo que dice el estado del alumno en la carrera en ese año (cursando, paso de año y repitio)
	 * @param      int $v new value
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setStatus($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->status !== $v || $v === 0) {
			$this->status = $v;
			$this->modifiedColumns[] = StudentCareerSchoolYearPeer::STATUS;
		}

		return $this;
	} // setStatus()

	/**
	 * Set the value of [is_processed] column.
	 * 
	 * @param      boolean $v new value
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 */
	public function setIsProcessed($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_processed !== $v || $v === false) {
			$this->is_processed = $v;
			$this->modifiedColumns[] = StudentCareerSchoolYearPeer::IS_PROCESSED;
		}

		return $this;
	} // setIsProcessed()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(StudentCareerSchoolYearPeer::STATUS,StudentCareerSchoolYearPeer::IS_PROCESSED))) {
				return false;
			}

			if ($this->status !== 0) {
				return false;
			}

			if ($this->is_processed !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->created_at = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->career_school_year_id = ($row[$startcol + 2] !== null) ? (int) $row[$startcol + 2] : null;
			$this->student_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->year = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->status = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->is_processed = ($row[$startcol + 6] !== null) ? (boolean) $row[$startcol + 6] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 7; // 7 = StudentCareerSchoolYearPeer::NUM_COLUMNS - StudentCareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating StudentCareerSchoolYear object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aCareerSchoolYear !== null && $this->career_school_year_id !== $this->aCareerSchoolYear->getId()) {
			$this->aCareerSchoolYear = null;
		}
		if ($this->aStudent !== null && $this->student_id !== $this->aStudent->getId()) {
			$this->aStudent = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentCareerSchoolYearPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = StudentCareerSchoolYearPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aCareerSchoolYear = null;
			$this->aStudent = null;
			$this->collTentativeRepprovedStudents = null;
			$this->lastTentativeRepprovedStudentCriteria = null;

			$this->collStudentCareerSchoolYearConducts = null;
			$this->lastStudentCareerSchoolYearConductCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentCareerSchoolYear:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentCareerSchoolYearPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			StudentCareerSchoolYearPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseStudentCareerSchoolYear:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentCareerSchoolYear:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(StudentCareerSchoolYearPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentCareerSchoolYearPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseStudentCareerSchoolYear:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			StudentCareerSchoolYearPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareerSchoolYear !== null) {
				if ($this->aCareerSchoolYear->isModified() || $this->aCareerSchoolYear->isNew()) {
					$affectedRows += $this->aCareerSchoolYear->save($con);
				}
				$this->setCareerSchoolYear($this->aCareerSchoolYear);
			}

			if ($this->aStudent !== null) {
				if ($this->aStudent->isModified() || $this->aStudent->isNew()) {
					$affectedRows += $this->aStudent->save($con);
				}
				$this->setStudent($this->aStudent);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = StudentCareerSchoolYearPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = StudentCareerSchoolYearPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += StudentCareerSchoolYearPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collTentativeRepprovedStudents !== null) {
				foreach ($this->collTentativeRepprovedStudents as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentCareerSchoolYearConducts !== null) {
				foreach ($this->collStudentCareerSchoolYearConducts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareerSchoolYear !== null) {
				if (!$this->aCareerSchoolYear->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCareerSchoolYear->getValidationFailures());
				}
			}

			if ($this->aStudent !== null) {
				if (!$this->aStudent->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aStudent->getValidationFailures());
				}
			}


			if (($retval = StudentCareerSchoolYearPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collTentativeRepprovedStudents !== null) {
					foreach ($this->collTentativeRepprovedStudents as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentCareerSchoolYearConducts !== null) {
					foreach ($this->collStudentCareerSchoolYearConducts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = StudentCareerSchoolYearPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getCreatedAt();
				break;
			case 2:
				return $this->getCareerSchoolYearId();
				break;
			case 3:
				return $this->getStudentId();
				break;
			case 4:
				return $this->getYear();
				break;
			case 5:
				return $this->getStatus();
				break;
			case 6:
				return $this->getIsProcessed();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = StudentCareerSchoolYearPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getCreatedAt(),
			$keys[2] => $this->getCareerSchoolYearId(),
			$keys[3] => $this->getStudentId(),
			$keys[4] => $this->getYear(),
			$keys[5] => $this->getStatus(),
			$keys[6] => $this->getIsProcessed(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = StudentCareerSchoolYearPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setCreatedAt($value);
				break;
			case 2:
				$this->setCareerSchoolYearId($value);
				break;
			case 3:
				$this->setStudentId($value);
				break;
			case 4:
				$this->setYear($value);
				break;
			case 5:
				$this->setStatus($value);
				break;
			case 6:
				$this->setIsProcessed($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = StudentCareerSchoolYearPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCreatedAt($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCareerSchoolYearId($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setStudentId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setYear($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setStatus($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setIsProcessed($arr[$keys[6]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);

		if ($this->isColumnModified(StudentCareerSchoolYearPeer::ID)) $criteria->add(StudentCareerSchoolYearPeer::ID, $this->id);
		if ($this->isColumnModified(StudentCareerSchoolYearPeer::CREATED_AT)) $criteria->add(StudentCareerSchoolYearPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID)) $criteria->add(StudentCareerSchoolYearPeer::CAREER_SCHOOL_YEAR_ID, $this->career_school_year_id);
		if ($this->isColumnModified(StudentCareerSchoolYearPeer::STUDENT_ID)) $criteria->add(StudentCareerSchoolYearPeer::STUDENT_ID, $this->student_id);
		if ($this->isColumnModified(StudentCareerSchoolYearPeer::YEAR)) $criteria->add(StudentCareerSchoolYearPeer::YEAR, $this->year);
		if ($this->isColumnModified(StudentCareerSchoolYearPeer::STATUS)) $criteria->add(StudentCareerSchoolYearPeer::STATUS, $this->status);
		if ($this->isColumnModified(StudentCareerSchoolYearPeer::IS_PROCESSED)) $criteria->add(StudentCareerSchoolYearPeer::IS_PROCESSED, $this->is_processed);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);

		$criteria->add(StudentCareerSchoolYearPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of StudentCareerSchoolYear (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setCareerSchoolYearId($this->career_school_year_id);

		$copyObj->setStudentId($this->student_id);

		$copyObj->setYear($this->year);

		$copyObj->setStatus($this->status);

		$copyObj->setIsProcessed($this->is_processed);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getTentativeRepprovedStudents() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addTentativeRepprovedStudent($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentCareerSchoolYearConducts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentCareerSchoolYearConduct($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     StudentCareerSchoolYear Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     StudentCareerSchoolYearPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new StudentCareerSchoolYearPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a CareerSchoolYear object.
	 *
	 * @param      CareerSchoolYear $v
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCareerSchoolYear(CareerSchoolYear $v = null)
	{
		if ($v === null) {
			$this->setCareerSchoolYearId(NULL);
		} else {
			$this->setCareerSchoolYearId($v->getId());
		}

		$this->aCareerSchoolYear = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the CareerSchoolYear object, it will not be re-added.
		if ($v !== null) {
			$v->addStudentCareerSchoolYear($this);
		}

		return $this;
	}


	/**
	 * Get the associated CareerSchoolYear object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     CareerSchoolYear The associated CareerSchoolYear object.
	 * @throws     PropelException
	 */
	public function getCareerSchoolYear(PropelPDO $con = null)
	{
		if ($this->aCareerSchoolYear === null && ($this->career_school_year_id !== null)) {
			$c = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
			$c->add(CareerSchoolYearPeer::ID, $this->career_school_year_id);
			$this->aCareerSchoolYear = CareerSchoolYearPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCareerSchoolYear->addStudentCareerSchoolYears($this);
			 */
		}
		return $this->aCareerSchoolYear;
	}

	/**
	 * Declares an association between this object and a Student object.
	 *
	 * @param      Student $v
	 * @return     StudentCareerSchoolYear The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setStudent(Student $v = null)
	{
		if ($v === null) {
			$this->setStudentId(NULL);
		} else {
			$this->setStudentId($v->getId());
		}

		$this->aStudent = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Student object, it will not be re-added.
		if ($v !== null) {
			$v->addStudentCareerSchoolYear($this);
		}

		return $this;
	}


	/**
	 * Get the associated Student object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Student The associated Student object.
	 * @throws     PropelException
	 */
	public function getStudent(PropelPDO $con = null)
	{
		if ($this->aStudent === null && ($this->student_id !== null)) {
			$c = new Criteria(StudentPeer::DATABASE_NAME);
			$c->add(StudentPeer::ID, $this->student_id);
			$this->aStudent = StudentPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aStudent->addStudentCareerSchoolYears($this);
			 */
		}
		return $this->aStudent;
	}

	/**
	 * Clears out the collTentativeRepprovedStudents collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addTentativeRepprovedStudents()
	 */
	public function clearTentativeRepprovedStudents()
	{
		$this->collTentativeRepprovedStudents = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collTentativeRepprovedStudents collection (array).
	 *
	 * By default this just sets the collTentativeRepprovedStudents collection to an empty array (like clearcollTentativeRepprovedStudents());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initTentativeRepprovedStudents()
	{
		$this->collTentativeRepprovedStudents = array();
	}

	/**
	 * Gets an array of TentativeRepprovedStudent objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this StudentCareerSchoolYear has previously been saved, it will retrieve
	 * related TentativeRepprovedStudents from storage. If this StudentCareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array TentativeRepprovedStudent[]
	 * @throws     PropelException
	 */
	public function getTentativeRepprovedStudents($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collTentativeRepprovedStudents === null) {
			if ($this->isNew()) {
			   $this->collTentativeRepprovedStudents = array();
			} else {

				$criteria->add(TentativeRepprovedStudentPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				TentativeRepprovedStudentPeer::addSelectColumns($criteria);
				$this->collTentativeRepprovedStudents = TentativeRepprovedStudentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(TentativeRepprovedStudentPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				TentativeRepprovedStudentPeer::addSelectColumns($criteria);
				if (!isset($this->lastTentativeRepprovedStudentCriteria) || !$this->lastTentativeRepprovedStudentCriteria->equals($criteria)) {
					$this->collTentativeRepprovedStudents = TentativeRepprovedStudentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastTentativeRepprovedStudentCriteria = $criteria;
		return $this->collTentativeRepprovedStudents;
	}

	/**
	 * Returns the number of related TentativeRepprovedStudent objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related TentativeRepprovedStudent objects.
	 * @throws     PropelException
	 */
	public function countTentativeRepprovedStudents(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collTentativeRepprovedStudents === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(TentativeRepprovedStudentPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = TentativeRepprovedStudentPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(TentativeRepprovedStudentPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastTentativeRepprovedStudentCriteria) || !$this->lastTentativeRepprovedStudentCriteria->equals($criteria)) {
					$count = TentativeRepprovedStudentPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collTentativeRepprovedStudents);
				}
			} else {
				$count = count($this->collTentativeRepprovedStudents);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a TentativeRepprovedStudent object to this object
	 * through the TentativeRepprovedStudent foreign key attribute.
	 *
	 * @param      TentativeRepprovedStudent $l TentativeRepprovedStudent
	 * @return     void
	 * @throws     PropelException
	 */
	public function addTentativeRepprovedStudent(TentativeRepprovedStudent $l)
	{
		if ($this->collTentativeRepprovedStudents === null) {
			$this->initTentativeRepprovedStudents();
		}
		if (!in_array($l, $this->collTentativeRepprovedStudents, true)) { // only add it if the **same** object is not already associated
			array_push($this->collTentativeRepprovedStudents, $l);
			$l->setStudentCareerSchoolYear($this);
		}
	}

	/**
	 * Clears out the collStudentCareerSchoolYearConducts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentCareerSchoolYearConducts()
	 */
	public function clearStudentCareerSchoolYearConducts()
	{
		$this->collStudentCareerSchoolYearConducts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentCareerSchoolYearConducts collection (array).
	 *
	 * By default this just sets the collStudentCareerSchoolYearConducts collection to an empty array (like clearcollStudentCareerSchoolYearConducts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentCareerSchoolYearConducts()
	{
		$this->collStudentCareerSchoolYearConducts = array();
	}

	/**
	 * Gets an array of StudentCareerSchoolYearConduct objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this StudentCareerSchoolYear has previously been saved, it will retrieve
	 * related StudentCareerSchoolYearConducts from storage. If this StudentCareerSchoolYear is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentCareerSchoolYearConduct[]
	 * @throws     PropelException
	 */
	public function getStudentCareerSchoolYearConducts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
			   $this->collStudentCareerSchoolYearConducts = array();
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentCareerSchoolYearConductPeer::addSelectColumns($criteria);
				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				StudentCareerSchoolYearConductPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
					$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentCareerSchoolYearConductCriteria = $criteria;
		return $this->collStudentCareerSchoolYearConducts;
	}

	/**
	 * Returns the number of related StudentCareerSchoolYearConduct objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentCareerSchoolYearConduct objects.
	 * @throws     PropelException
	 */
	public function countStudentCareerSchoolYearConducts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				$count = StudentCareerSchoolYearConductPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
					$count = StudentCareerSchoolYearConductPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentCareerSchoolYearConducts);
				}
			} else {
				$count = count($this->collStudentCareerSchoolYearConducts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentCareerSchoolYearConduct object to this object
	 * through the StudentCareerSchoolYearConduct foreign key attribute.
	 *
	 * @param      StudentCareerSchoolYearConduct $l StudentCareerSchoolYearConduct
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentCareerSchoolYearConduct(StudentCareerSchoolYearConduct $l)
	{
		if ($this->collStudentCareerSchoolYearConducts === null) {
			$this->initStudentCareerSchoolYearConducts();
		}
		if (!in_array($l, $this->collStudentCareerSchoolYearConducts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentCareerSchoolYearConducts, $l);
			$l->setStudentCareerSchoolYear($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentCareerSchoolYear is new, it will return
	 * an empty collection; or if this StudentCareerSchoolYear has previously
	 * been saved, it will retrieve related StudentCareerSchoolYearConducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentCareerSchoolYear.
	 */
	public function getStudentCareerSchoolYearConductsJoinConduct($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
				$this->collStudentCareerSchoolYearConducts = array();
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinConduct($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinConduct($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentCareerSchoolYearConductCriteria = $criteria;

		return $this->collStudentCareerSchoolYearConducts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentCareerSchoolYear is new, it will return
	 * an empty collection; or if this StudentCareerSchoolYear has previously
	 * been saved, it will retrieve related StudentCareerSchoolYearConducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentCareerSchoolYear.
	 */
	public function getStudentCareerSchoolYearConductsJoinCareerSchoolYearPeriod($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentCareerSchoolYearPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
				$this->collStudentCareerSchoolYearConducts = array();
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinCareerSchoolYearPeriod($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentCareerSchoolYearConductPeer::STUDENT_CAREER_SCHOOL_YEAR_ID, $this->id);

			if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinCareerSchoolYearPeriod($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentCareerSchoolYearConductCriteria = $criteria;

		return $this->collStudentCareerSchoolYearConducts;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collTentativeRepprovedStudents) {
				foreach ((array) $this->collTentativeRepprovedStudents as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentCareerSchoolYearConducts) {
				foreach ((array) $this->collStudentCareerSchoolYearConducts as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collTentativeRepprovedStudents = null;
		$this->collStudentCareerSchoolYearConducts = null;
			$this->aCareerSchoolYear = null;
			$this->aStudent = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseStudentCareerSchoolYear:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseStudentCareerSchoolYear::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseStudentCareerSchoolYear
