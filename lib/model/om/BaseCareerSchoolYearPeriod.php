<?php

/**
 * Base class that represents a row from the 'career_school_year_period' table.
 *
 * Cada tupla representa un periodo en una carrera de un año lectivo
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:10 2018
 *
 * @package    lib.model.om
 */
abstract class BaseCareerSchoolYearPeriod extends BaseObject  implements Persistent {


  const PEER = 'CareerSchoolYearPeriodPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CareerSchoolYearPeriodPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the name field.
	 * @var        string
	 */
	protected $name;

	/**
	 * The value for the short_name field.
	 * @var        string
	 */
	protected $short_name;

	/**
	 * The value for the career_school_year_id field.
	 * @var        int
	 */
	protected $career_school_year_id;

	/**
	 * The value for the start_at field.
	 * @var        string
	 */
	protected $start_at;

	/**
	 * The value for the end_at field.
	 * @var        string
	 */
	protected $end_at;

	/**
	 * The value for the is_closed field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_closed;

	/**
	 * The value for the course_type field.
	 * @var        int
	 */
	protected $course_type;

	/**
	 * The value for the career_school_year_period_id field.
	 * @var        int
	 */
	protected $career_school_year_period_id;

	/**
	 * The value for the max_absences field.
	 * @var        int
	 */
	protected $max_absences;

	/**
	 * @var        CareerSchoolYear
	 */
	protected $aCareerSchoolYear;

	/**
	 * @var        CareerSchoolYearPeriod
	 */
	protected $aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId;

	/**
	 * @var        array CourseSubjectConfiguration[] Collection to store aggregation of CourseSubjectConfiguration objects.
	 */
	protected $collCourseSubjectConfigurations;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCourseSubjectConfigurations.
	 */
	private $lastCourseSubjectConfigurationCriteria = null;

	/**
	 * @var        array StudentReincorporation[] Collection to store aggregation of StudentReincorporation objects.
	 */
	protected $collStudentReincorporations;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentReincorporations.
	 */
	private $lastStudentReincorporationCriteria = null;

	/**
	 * @var        array StudentFree[] Collection to store aggregation of StudentFree objects.
	 */
	protected $collStudentFrees;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentFrees.
	 */
	private $lastStudentFreeCriteria = null;

	/**
	 * @var        array StudentCareerSchoolYearConduct[] Collection to store aggregation of StudentCareerSchoolYearConduct objects.
	 */
	protected $collStudentCareerSchoolYearConducts;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentCareerSchoolYearConducts.
	 */
	private $lastStudentCareerSchoolYearConductCriteria = null;

	/**
	 * @var        array CareerSchoolYearPeriod[] Collection to store aggregation of CareerSchoolYearPeriod objects.
	 */
	protected $collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId.
	 */
	private $lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseCareerSchoolYearPeriod object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_closed = false;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [name] column value.
	 * 
	 * @return     string
	 */
	public function getName()
	{
		return $this->name;
	}

	/**
	 * Get the [short_name] column value.
	 * 
	 * @return     string
	 */
	public function getShortName()
	{
		return $this->short_name;
	}

	/**
	 * Get the [career_school_year_id] column value.
	 * Referencia a una carrera para un año lectivo
	 * @return     int
	 */
	public function getCareerSchoolYearId()
	{
		return $this->career_school_year_id;
	}

	/**
	 * Get the [optionally formatted] temporal [start_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getStartAt($format = 'Y-m-d')
	{
		if ($this->start_at === null) {
			return null;
		}


		if ($this->start_at === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->start_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->start_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [end_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getEndAt($format = 'Y-m-d')
	{
		if ($this->end_at === null) {
			return null;
		}


		if ($this->end_at === '0000-00-00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->end_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->end_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [is_closed] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsClosed()
	{
		return $this->is_closed;
	}

	/**
	 * Get the [course_type] column value.
	 * Indica el tipo de la materia (anual, anual cuatrimestral, etc)
	 * @return     int
	 */
	public function getCourseType()
	{
		return $this->course_type;
	}

	/**
	 * Get the [career_school_year_period_id] column value.
	 * Referencia a al periodo padre (En caso de ser un bimestre, señala a un cuatrimestre padre).
	 * @return     int
	 */
	public function getCareerSchoolYearPeriodId()
	{
		return $this->career_school_year_period_id;
	}

	/**
	 * Get the [max_absences] column value.
	 * 
	 * @return     int
	 */
	public function getMaxAbsences()
	{
		return $this->max_absences;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [name] column.
	 * 
	 * @param      string $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->name !== $v) {
			$this->name = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::NAME;
		}

		return $this;
	} // setName()

	/**
	 * Set the value of [short_name] column.
	 * 
	 * @param      string $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setShortName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->short_name !== $v) {
			$this->short_name = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::SHORT_NAME;
		}

		return $this;
	} // setShortName()

	/**
	 * Set the value of [career_school_year_id] column.
	 * Referencia a una carrera para un año lectivo
	 * @param      int $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setCareerSchoolYearId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->career_school_year_id !== $v) {
			$this->career_school_year_id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID;
		}

		if ($this->aCareerSchoolYear !== null && $this->aCareerSchoolYear->getId() !== $v) {
			$this->aCareerSchoolYear = null;
		}

		return $this;
	} // setCareerSchoolYearId()

	/**
	 * Sets the value of [start_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setStartAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->start_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->start_at !== null && $tmpDt = new DateTime($this->start_at)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->start_at = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::START_AT;
			}
		} // if either are not null

		return $this;
	} // setStartAt()

	/**
	 * Sets the value of [end_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setEndAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->end_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->end_at !== null && $tmpDt = new DateTime($this->end_at)) ? $tmpDt->format('Y-m-d') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->end_at = ($dt ? $dt->format('Y-m-d') : null);
				$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::END_AT;
			}
		} // if either are not null

		return $this;
	} // setEndAt()

	/**
	 * Set the value of [is_closed] column.
	 * 
	 * @param      boolean $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setIsClosed($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_closed !== $v || $v === false) {
			$this->is_closed = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::IS_CLOSED;
		}

		return $this;
	} // setIsClosed()

	/**
	 * Set the value of [course_type] column.
	 * Indica el tipo de la materia (anual, anual cuatrimestral, etc)
	 * @param      int $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setCourseType($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->course_type !== $v) {
			$this->course_type = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::COURSE_TYPE;
		}

		return $this;
	} // setCourseType()

	/**
	 * Set the value of [career_school_year_period_id] column.
	 * Referencia a al periodo padre (En caso de ser un bimestre, señala a un cuatrimestre padre).
	 * @param      int $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setCareerSchoolYearPeriodId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->career_school_year_period_id !== $v) {
			$this->career_school_year_period_id = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID;
		}

		if ($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId !== null && $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->getId() !== $v) {
			$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId = null;
		}

		return $this;
	} // setCareerSchoolYearPeriodId()

	/**
	 * Set the value of [max_absences] column.
	 * 
	 * @param      int $v new value
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 */
	public function setMaxAbsences($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->max_absences !== $v) {
			$this->max_absences = $v;
			$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::MAX_ABSENCES;
		}

		return $this;
	} // setMaxAbsences()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(CareerSchoolYearPeriodPeer::IS_CLOSED))) {
				return false;
			}

			if ($this->is_closed !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->name = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->short_name = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->career_school_year_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->start_at = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->end_at = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->is_closed = ($row[$startcol + 6] !== null) ? (boolean) $row[$startcol + 6] : null;
			$this->course_type = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->career_school_year_period_id = ($row[$startcol + 8] !== null) ? (int) $row[$startcol + 8] : null;
			$this->max_absences = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 10; // 10 = CareerSchoolYearPeriodPeer::NUM_COLUMNS - CareerSchoolYearPeriodPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating CareerSchoolYearPeriod object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aCareerSchoolYear !== null && $this->career_school_year_id !== $this->aCareerSchoolYear->getId()) {
			$this->aCareerSchoolYear = null;
		}
		if ($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId !== null && $this->career_school_year_period_id !== $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->getId()) {
			$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSchoolYearPeriodPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CareerSchoolYearPeriodPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aCareerSchoolYear = null;
			$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId = null;
			$this->collCourseSubjectConfigurations = null;
			$this->lastCourseSubjectConfigurationCriteria = null;

			$this->collStudentReincorporations = null;
			$this->lastStudentReincorporationCriteria = null;

			$this->collStudentFrees = null;
			$this->lastStudentFreeCriteria = null;

			$this->collStudentCareerSchoolYearConducts = null;
			$this->lastStudentCareerSchoolYearConductCriteria = null;

			$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = null;
			$this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareerSchoolYearPeriod:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSchoolYearPeriodPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			CareerSchoolYearPeriodPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseCareerSchoolYearPeriod:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareerSchoolYearPeriod:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerSchoolYearPeriodPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseCareerSchoolYearPeriod:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			CareerSchoolYearPeriodPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareerSchoolYear !== null) {
				if ($this->aCareerSchoolYear->isModified() || $this->aCareerSchoolYear->isNew()) {
					$affectedRows += $this->aCareerSchoolYear->save($con);
				}
				$this->setCareerSchoolYear($this->aCareerSchoolYear);
			}

			if ($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId !== null) {
				if ($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->isModified() || $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->isNew()) {
					$affectedRows += $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->save($con);
				}
				$this->setCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CareerSchoolYearPeriodPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CareerSchoolYearPeriodPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CareerSchoolYearPeriodPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCourseSubjectConfigurations !== null) {
				foreach ($this->collCourseSubjectConfigurations as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentReincorporations !== null) {
				foreach ($this->collStudentReincorporations as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentFrees !== null) {
				foreach ($this->collStudentFrees as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentCareerSchoolYearConducts !== null) {
				foreach ($this->collStudentCareerSchoolYearConducts as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId !== null) {
				foreach ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareerSchoolYear !== null) {
				if (!$this->aCareerSchoolYear->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCareerSchoolYear->getValidationFailures());
				}
			}

			if ($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId !== null) {
				if (!$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->getValidationFailures());
				}
			}


			if (($retval = CareerSchoolYearPeriodPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCourseSubjectConfigurations !== null) {
					foreach ($this->collCourseSubjectConfigurations as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentReincorporations !== null) {
					foreach ($this->collStudentReincorporations as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentFrees !== null) {
					foreach ($this->collStudentFrees as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentCareerSchoolYearConducts !== null) {
					foreach ($this->collStudentCareerSchoolYearConducts as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId !== null) {
					foreach ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerSchoolYearPeriodPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getName();
				break;
			case 2:
				return $this->getShortName();
				break;
			case 3:
				return $this->getCareerSchoolYearId();
				break;
			case 4:
				return $this->getStartAt();
				break;
			case 5:
				return $this->getEndAt();
				break;
			case 6:
				return $this->getIsClosed();
				break;
			case 7:
				return $this->getCourseType();
				break;
			case 8:
				return $this->getCareerSchoolYearPeriodId();
				break;
			case 9:
				return $this->getMaxAbsences();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CareerSchoolYearPeriodPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getName(),
			$keys[2] => $this->getShortName(),
			$keys[3] => $this->getCareerSchoolYearId(),
			$keys[4] => $this->getStartAt(),
			$keys[5] => $this->getEndAt(),
			$keys[6] => $this->getIsClosed(),
			$keys[7] => $this->getCourseType(),
			$keys[8] => $this->getCareerSchoolYearPeriodId(),
			$keys[9] => $this->getMaxAbsences(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerSchoolYearPeriodPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setName($value);
				break;
			case 2:
				$this->setShortName($value);
				break;
			case 3:
				$this->setCareerSchoolYearId($value);
				break;
			case 4:
				$this->setStartAt($value);
				break;
			case 5:
				$this->setEndAt($value);
				break;
			case 6:
				$this->setIsClosed($value);
				break;
			case 7:
				$this->setCourseType($value);
				break;
			case 8:
				$this->setCareerSchoolYearPeriodId($value);
				break;
			case 9:
				$this->setMaxAbsences($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CareerSchoolYearPeriodPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setName($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setShortName($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCareerSchoolYearId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setStartAt($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setEndAt($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setIsClosed($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setCourseType($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setCareerSchoolYearPeriodId($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setMaxAbsences($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);

		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::ID)) $criteria->add(CareerSchoolYearPeriodPeer::ID, $this->id);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::NAME)) $criteria->add(CareerSchoolYearPeriodPeer::NAME, $this->name);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::SHORT_NAME)) $criteria->add(CareerSchoolYearPeriodPeer::SHORT_NAME, $this->short_name);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID)) $criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_ID, $this->career_school_year_id);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::START_AT)) $criteria->add(CareerSchoolYearPeriodPeer::START_AT, $this->start_at);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::END_AT)) $criteria->add(CareerSchoolYearPeriodPeer::END_AT, $this->end_at);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::IS_CLOSED)) $criteria->add(CareerSchoolYearPeriodPeer::IS_CLOSED, $this->is_closed);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::COURSE_TYPE)) $criteria->add(CareerSchoolYearPeriodPeer::COURSE_TYPE, $this->course_type);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID)) $criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->career_school_year_period_id);
		if ($this->isColumnModified(CareerSchoolYearPeriodPeer::MAX_ABSENCES)) $criteria->add(CareerSchoolYearPeriodPeer::MAX_ABSENCES, $this->max_absences);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);

		$criteria->add(CareerSchoolYearPeriodPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of CareerSchoolYearPeriod (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setName($this->name);

		$copyObj->setShortName($this->short_name);

		$copyObj->setCareerSchoolYearId($this->career_school_year_id);

		$copyObj->setStartAt($this->start_at);

		$copyObj->setEndAt($this->end_at);

		$copyObj->setIsClosed($this->is_closed);

		$copyObj->setCourseType($this->course_type);

		$copyObj->setCareerSchoolYearPeriodId($this->career_school_year_period_id);

		$copyObj->setMaxAbsences($this->max_absences);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCourseSubjectConfigurations() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCourseSubjectConfiguration($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentReincorporations() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentReincorporation($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentFrees() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentFree($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentCareerSchoolYearConducts() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentCareerSchoolYearConduct($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     CareerSchoolYearPeriod Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CareerSchoolYearPeriodPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CareerSchoolYearPeriodPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a CareerSchoolYear object.
	 *
	 * @param      CareerSchoolYear $v
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCareerSchoolYear(CareerSchoolYear $v = null)
	{
		if ($v === null) {
			$this->setCareerSchoolYearId(NULL);
		} else {
			$this->setCareerSchoolYearId($v->getId());
		}

		$this->aCareerSchoolYear = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the CareerSchoolYear object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSchoolYearPeriod($this);
		}

		return $this;
	}


	/**
	 * Get the associated CareerSchoolYear object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     CareerSchoolYear The associated CareerSchoolYear object.
	 * @throws     PropelException
	 */
	public function getCareerSchoolYear(PropelPDO $con = null)
	{
		if ($this->aCareerSchoolYear === null && ($this->career_school_year_id !== null)) {
			$c = new Criteria(CareerSchoolYearPeer::DATABASE_NAME);
			$c->add(CareerSchoolYearPeer::ID, $this->career_school_year_id);
			$this->aCareerSchoolYear = CareerSchoolYearPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCareerSchoolYear->addCareerSchoolYearPeriods($this);
			 */
		}
		return $this->aCareerSchoolYear;
	}

	/**
	 * Declares an association between this object and a CareerSchoolYearPeriod object.
	 *
	 * @param      CareerSchoolYearPeriod $v
	 * @return     CareerSchoolYearPeriod The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId(CareerSchoolYearPeriod $v = null)
	{
		if ($v === null) {
			$this->setCareerSchoolYearPeriodId(NULL);
		} else {
			$this->setCareerSchoolYearPeriodId($v->getId());
		}

		$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the CareerSchoolYearPeriod object, it will not be re-added.
		if ($v !== null) {
			$v->addCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($this);
		}

		return $this;
	}


	/**
	 * Get the associated CareerSchoolYearPeriod object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     CareerSchoolYearPeriod The associated CareerSchoolYearPeriod object.
	 * @throws     PropelException
	 */
	public function getCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId(PropelPDO $con = null)
	{
		if ($this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId === null && ($this->career_school_year_period_id !== null)) {
			$c = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
			$c->add(CareerSchoolYearPeriodPeer::ID, $this->career_school_year_period_id);
			$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId = CareerSchoolYearPeriodPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId->addCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId($this);
			 */
		}
		return $this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId;
	}

	/**
	 * Clears out the collCourseSubjectConfigurations collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCourseSubjectConfigurations()
	 */
	public function clearCourseSubjectConfigurations()
	{
		$this->collCourseSubjectConfigurations = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCourseSubjectConfigurations collection (array).
	 *
	 * By default this just sets the collCourseSubjectConfigurations collection to an empty array (like clearcollCourseSubjectConfigurations());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCourseSubjectConfigurations()
	{
		$this->collCourseSubjectConfigurations = array();
	}

	/**
	 * Gets an array of CourseSubjectConfiguration objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod has previously been saved, it will retrieve
	 * related CourseSubjectConfigurations from storage. If this CareerSchoolYearPeriod is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CourseSubjectConfiguration[]
	 * @throws     PropelException
	 */
	public function getCourseSubjectConfigurations($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCourseSubjectConfigurations === null) {
			if ($this->isNew()) {
			   $this->collCourseSubjectConfigurations = array();
			} else {

				$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				CourseSubjectConfigurationPeer::addSelectColumns($criteria);
				$this->collCourseSubjectConfigurations = CourseSubjectConfigurationPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				CourseSubjectConfigurationPeer::addSelectColumns($criteria);
				if (!isset($this->lastCourseSubjectConfigurationCriteria) || !$this->lastCourseSubjectConfigurationCriteria->equals($criteria)) {
					$this->collCourseSubjectConfigurations = CourseSubjectConfigurationPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCourseSubjectConfigurationCriteria = $criteria;
		return $this->collCourseSubjectConfigurations;
	}

	/**
	 * Returns the number of related CourseSubjectConfiguration objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CourseSubjectConfiguration objects.
	 * @throws     PropelException
	 */
	public function countCourseSubjectConfigurations(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCourseSubjectConfigurations === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$count = CourseSubjectConfigurationPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				if (!isset($this->lastCourseSubjectConfigurationCriteria) || !$this->lastCourseSubjectConfigurationCriteria->equals($criteria)) {
					$count = CourseSubjectConfigurationPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCourseSubjectConfigurations);
				}
			} else {
				$count = count($this->collCourseSubjectConfigurations);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CourseSubjectConfiguration object to this object
	 * through the CourseSubjectConfiguration foreign key attribute.
	 *
	 * @param      CourseSubjectConfiguration $l CourseSubjectConfiguration
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCourseSubjectConfiguration(CourseSubjectConfiguration $l)
	{
		if ($this->collCourseSubjectConfigurations === null) {
			$this->initCourseSubjectConfigurations();
		}
		if (!in_array($l, $this->collCourseSubjectConfigurations, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCourseSubjectConfigurations, $l);
			$l->setCareerSchoolYearPeriod($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related CourseSubjectConfigurations from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getCourseSubjectConfigurationsJoinCourseSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCourseSubjectConfigurations === null) {
			if ($this->isNew()) {
				$this->collCourseSubjectConfigurations = array();
			} else {

				$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collCourseSubjectConfigurations = CourseSubjectConfigurationPeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastCourseSubjectConfigurationCriteria) || !$this->lastCourseSubjectConfigurationCriteria->equals($criteria)) {
				$this->collCourseSubjectConfigurations = CourseSubjectConfigurationPeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastCourseSubjectConfigurationCriteria = $criteria;

		return $this->collCourseSubjectConfigurations;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related CourseSubjectConfigurations from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getCourseSubjectConfigurationsJoinDivision($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCourseSubjectConfigurations === null) {
			if ($this->isNew()) {
				$this->collCourseSubjectConfigurations = array();
			} else {

				$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collCourseSubjectConfigurations = CourseSubjectConfigurationPeer::doSelectJoinDivision($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CourseSubjectConfigurationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastCourseSubjectConfigurationCriteria) || !$this->lastCourseSubjectConfigurationCriteria->equals($criteria)) {
				$this->collCourseSubjectConfigurations = CourseSubjectConfigurationPeer::doSelectJoinDivision($criteria, $con, $join_behavior);
			}
		}
		$this->lastCourseSubjectConfigurationCriteria = $criteria;

		return $this->collCourseSubjectConfigurations;
	}

	/**
	 * Clears out the collStudentReincorporations collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentReincorporations()
	 */
	public function clearStudentReincorporations()
	{
		$this->collStudentReincorporations = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentReincorporations collection (array).
	 *
	 * By default this just sets the collStudentReincorporations collection to an empty array (like clearcollStudentReincorporations());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentReincorporations()
	{
		$this->collStudentReincorporations = array();
	}

	/**
	 * Gets an array of StudentReincorporation objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod has previously been saved, it will retrieve
	 * related StudentReincorporations from storage. If this CareerSchoolYearPeriod is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentReincorporation[]
	 * @throws     PropelException
	 */
	public function getStudentReincorporations($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentReincorporations === null) {
			if ($this->isNew()) {
			   $this->collStudentReincorporations = array();
			} else {

				$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				StudentReincorporationPeer::addSelectColumns($criteria);
				$this->collStudentReincorporations = StudentReincorporationPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				StudentReincorporationPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentReincorporationCriteria) || !$this->lastStudentReincorporationCriteria->equals($criteria)) {
					$this->collStudentReincorporations = StudentReincorporationPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentReincorporationCriteria = $criteria;
		return $this->collStudentReincorporations;
	}

	/**
	 * Returns the number of related StudentReincorporation objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentReincorporation objects.
	 * @throws     PropelException
	 */
	public function countStudentReincorporations(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentReincorporations === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$count = StudentReincorporationPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				if (!isset($this->lastStudentReincorporationCriteria) || !$this->lastStudentReincorporationCriteria->equals($criteria)) {
					$count = StudentReincorporationPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentReincorporations);
				}
			} else {
				$count = count($this->collStudentReincorporations);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentReincorporation object to this object
	 * through the StudentReincorporation foreign key attribute.
	 *
	 * @param      StudentReincorporation $l StudentReincorporation
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentReincorporation(StudentReincorporation $l)
	{
		if ($this->collStudentReincorporations === null) {
			$this->initStudentReincorporations();
		}
		if (!in_array($l, $this->collStudentReincorporations, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentReincorporations, $l);
			$l->setCareerSchoolYearPeriod($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentReincorporations from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentReincorporationsJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentReincorporations === null) {
			if ($this->isNew()) {
				$this->collStudentReincorporations = array();
			} else {

				$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentReincorporations = StudentReincorporationPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentReincorporationCriteria) || !$this->lastStudentReincorporationCriteria->equals($criteria)) {
				$this->collStudentReincorporations = StudentReincorporationPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentReincorporationCriteria = $criteria;

		return $this->collStudentReincorporations;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentReincorporations from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentReincorporationsJoinCourseSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentReincorporations === null) {
			if ($this->isNew()) {
				$this->collStudentReincorporations = array();
			} else {

				$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentReincorporations = StudentReincorporationPeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentReincorporationPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentReincorporationCriteria) || !$this->lastStudentReincorporationCriteria->equals($criteria)) {
				$this->collStudentReincorporations = StudentReincorporationPeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentReincorporationCriteria = $criteria;

		return $this->collStudentReincorporations;
	}

	/**
	 * Clears out the collStudentFrees collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentFrees()
	 */
	public function clearStudentFrees()
	{
		$this->collStudentFrees = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentFrees collection (array).
	 *
	 * By default this just sets the collStudentFrees collection to an empty array (like clearcollStudentFrees());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentFrees()
	{
		$this->collStudentFrees = array();
	}

	/**
	 * Gets an array of StudentFree objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod has previously been saved, it will retrieve
	 * related StudentFrees from storage. If this CareerSchoolYearPeriod is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentFree[]
	 * @throws     PropelException
	 */
	public function getStudentFrees($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
			   $this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				StudentFreePeer::addSelectColumns($criteria);
				$this->collStudentFrees = StudentFreePeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				StudentFreePeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
					$this->collStudentFrees = StudentFreePeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentFreeCriteria = $criteria;
		return $this->collStudentFrees;
	}

	/**
	 * Returns the number of related StudentFree objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentFree objects.
	 * @throws     PropelException
	 */
	public function countStudentFrees(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$count = StudentFreePeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
					$count = StudentFreePeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentFrees);
				}
			} else {
				$count = count($this->collStudentFrees);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentFree object to this object
	 * through the StudentFree foreign key attribute.
	 *
	 * @param      StudentFree $l StudentFree
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentFree(StudentFree $l)
	{
		if ($this->collStudentFrees === null) {
			$this->initStudentFrees();
		}
		if (!in_array($l, $this->collStudentFrees, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentFrees, $l);
			$l->setCareerSchoolYearPeriod($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentFrees from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentFreesJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentFrees = StudentFreePeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
				$this->collStudentFrees = StudentFreePeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentFreeCriteria = $criteria;

		return $this->collStudentFrees;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentFrees from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentFreesJoinCareerSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentFrees = StudentFreePeer::doSelectJoinCareerSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
				$this->collStudentFrees = StudentFreePeer::doSelectJoinCareerSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentFreeCriteria = $criteria;

		return $this->collStudentFrees;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentFrees from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentFreesJoinCourseSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentFrees === null) {
			if ($this->isNew()) {
				$this->collStudentFrees = array();
			} else {

				$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentFrees = StudentFreePeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentFreePeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentFreeCriteria) || !$this->lastStudentFreeCriteria->equals($criteria)) {
				$this->collStudentFrees = StudentFreePeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentFreeCriteria = $criteria;

		return $this->collStudentFrees;
	}

	/**
	 * Clears out the collStudentCareerSchoolYearConducts collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentCareerSchoolYearConducts()
	 */
	public function clearStudentCareerSchoolYearConducts()
	{
		$this->collStudentCareerSchoolYearConducts = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentCareerSchoolYearConducts collection (array).
	 *
	 * By default this just sets the collStudentCareerSchoolYearConducts collection to an empty array (like clearcollStudentCareerSchoolYearConducts());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentCareerSchoolYearConducts()
	{
		$this->collStudentCareerSchoolYearConducts = array();
	}

	/**
	 * Gets an array of StudentCareerSchoolYearConduct objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod has previously been saved, it will retrieve
	 * related StudentCareerSchoolYearConducts from storage. If this CareerSchoolYearPeriod is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentCareerSchoolYearConduct[]
	 * @throws     PropelException
	 */
	public function getStudentCareerSchoolYearConducts($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
			   $this->collStudentCareerSchoolYearConducts = array();
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				StudentCareerSchoolYearConductPeer::addSelectColumns($criteria);
				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				StudentCareerSchoolYearConductPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
					$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentCareerSchoolYearConductCriteria = $criteria;
		return $this->collStudentCareerSchoolYearConducts;
	}

	/**
	 * Returns the number of related StudentCareerSchoolYearConduct objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentCareerSchoolYearConduct objects.
	 * @throws     PropelException
	 */
	public function countStudentCareerSchoolYearConducts(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$count = StudentCareerSchoolYearConductPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
					$count = StudentCareerSchoolYearConductPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentCareerSchoolYearConducts);
				}
			} else {
				$count = count($this->collStudentCareerSchoolYearConducts);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentCareerSchoolYearConduct object to this object
	 * through the StudentCareerSchoolYearConduct foreign key attribute.
	 *
	 * @param      StudentCareerSchoolYearConduct $l StudentCareerSchoolYearConduct
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentCareerSchoolYearConduct(StudentCareerSchoolYearConduct $l)
	{
		if ($this->collStudentCareerSchoolYearConducts === null) {
			$this->initStudentCareerSchoolYearConducts();
		}
		if (!in_array($l, $this->collStudentCareerSchoolYearConducts, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentCareerSchoolYearConducts, $l);
			$l->setCareerSchoolYearPeriod($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentCareerSchoolYearConducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentCareerSchoolYearConductsJoinStudentCareerSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
				$this->collStudentCareerSchoolYearConducts = array();
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinStudentCareerSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinStudentCareerSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentCareerSchoolYearConductCriteria = $criteria;

		return $this->collStudentCareerSchoolYearConducts;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related StudentCareerSchoolYearConducts from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getStudentCareerSchoolYearConductsJoinConduct($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentCareerSchoolYearConducts === null) {
			if ($this->isNew()) {
				$this->collStudentCareerSchoolYearConducts = array();
			} else {

				$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinConduct($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentCareerSchoolYearConductPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastStudentCareerSchoolYearConductCriteria) || !$this->lastStudentCareerSchoolYearConductCriteria->equals($criteria)) {
				$this->collStudentCareerSchoolYearConducts = StudentCareerSchoolYearConductPeer::doSelectJoinConduct($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentCareerSchoolYearConductCriteria = $criteria;

		return $this->collStudentCareerSchoolYearConducts;
	}

	/**
	 * Clears out the collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId()
	 */
	public function clearCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId()
	{
		$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId collection (array).
	 *
	 * By default this just sets the collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId collection to an empty array (like clearcollCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId()
	{
		$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = array();
	}

	/**
	 * Gets an array of CareerSchoolYearPeriod objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod has previously been saved, it will retrieve
	 * related CareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId from storage. If this CareerSchoolYearPeriod is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerSchoolYearPeriod[]
	 * @throws     PropelException
	 */
	public function getCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId === null) {
			if ($this->isNew()) {
			   $this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = array();
			} else {

				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				CareerSchoolYearPeriodPeer::addSelectColumns($criteria);
				$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = CareerSchoolYearPeriodPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				CareerSchoolYearPeriodPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria) || !$this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria->equals($criteria)) {
					$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = CareerSchoolYearPeriodPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria = $criteria;
		return $this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId;
	}

	/**
	 * Returns the number of related CareerSchoolYearPeriod objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerSchoolYearPeriod objects.
	 * @throws     PropelException
	 */
	public function countCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$count = CareerSchoolYearPeriodPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				if (!isset($this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria) || !$this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria->equals($criteria)) {
					$count = CareerSchoolYearPeriodPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId);
				}
			} else {
				$count = count($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerSchoolYearPeriod object to this object
	 * through the CareerSchoolYearPeriod foreign key attribute.
	 *
	 * @param      CareerSchoolYearPeriod $l CareerSchoolYearPeriod
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId(CareerSchoolYearPeriod $l)
	{
		if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId === null) {
			$this->initCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId();
		}
		if (!in_array($l, $this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId, $l);
			$l->setCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this CareerSchoolYearPeriod is new, it will return
	 * an empty collection; or if this CareerSchoolYearPeriod has previously
	 * been saved, it will retrieve related CareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in CareerSchoolYearPeriod.
	 */
	public function getCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodIdJoinCareerSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerSchoolYearPeriodPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId === null) {
			if ($this->isNew()) {
				$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = array();
			} else {

				$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

				$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = CareerSchoolYearPeriodPeer::doSelectJoinCareerSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSchoolYearPeriodPeer::CAREER_SCHOOL_YEAR_PERIOD_ID, $this->id);

			if (!isset($this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria) || !$this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria->equals($criteria)) {
				$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = CareerSchoolYearPeriodPeer::doSelectJoinCareerSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodIdCriteria = $criteria;

		return $this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCourseSubjectConfigurations) {
				foreach ((array) $this->collCourseSubjectConfigurations as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentReincorporations) {
				foreach ((array) $this->collStudentReincorporations as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentFrees) {
				foreach ((array) $this->collStudentFrees as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentCareerSchoolYearConducts) {
				foreach ((array) $this->collStudentCareerSchoolYearConducts as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId) {
				foreach ((array) $this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCourseSubjectConfigurations = null;
		$this->collStudentReincorporations = null;
		$this->collStudentFrees = null;
		$this->collStudentCareerSchoolYearConducts = null;
		$this->collCareerSchoolYearPeriodsRelatedByCareerSchoolYearPeriodId = null;
			$this->aCareerSchoolYear = null;
			$this->aCareerSchoolYearPeriodRelatedByCareerSchoolYearPeriodId = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseCareerSchoolYearPeriod:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseCareerSchoolYearPeriod::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseCareerSchoolYearPeriod
