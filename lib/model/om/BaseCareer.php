<?php

/**
 * Base class that represents a row from the 'career' table.
 *
 * Representa una Carrera
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseCareer extends BaseObject  implements Persistent {


  const PEER = 'CareerPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        CareerPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the file_number_sequence field.
	 * Note: this column has a database default value of: '1'
	 * @var        string
	 */
	protected $file_number_sequence;

	/**
	 * The value for the plan_name field.
	 * @var        string
	 */
	protected $plan_name;

	/**
	 * The value for the quantity_years field.
	 * @var        int
	 */
	protected $quantity_years;

	/**
	 * The value for the career_name field.
	 * @var        string
	 */
	protected $career_name;

	/**
	 * @var        array CareerStudent[] Collection to store aggregation of CareerStudent objects.
	 */
	protected $collCareerStudents;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerStudents.
	 */
	private $lastCareerStudentCriteria = null;

	/**
	 * @var        array CareerSubject[] Collection to store aggregation of CareerSubject objects.
	 */
	protected $collCareerSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerSubjects.
	 */
	private $lastCareerSubjectCriteria = null;

	/**
	 * @var        array CareerSchoolYear[] Collection to store aggregation of CareerSchoolYear objects.
	 */
	protected $collCareerSchoolYears;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCareerSchoolYears.
	 */
	private $lastCareerSchoolYearCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseCareer object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->file_number_sequence = '1';
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [file_number_sequence] column value.
	 * Proximo numero de alumno en esta carrera
	 * @return     string
	 */
	public function getFileNumberSequence()
	{
		return $this->file_number_sequence;
	}

	/**
	 * Get the [plan_name] column value.
	 * Nombre del plan
	 * @return     string
	 */
	public function getPlanName()
	{
		return $this->plan_name;
	}

	/**
	 * Get the [quantity_years] column value.
	 * Cantidad de años de la carrera
	 * @return     int
	 */
	public function getQuantityYears()
	{
		return $this->quantity_years;
	}

	/**
	 * Get the [career_name] column value.
	 * Nombre de la carrera
	 * @return     string
	 */
	public function getCareerName()
	{
		return $this->career_name;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Career The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = CareerPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Career The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = CareerPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Set the value of [file_number_sequence] column.
	 * Proximo numero de alumno en esta carrera
	 * @param      string $v new value
	 * @return     Career The current object (for fluent API support)
	 */
	public function setFileNumberSequence($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->file_number_sequence !== $v || $v === '1') {
			$this->file_number_sequence = $v;
			$this->modifiedColumns[] = CareerPeer::FILE_NUMBER_SEQUENCE;
		}

		return $this;
	} // setFileNumberSequence()

	/**
	 * Set the value of [plan_name] column.
	 * Nombre del plan
	 * @param      string $v new value
	 * @return     Career The current object (for fluent API support)
	 */
	public function setPlanName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->plan_name !== $v) {
			$this->plan_name = $v;
			$this->modifiedColumns[] = CareerPeer::PLAN_NAME;
		}

		return $this;
	} // setPlanName()

	/**
	 * Set the value of [quantity_years] column.
	 * Cantidad de años de la carrera
	 * @param      int $v new value
	 * @return     Career The current object (for fluent API support)
	 */
	public function setQuantityYears($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->quantity_years !== $v) {
			$this->quantity_years = $v;
			$this->modifiedColumns[] = CareerPeer::QUANTITY_YEARS;
		}

		return $this;
	} // setQuantityYears()

	/**
	 * Set the value of [career_name] column.
	 * Nombre de la carrera
	 * @param      string $v new value
	 * @return     Career The current object (for fluent API support)
	 */
	public function setCareerName($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->career_name !== $v) {
			$this->career_name = $v;
			$this->modifiedColumns[] = CareerPeer::CAREER_NAME;
		}

		return $this;
	} // setCareerName()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(CareerPeer::FILE_NUMBER_SEQUENCE))) {
				return false;
			}

			if ($this->file_number_sequence !== '1') {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->created_at = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->file_number_sequence = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->plan_name = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->quantity_years = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->career_name = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 6; // 6 = CareerPeer::NUM_COLUMNS - CareerPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Career object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = CareerPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collCareerStudents = null;
			$this->lastCareerStudentCriteria = null;

			$this->collCareerSubjects = null;
			$this->lastCareerSubjectCriteria = null;

			$this->collCareerSchoolYears = null;
			$this->lastCareerSchoolYearCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareer:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			CareerPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseCareer:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseCareer:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(CareerPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(CareerPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseCareer:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			CareerPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			if ($this->isNew() ) {
				$this->modifiedColumns[] = CareerPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = CareerPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += CareerPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collCareerStudents !== null) {
				foreach ($this->collCareerStudents as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCareerSubjects !== null) {
				foreach ($this->collCareerSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCareerSchoolYears !== null) {
				foreach ($this->collCareerSchoolYears as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = CareerPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collCareerStudents !== null) {
					foreach ($this->collCareerStudents as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCareerSubjects !== null) {
					foreach ($this->collCareerSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCareerSchoolYears !== null) {
					foreach ($this->collCareerSchoolYears as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getCreatedAt();
				break;
			case 2:
				return $this->getFileNumberSequence();
				break;
			case 3:
				return $this->getPlanName();
				break;
			case 4:
				return $this->getQuantityYears();
				break;
			case 5:
				return $this->getCareerName();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = CareerPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getCreatedAt(),
			$keys[2] => $this->getFileNumberSequence(),
			$keys[3] => $this->getPlanName(),
			$keys[4] => $this->getQuantityYears(),
			$keys[5] => $this->getCareerName(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = CareerPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setCreatedAt($value);
				break;
			case 2:
				$this->setFileNumberSequence($value);
				break;
			case 3:
				$this->setPlanName($value);
				break;
			case 4:
				$this->setQuantityYears($value);
				break;
			case 5:
				$this->setCareerName($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = CareerPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCreatedAt($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setFileNumberSequence($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setPlanName($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setQuantityYears($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setCareerName($arr[$keys[5]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(CareerPeer::DATABASE_NAME);

		if ($this->isColumnModified(CareerPeer::ID)) $criteria->add(CareerPeer::ID, $this->id);
		if ($this->isColumnModified(CareerPeer::CREATED_AT)) $criteria->add(CareerPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(CareerPeer::FILE_NUMBER_SEQUENCE)) $criteria->add(CareerPeer::FILE_NUMBER_SEQUENCE, $this->file_number_sequence);
		if ($this->isColumnModified(CareerPeer::PLAN_NAME)) $criteria->add(CareerPeer::PLAN_NAME, $this->plan_name);
		if ($this->isColumnModified(CareerPeer::QUANTITY_YEARS)) $criteria->add(CareerPeer::QUANTITY_YEARS, $this->quantity_years);
		if ($this->isColumnModified(CareerPeer::CAREER_NAME)) $criteria->add(CareerPeer::CAREER_NAME, $this->career_name);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(CareerPeer::DATABASE_NAME);

		$criteria->add(CareerPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Career (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setFileNumberSequence($this->file_number_sequence);

		$copyObj->setPlanName($this->plan_name);

		$copyObj->setQuantityYears($this->quantity_years);

		$copyObj->setCareerName($this->career_name);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getCareerStudents() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerStudent($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCareerSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerSubject($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCareerSchoolYears() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addCareerSchoolYear($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Career Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     CareerPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new CareerPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collCareerStudents collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerStudents()
	 */
	public function clearCareerStudents()
	{
		$this->collCareerStudents = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerStudents collection (array).
	 *
	 * By default this just sets the collCareerStudents collection to an empty array (like clearcollCareerStudents());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerStudents()
	{
		$this->collCareerStudents = array();
	}

	/**
	 * Gets an array of CareerStudent objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Career has previously been saved, it will retrieve
	 * related CareerStudents from storage. If this Career is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerStudent[]
	 * @throws     PropelException
	 */
	public function getCareerStudents($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerStudents === null) {
			if ($this->isNew()) {
			   $this->collCareerStudents = array();
			} else {

				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				CareerStudentPeer::addSelectColumns($criteria);
				$this->collCareerStudents = CareerStudentPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				CareerStudentPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerStudentCriteria) || !$this->lastCareerStudentCriteria->equals($criteria)) {
					$this->collCareerStudents = CareerStudentPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerStudentCriteria = $criteria;
		return $this->collCareerStudents;
	}

	/**
	 * Returns the number of related CareerStudent objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerStudent objects.
	 * @throws     PropelException
	 */
	public function countCareerStudents(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerStudents === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				$count = CareerStudentPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				if (!isset($this->lastCareerStudentCriteria) || !$this->lastCareerStudentCriteria->equals($criteria)) {
					$count = CareerStudentPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerStudents);
				}
			} else {
				$count = count($this->collCareerStudents);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerStudent object to this object
	 * through the CareerStudent foreign key attribute.
	 *
	 * @param      CareerStudent $l CareerStudent
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerStudent(CareerStudent $l)
	{
		if ($this->collCareerStudents === null) {
			$this->initCareerStudents();
		}
		if (!in_array($l, $this->collCareerStudents, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerStudents, $l);
			$l->setCareer($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerStudents from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerStudentsJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerStudents === null) {
			if ($this->isNew()) {
				$this->collCareerStudents = array();
			} else {

				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				$this->collCareerStudents = CareerStudentPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerStudentCriteria) || !$this->lastCareerStudentCriteria->equals($criteria)) {
				$this->collCareerStudents = CareerStudentPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerStudentCriteria = $criteria;

		return $this->collCareerStudents;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerStudents from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerStudentsJoinOrientation($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerStudents === null) {
			if ($this->isNew()) {
				$this->collCareerStudents = array();
			} else {

				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				$this->collCareerStudents = CareerStudentPeer::doSelectJoinOrientation($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerStudentCriteria) || !$this->lastCareerStudentCriteria->equals($criteria)) {
				$this->collCareerStudents = CareerStudentPeer::doSelectJoinOrientation($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerStudentCriteria = $criteria;

		return $this->collCareerStudents;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerStudents from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerStudentsJoinSubOrientation($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerStudents === null) {
			if ($this->isNew()) {
				$this->collCareerStudents = array();
			} else {

				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				$this->collCareerStudents = CareerStudentPeer::doSelectJoinSubOrientation($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerStudentCriteria) || !$this->lastCareerStudentCriteria->equals($criteria)) {
				$this->collCareerStudents = CareerStudentPeer::doSelectJoinSubOrientation($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerStudentCriteria = $criteria;

		return $this->collCareerStudents;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerStudents from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerStudentsJoinSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerStudents === null) {
			if ($this->isNew()) {
				$this->collCareerStudents = array();
			} else {

				$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

				$this->collCareerStudents = CareerStudentPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerStudentPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerStudentCriteria) || !$this->lastCareerStudentCriteria->equals($criteria)) {
				$this->collCareerStudents = CareerStudentPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerStudentCriteria = $criteria;

		return $this->collCareerStudents;
	}

	/**
	 * Clears out the collCareerSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerSubjects()
	 */
	public function clearCareerSubjects()
	{
		$this->collCareerSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerSubjects collection (array).
	 *
	 * By default this just sets the collCareerSubjects collection to an empty array (like clearcollCareerSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerSubjects()
	{
		$this->collCareerSubjects = array();
	}

	/**
	 * Gets an array of CareerSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Career has previously been saved, it will retrieve
	 * related CareerSubjects from storage. If this Career is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerSubject[]
	 * @throws     PropelException
	 */
	public function getCareerSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjects === null) {
			if ($this->isNew()) {
			   $this->collCareerSubjects = array();
			} else {

				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				CareerSubjectPeer::addSelectColumns($criteria);
				$this->collCareerSubjects = CareerSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				CareerSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerSubjectCriteria) || !$this->lastCareerSubjectCriteria->equals($criteria)) {
					$this->collCareerSubjects = CareerSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerSubjectCriteria = $criteria;
		return $this->collCareerSubjects;
	}

	/**
	 * Returns the number of related CareerSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerSubject objects.
	 * @throws     PropelException
	 */
	public function countCareerSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				$count = CareerSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				if (!isset($this->lastCareerSubjectCriteria) || !$this->lastCareerSubjectCriteria->equals($criteria)) {
					$count = CareerSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerSubjects);
				}
			} else {
				$count = count($this->collCareerSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerSubject object to this object
	 * through the CareerSubject foreign key attribute.
	 *
	 * @param      CareerSubject $l CareerSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerSubject(CareerSubject $l)
	{
		if ($this->collCareerSubjects === null) {
			$this->initCareerSubjects();
		}
		if (!in_array($l, $this->collCareerSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerSubjects, $l);
			$l->setCareer($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerSubjectsJoinSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjects === null) {
			if ($this->isNew()) {
				$this->collCareerSubjects = array();
			} else {

				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				$this->collCareerSubjects = CareerSubjectPeer::doSelectJoinSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerSubjectCriteria) || !$this->lastCareerSubjectCriteria->equals($criteria)) {
				$this->collCareerSubjects = CareerSubjectPeer::doSelectJoinSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectCriteria = $criteria;

		return $this->collCareerSubjects;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerSubjectsJoinOrientation($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjects === null) {
			if ($this->isNew()) {
				$this->collCareerSubjects = array();
			} else {

				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				$this->collCareerSubjects = CareerSubjectPeer::doSelectJoinOrientation($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerSubjectCriteria) || !$this->lastCareerSubjectCriteria->equals($criteria)) {
				$this->collCareerSubjects = CareerSubjectPeer::doSelectJoinOrientation($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectCriteria = $criteria;

		return $this->collCareerSubjects;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerSubjectsJoinSubOrientation($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSubjects === null) {
			if ($this->isNew()) {
				$this->collCareerSubjects = array();
			} else {

				$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

				$this->collCareerSubjects = CareerSubjectPeer::doSelectJoinSubOrientation($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSubjectPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerSubjectCriteria) || !$this->lastCareerSubjectCriteria->equals($criteria)) {
				$this->collCareerSubjects = CareerSubjectPeer::doSelectJoinSubOrientation($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSubjectCriteria = $criteria;

		return $this->collCareerSubjects;
	}

	/**
	 * Clears out the collCareerSchoolYears collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCareerSchoolYears()
	 */
	public function clearCareerSchoolYears()
	{
		$this->collCareerSchoolYears = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCareerSchoolYears collection (array).
	 *
	 * By default this just sets the collCareerSchoolYears collection to an empty array (like clearcollCareerSchoolYears());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCareerSchoolYears()
	{
		$this->collCareerSchoolYears = array();
	}

	/**
	 * Gets an array of CareerSchoolYear objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Career has previously been saved, it will retrieve
	 * related CareerSchoolYears from storage. If this Career is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array CareerSchoolYear[]
	 * @throws     PropelException
	 */
	public function getCareerSchoolYears($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYears === null) {
			if ($this->isNew()) {
			   $this->collCareerSchoolYears = array();
			} else {

				$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

				CareerSchoolYearPeer::addSelectColumns($criteria);
				$this->collCareerSchoolYears = CareerSchoolYearPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

				CareerSchoolYearPeer::addSelectColumns($criteria);
				if (!isset($this->lastCareerSchoolYearCriteria) || !$this->lastCareerSchoolYearCriteria->equals($criteria)) {
					$this->collCareerSchoolYears = CareerSchoolYearPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCareerSchoolYearCriteria = $criteria;
		return $this->collCareerSchoolYears;
	}

	/**
	 * Returns the number of related CareerSchoolYear objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related CareerSchoolYear objects.
	 * @throws     PropelException
	 */
	public function countCareerSchoolYears(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCareerSchoolYears === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

				$count = CareerSchoolYearPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

				if (!isset($this->lastCareerSchoolYearCriteria) || !$this->lastCareerSchoolYearCriteria->equals($criteria)) {
					$count = CareerSchoolYearPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collCareerSchoolYears);
				}
			} else {
				$count = count($this->collCareerSchoolYears);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a CareerSchoolYear object to this object
	 * through the CareerSchoolYear foreign key attribute.
	 *
	 * @param      CareerSchoolYear $l CareerSchoolYear
	 * @return     void
	 * @throws     PropelException
	 */
	public function addCareerSchoolYear(CareerSchoolYear $l)
	{
		if ($this->collCareerSchoolYears === null) {
			$this->initCareerSchoolYears();
		}
		if (!in_array($l, $this->collCareerSchoolYears, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCareerSchoolYears, $l);
			$l->setCareer($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerSchoolYearsJoinSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYears === null) {
			if ($this->isNew()) {
				$this->collCareerSchoolYears = array();
			} else {

				$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

				$this->collCareerSchoolYears = CareerSchoolYearPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerSchoolYearCriteria) || !$this->lastCareerSchoolYearCriteria->equals($criteria)) {
				$this->collCareerSchoolYears = CareerSchoolYearPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSchoolYearCriteria = $criteria;

		return $this->collCareerSchoolYears;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Career is new, it will return
	 * an empty collection; or if this Career has previously
	 * been saved, it will retrieve related CareerSchoolYears from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Career.
	 */
	public function getCareerSchoolYearsJoinSubjectConfiguration($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(CareerPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCareerSchoolYears === null) {
			if ($this->isNew()) {
				$this->collCareerSchoolYears = array();
			} else {

				$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

				$this->collCareerSchoolYears = CareerSchoolYearPeer::doSelectJoinSubjectConfiguration($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CareerSchoolYearPeer::CAREER_ID, $this->id);

			if (!isset($this->lastCareerSchoolYearCriteria) || !$this->lastCareerSchoolYearCriteria->equals($criteria)) {
				$this->collCareerSchoolYears = CareerSchoolYearPeer::doSelectJoinSubjectConfiguration($criteria, $con, $join_behavior);
			}
		}
		$this->lastCareerSchoolYearCriteria = $criteria;

		return $this->collCareerSchoolYears;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collCareerStudents) {
				foreach ((array) $this->collCareerStudents as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCareerSubjects) {
				foreach ((array) $this->collCareerSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCareerSchoolYears) {
				foreach ((array) $this->collCareerSchoolYears as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collCareerStudents = null;
		$this->collCareerSubjects = null;
		$this->collCareerSchoolYears = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseCareer:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseCareer::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseCareer
