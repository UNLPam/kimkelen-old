<?php

/**
 * Base class that represents a row from the 'student_approved_career_subject' table.
 *
 * Tabla para almacenar que materias aprueban los alumnos. Una entrada en esta tabla indica que student_id aprobo career_subject_id.
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseStudentApprovedCareerSubject extends BaseObject  implements Persistent {


  const PEER = 'StudentApprovedCareerSubjectPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        StudentApprovedCareerSubjectPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * The value for the career_subject_id field.
	 * @var        int
	 */
	protected $career_subject_id;

	/**
	 * The value for the student_id field.
	 * @var        int
	 */
	protected $student_id;

	/**
	 * The value for the school_year_id field.
	 * @var        int
	 */
	protected $school_year_id;

	/**
	 * The value for the mark field.
	 * @var        string
	 */
	protected $mark;

	/**
	 * The value for the is_equivalence field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_equivalence;

	/**
	 * @var        CareerSubject
	 */
	protected $aCareerSubject;

	/**
	 * @var        Student
	 */
	protected $aStudent;

	/**
	 * @var        SchoolYear
	 */
	protected $aSchoolYear;

	/**
	 * @var        array StudentApprovedCourseSubject[] Collection to store aggregation of StudentApprovedCourseSubject objects.
	 */
	protected $collStudentApprovedCourseSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentApprovedCourseSubjects.
	 */
	private $lastStudentApprovedCourseSubjectCriteria = null;

	/**
	 * @var        array StudentDisapprovedCourseSubject[] Collection to store aggregation of StudentDisapprovedCourseSubject objects.
	 */
	protected $collStudentDisapprovedCourseSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentDisapprovedCourseSubjects.
	 */
	private $lastStudentDisapprovedCourseSubjectCriteria = null;

	/**
	 * @var        array StudentRepprovedCourseSubject[] Collection to store aggregation of StudentRepprovedCourseSubject objects.
	 */
	protected $collStudentRepprovedCourseSubjects;

	/**
	 * @var        Criteria The criteria used to select the current contents of collStudentRepprovedCourseSubjects.
	 */
	private $lastStudentRepprovedCourseSubjectCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Initializes internal state of BaseStudentApprovedCareerSubject object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_equivalence = false;
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [career_subject_id] column value.
	 * Referencia a la materia de una carrera
	 * @return     int
	 */
	public function getCareerSubjectId()
	{
		return $this->career_subject_id;
	}

	/**
	 * Get the [student_id] column value.
	 * Referencia al estudiante
	 * @return     int
	 */
	public function getStudentId()
	{
		return $this->student_id;
	}

	/**
	 * Get the [school_year_id] column value.
	 * Referencia al school_year en que se asento la nota
	 * @return     int
	 */
	public function getSchoolYearId()
	{
		return $this->school_year_id;
	}

	/**
	 * Get the [mark] column value.
	 * Nota que obtuvo el alumno
	 * @return     string
	 */
	public function getMark()
	{
		return $this->mark;
	}

	/**
	 * Get the [is_equivalence] column value.
	 * Si es verdadera, entonces esta nota se paso como equivalencia o de otra carrera o de la historia del alumno
	 * @return     boolean
	 */
	public function getIsEquivalence()
	{
		return $this->is_equivalence;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Set the value of [career_subject_id] column.
	 * Referencia a la materia de una carrera
	 * @param      int $v new value
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setCareerSubjectId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->career_subject_id !== $v) {
			$this->career_subject_id = $v;
			$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID;
		}

		if ($this->aCareerSubject !== null && $this->aCareerSubject->getId() !== $v) {
			$this->aCareerSubject = null;
		}

		return $this;
	} // setCareerSubjectId()

	/**
	 * Set the value of [student_id] column.
	 * Referencia al estudiante
	 * @param      int $v new value
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setStudentId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->student_id !== $v) {
			$this->student_id = $v;
			$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::STUDENT_ID;
		}

		if ($this->aStudent !== null && $this->aStudent->getId() !== $v) {
			$this->aStudent = null;
		}

		return $this;
	} // setStudentId()

	/**
	 * Set the value of [school_year_id] column.
	 * Referencia al school_year en que se asento la nota
	 * @param      int $v new value
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setSchoolYearId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->school_year_id !== $v) {
			$this->school_year_id = $v;
			$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::SCHOOL_YEAR_ID;
		}

		if ($this->aSchoolYear !== null && $this->aSchoolYear->getId() !== $v) {
			$this->aSchoolYear = null;
		}

		return $this;
	} // setSchoolYearId()

	/**
	 * Set the value of [mark] column.
	 * Nota que obtuvo el alumno
	 * @param      string $v new value
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setMark($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->mark !== $v) {
			$this->mark = $v;
			$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::MARK;
		}

		return $this;
	} // setMark()

	/**
	 * Set the value of [is_equivalence] column.
	 * Si es verdadera, entonces esta nota se paso como equivalencia o de otra carrera o de la historia del alumno
	 * @param      boolean $v new value
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 */
	public function setIsEquivalence($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_equivalence !== $v || $v === false) {
			$this->is_equivalence = $v;
			$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::IS_EQUIVALENCE;
		}

		return $this;
	} // setIsEquivalence()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			// First, ensure that we don't have any columns that have been modified which aren't default columns.
			if (array_diff($this->modifiedColumns, array(StudentApprovedCareerSubjectPeer::IS_EQUIVALENCE))) {
				return false;
			}

			if ($this->is_equivalence !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->created_at = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->updated_at = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->career_subject_id = ($row[$startcol + 3] !== null) ? (int) $row[$startcol + 3] : null;
			$this->student_id = ($row[$startcol + 4] !== null) ? (int) $row[$startcol + 4] : null;
			$this->school_year_id = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->mark = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->is_equivalence = ($row[$startcol + 7] !== null) ? (boolean) $row[$startcol + 7] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 8; // 8 = StudentApprovedCareerSubjectPeer::NUM_COLUMNS - StudentApprovedCareerSubjectPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating StudentApprovedCareerSubject object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aCareerSubject !== null && $this->career_subject_id !== $this->aCareerSubject->getId()) {
			$this->aCareerSubject = null;
		}
		if ($this->aStudent !== null && $this->student_id !== $this->aStudent->getId()) {
			$this->aStudent = null;
		}
		if ($this->aSchoolYear !== null && $this->school_year_id !== $this->aSchoolYear->getId()) {
			$this->aSchoolYear = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentApprovedCareerSubjectPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = StudentApprovedCareerSubjectPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aCareerSubject = null;
			$this->aStudent = null;
			$this->aSchoolYear = null;
			$this->collStudentApprovedCourseSubjects = null;
			$this->lastStudentApprovedCourseSubjectCriteria = null;

			$this->collStudentDisapprovedCourseSubjects = null;
			$this->lastStudentDisapprovedCourseSubjectCriteria = null;

			$this->collStudentRepprovedCourseSubjects = null;
			$this->lastStudentRepprovedCourseSubjectCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentApprovedCareerSubject:delete:pre') as $callable)
    {
      $ret = call_user_func($callable, $this, $con);
      if ($ret)
      {
        return;
      }
    }


		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentApprovedCareerSubjectPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			StudentApprovedCareerSubjectPeer::doDelete($this, $con);
			$this->setDeleted(true);
			$con->commit();
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	

    foreach (sfMixer::getCallables('BaseStudentApprovedCareerSubject:delete:post') as $callable)
    {
      call_user_func($callable, $this, $con);
    }

  }
	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentApprovedCareerSubject:save:pre') as $callable)
    {
      $affectedRows = call_user_func($callable, $this, $con);
      if (is_int($affectedRows))
      {
        return $affectedRows;
      }
    }


    if ($this->isNew() && !$this->isColumnModified(StudentApprovedCareerSubjectPeer::CREATED_AT))
    {
      $this->setCreatedAt(time());
    }

    if ($this->isModified() && !$this->isColumnModified(StudentApprovedCareerSubjectPeer::UPDATED_AT))
    {
      $this->setUpdatedAt(time());
    }

		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentApprovedCareerSubjectPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$affectedRows = $this->doSave($con);
			$con->commit();
    foreach (sfMixer::getCallables('BaseStudentApprovedCareerSubject:save:post') as $callable)
    {
      call_user_func($callable, $this, $con, $affectedRows);
    }

			StudentApprovedCareerSubjectPeer::addInstanceToPool($this);
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareerSubject !== null) {
				if ($this->aCareerSubject->isModified() || $this->aCareerSubject->isNew()) {
					$affectedRows += $this->aCareerSubject->save($con);
				}
				$this->setCareerSubject($this->aCareerSubject);
			}

			if ($this->aStudent !== null) {
				if ($this->aStudent->isModified() || $this->aStudent->isNew()) {
					$affectedRows += $this->aStudent->save($con);
				}
				$this->setStudent($this->aStudent);
			}

			if ($this->aSchoolYear !== null) {
				if ($this->aSchoolYear->isModified() || $this->aSchoolYear->isNew()) {
					$affectedRows += $this->aSchoolYear->save($con);
				}
				$this->setSchoolYear($this->aSchoolYear);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = StudentApprovedCareerSubjectPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = StudentApprovedCareerSubjectPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += StudentApprovedCareerSubjectPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collStudentApprovedCourseSubjects !== null) {
				foreach ($this->collStudentApprovedCourseSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentDisapprovedCourseSubjects !== null) {
				foreach ($this->collStudentDisapprovedCourseSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collStudentRepprovedCourseSubjects !== null) {
				foreach ($this->collStudentRepprovedCourseSubjects as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aCareerSubject !== null) {
				if (!$this->aCareerSubject->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aCareerSubject->getValidationFailures());
				}
			}

			if ($this->aStudent !== null) {
				if (!$this->aStudent->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aStudent->getValidationFailures());
				}
			}

			if ($this->aSchoolYear !== null) {
				if (!$this->aSchoolYear->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aSchoolYear->getValidationFailures());
				}
			}


			if (($retval = StudentApprovedCareerSubjectPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collStudentApprovedCourseSubjects !== null) {
					foreach ($this->collStudentApprovedCourseSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentDisapprovedCourseSubjects !== null) {
					foreach ($this->collStudentDisapprovedCourseSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collStudentRepprovedCourseSubjects !== null) {
					foreach ($this->collStudentRepprovedCourseSubjects as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = StudentApprovedCareerSubjectPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getCreatedAt();
				break;
			case 2:
				return $this->getUpdatedAt();
				break;
			case 3:
				return $this->getCareerSubjectId();
				break;
			case 4:
				return $this->getStudentId();
				break;
			case 5:
				return $this->getSchoolYearId();
				break;
			case 6:
				return $this->getMark();
				break;
			case 7:
				return $this->getIsEquivalence();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = StudentApprovedCareerSubjectPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getCreatedAt(),
			$keys[2] => $this->getUpdatedAt(),
			$keys[3] => $this->getCareerSubjectId(),
			$keys[4] => $this->getStudentId(),
			$keys[5] => $this->getSchoolYearId(),
			$keys[6] => $this->getMark(),
			$keys[7] => $this->getIsEquivalence(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = StudentApprovedCareerSubjectPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setCreatedAt($value);
				break;
			case 2:
				$this->setUpdatedAt($value);
				break;
			case 3:
				$this->setCareerSubjectId($value);
				break;
			case 4:
				$this->setStudentId($value);
				break;
			case 5:
				$this->setSchoolYearId($value);
				break;
			case 6:
				$this->setMark($value);
				break;
			case 7:
				$this->setIsEquivalence($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = StudentApprovedCareerSubjectPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setCreatedAt($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setUpdatedAt($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setCareerSubjectId($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setStudentId($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setSchoolYearId($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setMark($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setIsEquivalence($arr[$keys[7]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);

		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::ID)) $criteria->add(StudentApprovedCareerSubjectPeer::ID, $this->id);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::CREATED_AT)) $criteria->add(StudentApprovedCareerSubjectPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::UPDATED_AT)) $criteria->add(StudentApprovedCareerSubjectPeer::UPDATED_AT, $this->updated_at);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID)) $criteria->add(StudentApprovedCareerSubjectPeer::CAREER_SUBJECT_ID, $this->career_subject_id);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::STUDENT_ID)) $criteria->add(StudentApprovedCareerSubjectPeer::STUDENT_ID, $this->student_id);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::SCHOOL_YEAR_ID)) $criteria->add(StudentApprovedCareerSubjectPeer::SCHOOL_YEAR_ID, $this->school_year_id);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::MARK)) $criteria->add(StudentApprovedCareerSubjectPeer::MARK, $this->mark);
		if ($this->isColumnModified(StudentApprovedCareerSubjectPeer::IS_EQUIVALENCE)) $criteria->add(StudentApprovedCareerSubjectPeer::IS_EQUIVALENCE, $this->is_equivalence);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);

		$criteria->add(StudentApprovedCareerSubjectPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of StudentApprovedCareerSubject (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);

		$copyObj->setCareerSubjectId($this->career_subject_id);

		$copyObj->setStudentId($this->student_id);

		$copyObj->setSchoolYearId($this->school_year_id);

		$copyObj->setMark($this->mark);

		$copyObj->setIsEquivalence($this->is_equivalence);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getStudentApprovedCourseSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentApprovedCourseSubject($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentDisapprovedCourseSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentDisapprovedCourseSubject($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getStudentRepprovedCourseSubjects() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addStudentRepprovedCourseSubject($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     StudentApprovedCareerSubject Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     StudentApprovedCareerSubjectPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new StudentApprovedCareerSubjectPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a CareerSubject object.
	 *
	 * @param      CareerSubject $v
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setCareerSubject(CareerSubject $v = null)
	{
		if ($v === null) {
			$this->setCareerSubjectId(NULL);
		} else {
			$this->setCareerSubjectId($v->getId());
		}

		$this->aCareerSubject = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the CareerSubject object, it will not be re-added.
		if ($v !== null) {
			$v->addStudentApprovedCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated CareerSubject object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     CareerSubject The associated CareerSubject object.
	 * @throws     PropelException
	 */
	public function getCareerSubject(PropelPDO $con = null)
	{
		if ($this->aCareerSubject === null && ($this->career_subject_id !== null)) {
			$c = new Criteria(CareerSubjectPeer::DATABASE_NAME);
			$c->add(CareerSubjectPeer::ID, $this->career_subject_id);
			$this->aCareerSubject = CareerSubjectPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aCareerSubject->addStudentApprovedCareerSubjects($this);
			 */
		}
		return $this->aCareerSubject;
	}

	/**
	 * Declares an association between this object and a Student object.
	 *
	 * @param      Student $v
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setStudent(Student $v = null)
	{
		if ($v === null) {
			$this->setStudentId(NULL);
		} else {
			$this->setStudentId($v->getId());
		}

		$this->aStudent = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Student object, it will not be re-added.
		if ($v !== null) {
			$v->addStudentApprovedCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated Student object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Student The associated Student object.
	 * @throws     PropelException
	 */
	public function getStudent(PropelPDO $con = null)
	{
		if ($this->aStudent === null && ($this->student_id !== null)) {
			$c = new Criteria(StudentPeer::DATABASE_NAME);
			$c->add(StudentPeer::ID, $this->student_id);
			$this->aStudent = StudentPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aStudent->addStudentApprovedCareerSubjects($this);
			 */
		}
		return $this->aStudent;
	}

	/**
	 * Declares an association between this object and a SchoolYear object.
	 *
	 * @param      SchoolYear $v
	 * @return     StudentApprovedCareerSubject The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setSchoolYear(SchoolYear $v = null)
	{
		if ($v === null) {
			$this->setSchoolYearId(NULL);
		} else {
			$this->setSchoolYearId($v->getId());
		}

		$this->aSchoolYear = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the SchoolYear object, it will not be re-added.
		if ($v !== null) {
			$v->addStudentApprovedCareerSubject($this);
		}

		return $this;
	}


	/**
	 * Get the associated SchoolYear object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     SchoolYear The associated SchoolYear object.
	 * @throws     PropelException
	 */
	public function getSchoolYear(PropelPDO $con = null)
	{
		if ($this->aSchoolYear === null && ($this->school_year_id !== null)) {
			$c = new Criteria(SchoolYearPeer::DATABASE_NAME);
			$c->add(SchoolYearPeer::ID, $this->school_year_id);
			$this->aSchoolYear = SchoolYearPeer::doSelectOne($c, $con);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aSchoolYear->addStudentApprovedCareerSubjects($this);
			 */
		}
		return $this->aSchoolYear;
	}

	/**
	 * Clears out the collStudentApprovedCourseSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentApprovedCourseSubjects()
	 */
	public function clearStudentApprovedCourseSubjects()
	{
		$this->collStudentApprovedCourseSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentApprovedCourseSubjects collection (array).
	 *
	 * By default this just sets the collStudentApprovedCourseSubjects collection to an empty array (like clearcollStudentApprovedCourseSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentApprovedCourseSubjects()
	{
		$this->collStudentApprovedCourseSubjects = array();
	}

	/**
	 * Gets an array of StudentApprovedCourseSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject has previously been saved, it will retrieve
	 * related StudentApprovedCourseSubjects from storage. If this StudentApprovedCareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentApprovedCourseSubject[]
	 * @throws     PropelException
	 */
	public function getStudentApprovedCourseSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCourseSubjects === null) {
			if ($this->isNew()) {
			   $this->collStudentApprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				StudentApprovedCourseSubjectPeer::addSelectColumns($criteria);
				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				StudentApprovedCourseSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentApprovedCourseSubjectCriteria) || !$this->lastStudentApprovedCourseSubjectCriteria->equals($criteria)) {
					$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentApprovedCourseSubjectCriteria = $criteria;
		return $this->collStudentApprovedCourseSubjects;
	}

	/**
	 * Returns the number of related StudentApprovedCourseSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentApprovedCourseSubject objects.
	 * @throws     PropelException
	 */
	public function countStudentApprovedCourseSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentApprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$count = StudentApprovedCourseSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastStudentApprovedCourseSubjectCriteria) || !$this->lastStudentApprovedCourseSubjectCriteria->equals($criteria)) {
					$count = StudentApprovedCourseSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentApprovedCourseSubjects);
				}
			} else {
				$count = count($this->collStudentApprovedCourseSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentApprovedCourseSubject object to this object
	 * through the StudentApprovedCourseSubject foreign key attribute.
	 *
	 * @param      StudentApprovedCourseSubject $l StudentApprovedCourseSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentApprovedCourseSubject(StudentApprovedCourseSubject $l)
	{
		if ($this->collStudentApprovedCourseSubjects === null) {
			$this->initStudentApprovedCourseSubjects();
		}
		if (!in_array($l, $this->collStudentApprovedCourseSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentApprovedCourseSubjects, $l);
			$l->setStudentApprovedCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject is new, it will return
	 * an empty collection; or if this StudentApprovedCareerSubject has previously
	 * been saved, it will retrieve related StudentApprovedCourseSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentApprovedCareerSubject.
	 */
	public function getStudentApprovedCourseSubjectsJoinCourseSubject($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentApprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentApprovedCourseSubjectCriteria) || !$this->lastStudentApprovedCourseSubjectCriteria->equals($criteria)) {
				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelectJoinCourseSubject($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentApprovedCourseSubjectCriteria = $criteria;

		return $this->collStudentApprovedCourseSubjects;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject is new, it will return
	 * an empty collection; or if this StudentApprovedCareerSubject has previously
	 * been saved, it will retrieve related StudentApprovedCourseSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentApprovedCareerSubject.
	 */
	public function getStudentApprovedCourseSubjectsJoinStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentApprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentApprovedCourseSubjectCriteria) || !$this->lastStudentApprovedCourseSubjectCriteria->equals($criteria)) {
				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelectJoinStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentApprovedCourseSubjectCriteria = $criteria;

		return $this->collStudentApprovedCourseSubjects;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject is new, it will return
	 * an empty collection; or if this StudentApprovedCareerSubject has previously
	 * been saved, it will retrieve related StudentApprovedCourseSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentApprovedCareerSubject.
	 */
	public function getStudentApprovedCourseSubjectsJoinSchoolYear($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentApprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentApprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentApprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentApprovedCourseSubjectCriteria) || !$this->lastStudentApprovedCourseSubjectCriteria->equals($criteria)) {
				$this->collStudentApprovedCourseSubjects = StudentApprovedCourseSubjectPeer::doSelectJoinSchoolYear($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentApprovedCourseSubjectCriteria = $criteria;

		return $this->collStudentApprovedCourseSubjects;
	}

	/**
	 * Clears out the collStudentDisapprovedCourseSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentDisapprovedCourseSubjects()
	 */
	public function clearStudentDisapprovedCourseSubjects()
	{
		$this->collStudentDisapprovedCourseSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentDisapprovedCourseSubjects collection (array).
	 *
	 * By default this just sets the collStudentDisapprovedCourseSubjects collection to an empty array (like clearcollStudentDisapprovedCourseSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentDisapprovedCourseSubjects()
	{
		$this->collStudentDisapprovedCourseSubjects = array();
	}

	/**
	 * Gets an array of StudentDisapprovedCourseSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject has previously been saved, it will retrieve
	 * related StudentDisapprovedCourseSubjects from storage. If this StudentApprovedCareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentDisapprovedCourseSubject[]
	 * @throws     PropelException
	 */
	public function getStudentDisapprovedCourseSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentDisapprovedCourseSubjects === null) {
			if ($this->isNew()) {
			   $this->collStudentDisapprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentDisapprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				StudentDisapprovedCourseSubjectPeer::addSelectColumns($criteria);
				$this->collStudentDisapprovedCourseSubjects = StudentDisapprovedCourseSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentDisapprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				StudentDisapprovedCourseSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentDisapprovedCourseSubjectCriteria) || !$this->lastStudentDisapprovedCourseSubjectCriteria->equals($criteria)) {
					$this->collStudentDisapprovedCourseSubjects = StudentDisapprovedCourseSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentDisapprovedCourseSubjectCriteria = $criteria;
		return $this->collStudentDisapprovedCourseSubjects;
	}

	/**
	 * Returns the number of related StudentDisapprovedCourseSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentDisapprovedCourseSubject objects.
	 * @throws     PropelException
	 */
	public function countStudentDisapprovedCourseSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentDisapprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentDisapprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$count = StudentDisapprovedCourseSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentDisapprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastStudentDisapprovedCourseSubjectCriteria) || !$this->lastStudentDisapprovedCourseSubjectCriteria->equals($criteria)) {
					$count = StudentDisapprovedCourseSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentDisapprovedCourseSubjects);
				}
			} else {
				$count = count($this->collStudentDisapprovedCourseSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentDisapprovedCourseSubject object to this object
	 * through the StudentDisapprovedCourseSubject foreign key attribute.
	 *
	 * @param      StudentDisapprovedCourseSubject $l StudentDisapprovedCourseSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentDisapprovedCourseSubject(StudentDisapprovedCourseSubject $l)
	{
		if ($this->collStudentDisapprovedCourseSubjects === null) {
			$this->initStudentDisapprovedCourseSubjects();
		}
		if (!in_array($l, $this->collStudentDisapprovedCourseSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentDisapprovedCourseSubjects, $l);
			$l->setStudentApprovedCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject is new, it will return
	 * an empty collection; or if this StudentApprovedCareerSubject has previously
	 * been saved, it will retrieve related StudentDisapprovedCourseSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentApprovedCareerSubject.
	 */
	public function getStudentDisapprovedCourseSubjectsJoinCourseSubjectStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentDisapprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentDisapprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentDisapprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$this->collStudentDisapprovedCourseSubjects = StudentDisapprovedCourseSubjectPeer::doSelectJoinCourseSubjectStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentDisapprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentDisapprovedCourseSubjectCriteria) || !$this->lastStudentDisapprovedCourseSubjectCriteria->equals($criteria)) {
				$this->collStudentDisapprovedCourseSubjects = StudentDisapprovedCourseSubjectPeer::doSelectJoinCourseSubjectStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentDisapprovedCourseSubjectCriteria = $criteria;

		return $this->collStudentDisapprovedCourseSubjects;
	}

	/**
	 * Clears out the collStudentRepprovedCourseSubjects collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addStudentRepprovedCourseSubjects()
	 */
	public function clearStudentRepprovedCourseSubjects()
	{
		$this->collStudentRepprovedCourseSubjects = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collStudentRepprovedCourseSubjects collection (array).
	 *
	 * By default this just sets the collStudentRepprovedCourseSubjects collection to an empty array (like clearcollStudentRepprovedCourseSubjects());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initStudentRepprovedCourseSubjects()
	{
		$this->collStudentRepprovedCourseSubjects = array();
	}

	/**
	 * Gets an array of StudentRepprovedCourseSubject objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject has previously been saved, it will retrieve
	 * related StudentRepprovedCourseSubjects from storage. If this StudentApprovedCareerSubject is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array StudentRepprovedCourseSubject[]
	 * @throws     PropelException
	 */
	public function getStudentRepprovedCourseSubjects($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentRepprovedCourseSubjects === null) {
			if ($this->isNew()) {
			   $this->collStudentRepprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentRepprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				StudentRepprovedCourseSubjectPeer::addSelectColumns($criteria);
				$this->collStudentRepprovedCourseSubjects = StudentRepprovedCourseSubjectPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(StudentRepprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				StudentRepprovedCourseSubjectPeer::addSelectColumns($criteria);
				if (!isset($this->lastStudentRepprovedCourseSubjectCriteria) || !$this->lastStudentRepprovedCourseSubjectCriteria->equals($criteria)) {
					$this->collStudentRepprovedCourseSubjects = StudentRepprovedCourseSubjectPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastStudentRepprovedCourseSubjectCriteria = $criteria;
		return $this->collStudentRepprovedCourseSubjects;
	}

	/**
	 * Returns the number of related StudentRepprovedCourseSubject objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related StudentRepprovedCourseSubject objects.
	 * @throws     PropelException
	 */
	public function countStudentRepprovedCourseSubjects(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collStudentRepprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(StudentRepprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$count = StudentRepprovedCourseSubjectPeer::doCount($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(StudentRepprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				if (!isset($this->lastStudentRepprovedCourseSubjectCriteria) || !$this->lastStudentRepprovedCourseSubjectCriteria->equals($criteria)) {
					$count = StudentRepprovedCourseSubjectPeer::doCount($criteria, $con);
				} else {
					$count = count($this->collStudentRepprovedCourseSubjects);
				}
			} else {
				$count = count($this->collStudentRepprovedCourseSubjects);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a StudentRepprovedCourseSubject object to this object
	 * through the StudentRepprovedCourseSubject foreign key attribute.
	 *
	 * @param      StudentRepprovedCourseSubject $l StudentRepprovedCourseSubject
	 * @return     void
	 * @throws     PropelException
	 */
	public function addStudentRepprovedCourseSubject(StudentRepprovedCourseSubject $l)
	{
		if ($this->collStudentRepprovedCourseSubjects === null) {
			$this->initStudentRepprovedCourseSubjects();
		}
		if (!in_array($l, $this->collStudentRepprovedCourseSubjects, true)) { // only add it if the **same** object is not already associated
			array_push($this->collStudentRepprovedCourseSubjects, $l);
			$l->setStudentApprovedCareerSubject($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this StudentApprovedCareerSubject is new, it will return
	 * an empty collection; or if this StudentApprovedCareerSubject has previously
	 * been saved, it will retrieve related StudentRepprovedCourseSubjects from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in StudentApprovedCareerSubject.
	 */
	public function getStudentRepprovedCourseSubjectsJoinCourseSubjectStudent($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(StudentApprovedCareerSubjectPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collStudentRepprovedCourseSubjects === null) {
			if ($this->isNew()) {
				$this->collStudentRepprovedCourseSubjects = array();
			} else {

				$criteria->add(StudentRepprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

				$this->collStudentRepprovedCourseSubjects = StudentRepprovedCourseSubjectPeer::doSelectJoinCourseSubjectStudent($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(StudentRepprovedCourseSubjectPeer::STUDENT_APPROVED_CAREER_SUBJECT_ID, $this->id);

			if (!isset($this->lastStudentRepprovedCourseSubjectCriteria) || !$this->lastStudentRepprovedCourseSubjectCriteria->equals($criteria)) {
				$this->collStudentRepprovedCourseSubjects = StudentRepprovedCourseSubjectPeer::doSelectJoinCourseSubjectStudent($criteria, $con, $join_behavior);
			}
		}
		$this->lastStudentRepprovedCourseSubjectCriteria = $criteria;

		return $this->collStudentRepprovedCourseSubjects;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collStudentApprovedCourseSubjects) {
				foreach ((array) $this->collStudentApprovedCourseSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentDisapprovedCourseSubjects) {
				foreach ((array) $this->collStudentDisapprovedCourseSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collStudentRepprovedCourseSubjects) {
				foreach ((array) $this->collStudentRepprovedCourseSubjects as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collStudentApprovedCourseSubjects = null;
		$this->collStudentDisapprovedCourseSubjects = null;
		$this->collStudentRepprovedCourseSubjects = null;
			$this->aCareerSubject = null;
			$this->aStudent = null;
			$this->aSchoolYear = null;
	}


  public function __call($method, $arguments)
  {
    if (!$callable = sfMixer::getCallable('BaseStudentApprovedCareerSubject:'.$method))
    {
      throw new sfException(sprintf('Call to undefined method BaseStudentApprovedCareerSubject::%s', $method));
    }

    array_unshift($arguments, $this);

    return call_user_func_array($callable, $arguments);
  }


} // BaseStudentApprovedCareerSubject
