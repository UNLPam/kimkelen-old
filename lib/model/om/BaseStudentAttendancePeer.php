<?php

/**
 * Base static class for performing query and update operations on the 'student_attendance' table.
 *
 * Representa las asistencias de un alumno en un dia/materia
 *
 * This class was autogenerated by Propel 1.3.0-dev on:
 *
 * Thu Aug 23 21:09:09 2018
 *
 * @package    lib.model.om
 */
abstract class BaseStudentAttendancePeer {

	/** the default database name for this class */
	const DATABASE_NAME = 'propel';

	/** the table name for this class */
	const TABLE_NAME = 'student_attendance';

	/** A class that can be returned by this peer. */
	const CLASS_DEFAULT = 'lib.model.StudentAttendance';

	/** The total number of columns. */
	const NUM_COLUMNS = 8;

	/** The number of lazy-loaded columns. */
	const NUM_LAZY_LOAD_COLUMNS = 0;

	/** the column name for the CAREER_SCHOOL_YEAR_ID field */
	const CAREER_SCHOOL_YEAR_ID = 'student_attendance.CAREER_SCHOOL_YEAR_ID';

	/** the column name for the STUDENT_ID field */
	const STUDENT_ID = 'student_attendance.STUDENT_ID';

	/** the column name for the DAY field */
	const DAY = 'student_attendance.DAY';

	/** the column name for the ABSENCE_TYPE_ID field */
	const ABSENCE_TYPE_ID = 'student_attendance.ABSENCE_TYPE_ID';

	/** the column name for the VALUE field */
	const VALUE = 'student_attendance.VALUE';

	/** the column name for the COURSE_SUBJECT_ID field */
	const COURSE_SUBJECT_ID = 'student_attendance.COURSE_SUBJECT_ID';

	/** the column name for the STUDENT_ATTENDANCE_JUSTIFICATION_ID field */
	const STUDENT_ATTENDANCE_JUSTIFICATION_ID = 'student_attendance.STUDENT_ATTENDANCE_JUSTIFICATION_ID';

	/** the column name for the ID field */
	const ID = 'student_attendance.ID';

	/**
	 * An identiy map to hold any loaded instances of StudentAttendance objects.
	 * This must be public so that other peer classes can access this when hydrating from JOIN
	 * queries.
	 * @var        array StudentAttendance[]
	 */
	public static $instances = array();

	/**
	 * The MapBuilder instance for this peer.
	 * @var        MapBuilder
	 */
	private static $mapBuilder = null;

	/**
	 * holds an array of fieldnames
	 *
	 * first dimension keys are the type constants
	 * e.g. self::$fieldNames[self::TYPE_PHPNAME][0] = 'Id'
	 */
	private static $fieldNames = array (
		BasePeer::TYPE_PHPNAME => array ('CareerSchoolYearId', 'StudentId', 'Day', 'AbsenceTypeId', 'Value', 'CourseSubjectId', 'StudentAttendanceJustificationId', 'Id', ),
		BasePeer::TYPE_STUDLYPHPNAME => array ('careerSchoolYearId', 'studentId', 'day', 'absenceTypeId', 'value', 'courseSubjectId', 'studentAttendanceJustificationId', 'id', ),
		BasePeer::TYPE_COLNAME => array (self::CAREER_SCHOOL_YEAR_ID, self::STUDENT_ID, self::DAY, self::ABSENCE_TYPE_ID, self::VALUE, self::COURSE_SUBJECT_ID, self::STUDENT_ATTENDANCE_JUSTIFICATION_ID, self::ID, ),
		BasePeer::TYPE_FIELDNAME => array ('career_school_year_id', 'student_id', 'day', 'absence_type_id', 'value', 'course_subject_id', 'student_attendance_justification_id', 'id', ),
		BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, 7, )
	);

	/**
	 * holds an array of keys for quick access to the fieldnames array
	 *
	 * first dimension keys are the type constants
	 * e.g. self::$fieldNames[BasePeer::TYPE_PHPNAME]['Id'] = 0
	 */
	private static $fieldKeys = array (
		BasePeer::TYPE_PHPNAME => array ('CareerSchoolYearId' => 0, 'StudentId' => 1, 'Day' => 2, 'AbsenceTypeId' => 3, 'Value' => 4, 'CourseSubjectId' => 5, 'StudentAttendanceJustificationId' => 6, 'Id' => 7, ),
		BasePeer::TYPE_STUDLYPHPNAME => array ('careerSchoolYearId' => 0, 'studentId' => 1, 'day' => 2, 'absenceTypeId' => 3, 'value' => 4, 'courseSubjectId' => 5, 'studentAttendanceJustificationId' => 6, 'id' => 7, ),
		BasePeer::TYPE_COLNAME => array (self::CAREER_SCHOOL_YEAR_ID => 0, self::STUDENT_ID => 1, self::DAY => 2, self::ABSENCE_TYPE_ID => 3, self::VALUE => 4, self::COURSE_SUBJECT_ID => 5, self::STUDENT_ATTENDANCE_JUSTIFICATION_ID => 6, self::ID => 7, ),
		BasePeer::TYPE_FIELDNAME => array ('career_school_year_id' => 0, 'student_id' => 1, 'day' => 2, 'absence_type_id' => 3, 'value' => 4, 'course_subject_id' => 5, 'student_attendance_justification_id' => 6, 'id' => 7, ),
		BasePeer::TYPE_NUM => array (0, 1, 2, 3, 4, 5, 6, 7, )
	);

	/**
	 * Get a (singleton) instance of the MapBuilder for this peer class.
	 * @return     MapBuilder The map builder for this peer
	 */
	public static function getMapBuilder()
	{
		if (self::$mapBuilder === null) {
			self::$mapBuilder = new StudentAttendanceMapBuilder();
		}
		return self::$mapBuilder;
	}
	/**
	 * Translates a fieldname to another type
	 *
	 * @param      string $name field name
	 * @param      string $fromType One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                         BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @param      string $toType   One of the class type constants
	 * @return     string translated name of the field.
	 * @throws     PropelException - if the specified name could not be found in the fieldname mappings.
	 */
	static public function translateFieldName($name, $fromType, $toType)
	{
		$toNames = self::getFieldNames($toType);
		$key = isset(self::$fieldKeys[$fromType][$name]) ? self::$fieldKeys[$fromType][$name] : null;
		if ($key === null) {
			throw new PropelException("'$name' could not be found in the field names of type '$fromType'. These are: " . print_r(self::$fieldKeys[$fromType], true));
		}
		return $toNames[$key];
	}

	/**
	 * Returns an array of field names.
	 *
	 * @param      string $type The type of fieldnames to return:
	 *                      One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                      BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     array A list of field names
	 */

	static public function getFieldNames($type = BasePeer::TYPE_PHPNAME)
	{
		if (!array_key_exists($type, self::$fieldNames)) {
			throw new PropelException('Method getFieldNames() expects the parameter $type to be one of the class constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME, BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. ' . $type . ' was given.');
		}
		return self::$fieldNames[$type];
	}

	/**
	 * Convenience method which changes table.column to alias.column.
	 *
	 * Using this method you can maintain SQL abstraction while using column aliases.
	 * <code>
	 *		$c->addAlias("alias1", TablePeer::TABLE_NAME);
	 *		$c->addJoin(TablePeer::alias("alias1", TablePeer::PRIMARY_KEY_COLUMN), TablePeer::PRIMARY_KEY_COLUMN);
	 * </code>
	 * @param      string $alias The alias for the current table.
	 * @param      string $column The column name for current table. (i.e. StudentAttendancePeer::COLUMN_NAME).
	 * @return     string
	 */
	public static function alias($alias, $column)
	{
		return str_replace(StudentAttendancePeer::TABLE_NAME.'.', $alias.'.', $column);
	}

	/**
	 * Add all the columns needed to create a new object.
	 *
	 * Note: any columns that were marked with lazyLoad="true" in the
	 * XML schema will not be added to the select list and only loaded
	 * on demand.
	 *
	 * @param      criteria object containing the columns to add.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function addSelectColumns(Criteria $criteria)
	{

		$criteria->addSelectColumn(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID);

		$criteria->addSelectColumn(StudentAttendancePeer::STUDENT_ID);

		$criteria->addSelectColumn(StudentAttendancePeer::DAY);

		$criteria->addSelectColumn(StudentAttendancePeer::ABSENCE_TYPE_ID);

		$criteria->addSelectColumn(StudentAttendancePeer::VALUE);

		$criteria->addSelectColumn(StudentAttendancePeer::COURSE_SUBJECT_ID);

		$criteria->addSelectColumn(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID);

		$criteria->addSelectColumn(StudentAttendancePeer::ID);

	}

	/**
	 * Returns the number of rows matching criteria.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @return     int Number of matching rows.
	 */
	public static function doCount(Criteria $criteria, $distinct = false, PropelPDO $con = null)
	{
		// we may modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count
		$criteria->setDbName(self::DATABASE_NAME); // Set the correct dbName

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}


    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		// BasePeer returns a PDOStatement
		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}
	/**
	 * Method to select one object from the DB.
	 *
	 * @param      Criteria $criteria object used to create the SELECT statement.
	 * @param      PropelPDO $con
	 * @return     StudentAttendance
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectOne(Criteria $criteria, PropelPDO $con = null)
	{
		$critcopy = clone $criteria;
		$critcopy->setLimit(1);
		$objects = StudentAttendancePeer::doSelect($critcopy, $con);
		if ($objects) {
			return $objects[0];
		}
		return null;
	}
	/**
	 * Method to do selects.
	 *
	 * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
	 * @param      PropelPDO $con
	 * @return     array Array of selected Objects
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelect(Criteria $criteria, PropelPDO $con = null)
	{
		return StudentAttendancePeer::populateObjects(StudentAttendancePeer::doSelectStmt($criteria, $con));
	}
	/**
	 * Prepares the Criteria object and uses the parent doSelect() method to execute a PDOStatement.
	 *
	 * Use this method directly if you want to work with an executed statement durirectly (for example
	 * to perform your own object hydration).
	 *
	 * @param      Criteria $criteria The Criteria object used to build the SELECT statement.
	 * @param      PropelPDO $con The connection to use
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 * @return     PDOStatement The executed PDOStatement object.
	 * @see        BasePeer::doSelect()
	 */
	public static function doSelectStmt(Criteria $criteria, PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doSelectStmt:doSelectStmt') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		if (!$criteria->hasSelectClause()) {
			$criteria = clone $criteria;
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		// BasePeer returns a PDOStatement
		return BasePeer::doSelect($criteria, $con);
	}
	/**
	 * Adds an object to the instance pool.
	 *
	 * Propel keeps cached copies of objects in an instance pool when they are retrieved
	 * from the database.  In some cases -- especially when you override doSelect*()
	 * methods in your stub classes -- you may need to explicitly add objects
	 * to the cache in order to ensure that the same objects are always returned by doSelect*()
	 * and retrieveByPK*() calls.
	 *
	 * @param      StudentAttendance $value A StudentAttendance object.
	 * @param      string $key (optional) key to use for instance map (for performance boost if key was already calculated externally).
	 */
	public static function addInstanceToPool(StudentAttendance $obj, $key = null)
	{
		if (Propel::isInstancePoolingEnabled()) {
			if ($key === null) {
				$key = (string) $obj->getId();
			} // if key === null
			self::$instances[$key] = $obj;
		}
	}

	/**
	 * Removes an object from the instance pool.
	 *
	 * Propel keeps cached copies of objects in an instance pool when they are retrieved
	 * from the database.  In some cases -- especially when you override doDelete
	 * methods in your stub classes -- you may need to explicitly remove objects
	 * from the cache in order to prevent returning objects that no longer exist.
	 *
	 * @param      mixed $value A StudentAttendance object or a primary key value.
	 */
	public static function removeInstanceFromPool($value)
	{
		if (Propel::isInstancePoolingEnabled() && $value !== null) {
			if (is_object($value) && $value instanceof StudentAttendance) {
				$key = (string) $value->getId();
			} elseif (is_scalar($value)) {
				// assume we've been passed a primary key
				$key = (string) $value;
			} else {
				$e = new PropelException("Invalid value passed to removeInstanceFromPool().  Expected primary key or StudentAttendance object; got " . (is_object($value) ? get_class($value) . ' object.' : var_export($value,true)));
				throw $e;
			}

			unset(self::$instances[$key]);
		}
	} // removeInstanceFromPool()

	/**
	 * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
	 *
	 * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
	 * a multi-column primary key, a serialize()d version of the primary key will be returned.
	 *
	 * @param      string $key The key (@see getPrimaryKeyHash()) for this instance.
	 * @return     StudentAttendance Found object or NULL if 1) no instance exists for specified key or 2) instance pooling has been disabled.
	 * @see        getPrimaryKeyHash()
	 */
	public static function getInstanceFromPool($key)
	{
		if (Propel::isInstancePoolingEnabled()) {
			if (isset(self::$instances[$key])) {
				return self::$instances[$key];
			}
		}
		return null; // just to be explicit
	}
	
	/**
	 * Clear the instance pool.
	 *
	 * @return     void
	 */
	public static function clearInstancePool()
	{
		self::$instances = array();
	}
	
	/**
	 * Retrieves a string version of the primary key from the DB resultset row that can be used to uniquely identify a row in this table.
	 *
	 * For tables with a single-column primary key, that simple pkey value will be returned.  For tables with
	 * a multi-column primary key, a serialize()d version of the primary key will be returned.
	 *
	 * @param      array $row PropelPDO resultset row.
	 * @param      int $startcol The 0-based offset for reading from the resultset row.
	 * @return     string A string version of PK or NULL if the components of primary key in result array are all null.
	 */
	public static function getPrimaryKeyHashFromRow($row, $startcol = 0)
	{
		// If the PK cannot be derived from the row, return NULL.
		if ($row[$startcol + 7] === null) {
			return null;
		}
		return (string) $row[$startcol + 7];
	}

	/**
	 * The returned array will contain objects of the default type or
	 * objects that inherit from the default.
	 *
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function populateObjects(PDOStatement $stmt)
	{
		$results = array();
	
		// set the class once to avoid overhead in the loop
		$cls = StudentAttendancePeer::getOMClass();
		$cls = substr('.'.$cls, strrpos('.'.$cls, '.') + 1);
		// populate the object(s)
		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj = StudentAttendancePeer::getInstanceFromPool($key))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj->hydrate($row, 0, true); // rehydrate
				$results[] = $obj;
			} else {
		
				$obj = new $cls();
				$obj->hydrate($row);
				$results[] = $obj;
				StudentAttendancePeer::addInstanceToPool($obj, $key);
			} // if key exists
		}
		$stmt->closeCursor();
		return $results;
	}

	/**
	 * Returns the number of rows matching criteria, joining the related CareerSchoolYear table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinCareerSchoolYear(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);


    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Student table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinStudent(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);


    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related AbsenceType table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAbsenceType(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);


    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related CourseSubject table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinCourseSubject(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);


    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related StudentAttendanceJustification table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinStudentAttendanceJustification(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);


    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with their CareerSchoolYear objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinCareerSchoolYear(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doSelectJoin:doSelectJoin') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $c, $con);
    }


		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);
		CareerSchoolYearPeer::addSelectColumns($c);

		$c->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = CareerSchoolYearPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = CareerSchoolYearPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = CareerSchoolYearPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					CareerSchoolYearPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (StudentAttendance) to $obj2 (CareerSchoolYear)
				$obj2->addStudentAttendance($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with their Student objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinStudent(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);
		StudentPeer::addSelectColumns($c);

		$c->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = StudentPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = StudentPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = StudentPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					StudentPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (StudentAttendance) to $obj2 (Student)
				$obj2->addStudentAttendance($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with their AbsenceType objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAbsenceType(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);
		AbsenceTypePeer::addSelectColumns($c);

		$c->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = AbsenceTypePeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = AbsenceTypePeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = AbsenceTypePeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					AbsenceTypePeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (StudentAttendance) to $obj2 (AbsenceType)
				$obj2->addStudentAttendance($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with their CourseSubject objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinCourseSubject(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);
		CourseSubjectPeer::addSelectColumns($c);

		$c->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = CourseSubjectPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = CourseSubjectPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = CourseSubjectPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					CourseSubjectPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (StudentAttendance) to $obj2 (CourseSubject)
				$obj2->addStudentAttendance($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with their StudentAttendanceJustification objects.
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinStudentAttendanceJustification(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);
		StudentAttendanceJustificationPeer::addSelectColumns($c);

		$c->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {

				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if $obj1 already loaded

			$key2 = StudentAttendanceJustificationPeer::getPrimaryKeyHashFromRow($row, $startcol);
			if ($key2 !== null) {
				$obj2 = StudentAttendanceJustificationPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = StudentAttendanceJustificationPeer::getOMClass();

					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol);
					StudentAttendanceJustificationPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 already loaded

				// Add the $obj1 (StudentAttendance) to $obj2 (StudentAttendanceJustification)
				$obj2->addStudentAttendance($obj1);

			} // if joined row was not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Returns the number of rows matching criteria, joining all related tables
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAll(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		// We need to set the primary table name, since in the case that there are no WHERE columns
		// it will be impossible for the BasePeer::createSelectSql() method to determine which
		// tables go into the FROM clause.
		$criteria->setPrimaryTableName(StudentAttendancePeer::TABLE_NAME);

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
		$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
		$criteria->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
		$criteria->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
		$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}

	/**
	 * Selects a collection of StudentAttendance objects pre-filled with all related objects.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAll(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doSelectJoinAll:doSelectJoinAll') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $c, $con);
    }


		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol2 = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);

		CareerSchoolYearPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (CareerSchoolYearPeer::NUM_COLUMNS - CareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (StudentPeer::NUM_COLUMNS - StudentPeer::NUM_LAZY_LOAD_COLUMNS);

		AbsenceTypePeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (AbsenceTypePeer::NUM_COLUMNS - AbsenceTypePeer::NUM_LAZY_LOAD_COLUMNS);

		CourseSubjectPeer::addSelectColumns($c);
		$startcol6 = $startcol5 + (CourseSubjectPeer::NUM_COLUMNS - CourseSubjectPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentAttendanceJustificationPeer::addSelectColumns($c);
		$startcol7 = $startcol6 + (StudentAttendanceJustificationPeer::NUM_COLUMNS - StudentAttendanceJustificationPeer::NUM_LAZY_LOAD_COLUMNS);

		$c->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
		$c->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
		$c->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
		$c->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
		$c->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);
		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

			// Add objects for joined CareerSchoolYear rows

			$key2 = CareerSchoolYearPeer::getPrimaryKeyHashFromRow($row, $startcol2);
			if ($key2 !== null) {
				$obj2 = CareerSchoolYearPeer::getInstanceFromPool($key2);
				if (!$obj2) {

					$omClass = CareerSchoolYearPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					CareerSchoolYearPeer::addInstanceToPool($obj2, $key2);
				} // if obj2 loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj2 (CareerSchoolYear)
				$obj2->addStudentAttendance($obj1);
			} // if joined row not null

			// Add objects for joined Student rows

			$key3 = StudentPeer::getPrimaryKeyHashFromRow($row, $startcol3);
			if ($key3 !== null) {
				$obj3 = StudentPeer::getInstanceFromPool($key3);
				if (!$obj3) {

					$omClass = StudentPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					StudentPeer::addInstanceToPool($obj3, $key3);
				} // if obj3 loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj3 (Student)
				$obj3->addStudentAttendance($obj1);
			} // if joined row not null

			// Add objects for joined AbsenceType rows

			$key4 = AbsenceTypePeer::getPrimaryKeyHashFromRow($row, $startcol4);
			if ($key4 !== null) {
				$obj4 = AbsenceTypePeer::getInstanceFromPool($key4);
				if (!$obj4) {

					$omClass = AbsenceTypePeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					AbsenceTypePeer::addInstanceToPool($obj4, $key4);
				} // if obj4 loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj4 (AbsenceType)
				$obj4->addStudentAttendance($obj1);
			} // if joined row not null

			// Add objects for joined CourseSubject rows

			$key5 = CourseSubjectPeer::getPrimaryKeyHashFromRow($row, $startcol5);
			if ($key5 !== null) {
				$obj5 = CourseSubjectPeer::getInstanceFromPool($key5);
				if (!$obj5) {

					$omClass = CourseSubjectPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					CourseSubjectPeer::addInstanceToPool($obj5, $key5);
				} // if obj5 loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj5 (CourseSubject)
				$obj5->addStudentAttendance($obj1);
			} // if joined row not null

			// Add objects for joined StudentAttendanceJustification rows

			$key6 = StudentAttendanceJustificationPeer::getPrimaryKeyHashFromRow($row, $startcol6);
			if ($key6 !== null) {
				$obj6 = StudentAttendanceJustificationPeer::getInstanceFromPool($key6);
				if (!$obj6) {

					$omClass = StudentAttendanceJustificationPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj6 = new $cls();
					$obj6->hydrate($row, $startcol6);
					StudentAttendanceJustificationPeer::addInstanceToPool($obj6, $key6);
				} // if obj6 loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj6 (StudentAttendanceJustification)
				$obj6->addStudentAttendance($obj1);
			} // if joined row not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related CareerSchoolYear table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptCareerSchoolYear(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related Student table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptStudent(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related AbsenceType table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptAbsenceType(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related CourseSubject table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptCourseSubject(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Returns the number of rows matching criteria, joining the related StudentAttendanceJustification table
	 *
	 * @param      Criteria $c
	 * @param      boolean $distinct Whether to select only distinct columns; deprecated: use Criteria->setDistinct() instead.
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     int Number of matching rows.
	 */
	public static function doCountJoinAllExceptStudentAttendanceJustification(Criteria $criteria, $distinct = false, PropelPDO $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		// we're going to modify criteria, so copy it first
		$criteria = clone $criteria;

		if ($distinct && !in_array(Criteria::DISTINCT, $criteria->getSelectModifiers())) {
			$criteria->setDistinct();
		}

		if (!$criteria->hasSelectClause()) {
			StudentAttendancePeer::addSelectColumns($criteria);
		}

		$criteria->clearOrderByColumns(); // ORDER BY won't ever affect the count

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}
	
				$criteria->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$criteria->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doCount:doCount') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $criteria, $con);
    }


		$stmt = BasePeer::doCount($criteria, $con);

		if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$count = (int) $row[0];
		} else {
			$count = 0; // no rows returned; we infer that means 0 matches.
		}
		$stmt->closeCursor();
		return $count;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with all related objects except CareerSchoolYear.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptCareerSchoolYear(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doSelectJoinAllExcept:doSelectJoinAllExcept') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $c, $con);
    }


		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol2 = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);

		StudentPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (StudentPeer::NUM_COLUMNS - StudentPeer::NUM_LAZY_LOAD_COLUMNS);

		AbsenceTypePeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (AbsenceTypePeer::NUM_COLUMNS - AbsenceTypePeer::NUM_LAZY_LOAD_COLUMNS);

		CourseSubjectPeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (CourseSubjectPeer::NUM_COLUMNS - CourseSubjectPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentAttendanceJustificationPeer::addSelectColumns($c);
		$startcol6 = $startcol5 + (StudentAttendanceJustificationPeer::NUM_COLUMNS - StudentAttendanceJustificationPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined Student rows

				$key2 = StudentPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = StudentPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = StudentPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					StudentPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj2 (Student)
				$obj2->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined AbsenceType rows

				$key3 = AbsenceTypePeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = AbsenceTypePeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = AbsenceTypePeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					AbsenceTypePeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj3 (AbsenceType)
				$obj3->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined CourseSubject rows

				$key4 = CourseSubjectPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = CourseSubjectPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$omClass = CourseSubjectPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					CourseSubjectPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj4 (CourseSubject)
				$obj4->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined StudentAttendanceJustification rows

				$key5 = StudentAttendanceJustificationPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = StudentAttendanceJustificationPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$omClass = StudentAttendanceJustificationPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					StudentAttendanceJustificationPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj5 (StudentAttendanceJustification)
				$obj5->addStudentAttendance($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with all related objects except Student.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptStudent(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol2 = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);

		CareerSchoolYearPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (CareerSchoolYearPeer::NUM_COLUMNS - CareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS);

		AbsenceTypePeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (AbsenceTypePeer::NUM_COLUMNS - AbsenceTypePeer::NUM_LAZY_LOAD_COLUMNS);

		CourseSubjectPeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (CourseSubjectPeer::NUM_COLUMNS - CourseSubjectPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentAttendanceJustificationPeer::addSelectColumns($c);
		$startcol6 = $startcol5 + (StudentAttendanceJustificationPeer::NUM_COLUMNS - StudentAttendanceJustificationPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined CareerSchoolYear rows

				$key2 = CareerSchoolYearPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = CareerSchoolYearPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = CareerSchoolYearPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					CareerSchoolYearPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj2 (CareerSchoolYear)
				$obj2->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined AbsenceType rows

				$key3 = AbsenceTypePeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = AbsenceTypePeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = AbsenceTypePeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					AbsenceTypePeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj3 (AbsenceType)
				$obj3->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined CourseSubject rows

				$key4 = CourseSubjectPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = CourseSubjectPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$omClass = CourseSubjectPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					CourseSubjectPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj4 (CourseSubject)
				$obj4->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined StudentAttendanceJustification rows

				$key5 = StudentAttendanceJustificationPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = StudentAttendanceJustificationPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$omClass = StudentAttendanceJustificationPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					StudentAttendanceJustificationPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj5 (StudentAttendanceJustification)
				$obj5->addStudentAttendance($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with all related objects except AbsenceType.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptAbsenceType(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol2 = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);

		CareerSchoolYearPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (CareerSchoolYearPeer::NUM_COLUMNS - CareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (StudentPeer::NUM_COLUMNS - StudentPeer::NUM_LAZY_LOAD_COLUMNS);

		CourseSubjectPeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (CourseSubjectPeer::NUM_COLUMNS - CourseSubjectPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentAttendanceJustificationPeer::addSelectColumns($c);
		$startcol6 = $startcol5 + (StudentAttendanceJustificationPeer::NUM_COLUMNS - StudentAttendanceJustificationPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined CareerSchoolYear rows

				$key2 = CareerSchoolYearPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = CareerSchoolYearPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = CareerSchoolYearPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					CareerSchoolYearPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj2 (CareerSchoolYear)
				$obj2->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined Student rows

				$key3 = StudentPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = StudentPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = StudentPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					StudentPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj3 (Student)
				$obj3->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined CourseSubject rows

				$key4 = CourseSubjectPeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = CourseSubjectPeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$omClass = CourseSubjectPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					CourseSubjectPeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj4 (CourseSubject)
				$obj4->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined StudentAttendanceJustification rows

				$key5 = StudentAttendanceJustificationPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = StudentAttendanceJustificationPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$omClass = StudentAttendanceJustificationPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					StudentAttendanceJustificationPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj5 (StudentAttendanceJustification)
				$obj5->addStudentAttendance($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with all related objects except CourseSubject.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptCourseSubject(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol2 = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);

		CareerSchoolYearPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (CareerSchoolYearPeer::NUM_COLUMNS - CareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (StudentPeer::NUM_COLUMNS - StudentPeer::NUM_LAZY_LOAD_COLUMNS);

		AbsenceTypePeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (AbsenceTypePeer::NUM_COLUMNS - AbsenceTypePeer::NUM_LAZY_LOAD_COLUMNS);

		StudentAttendanceJustificationPeer::addSelectColumns($c);
		$startcol6 = $startcol5 + (StudentAttendanceJustificationPeer::NUM_COLUMNS - StudentAttendanceJustificationPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ATTENDANCE_JUSTIFICATION_ID,), array(StudentAttendanceJustificationPeer::ID,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined CareerSchoolYear rows

				$key2 = CareerSchoolYearPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = CareerSchoolYearPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = CareerSchoolYearPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					CareerSchoolYearPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj2 (CareerSchoolYear)
				$obj2->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined Student rows

				$key3 = StudentPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = StudentPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = StudentPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					StudentPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj3 (Student)
				$obj3->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined AbsenceType rows

				$key4 = AbsenceTypePeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = AbsenceTypePeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$omClass = AbsenceTypePeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					AbsenceTypePeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj4 (AbsenceType)
				$obj4->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined StudentAttendanceJustification rows

				$key5 = StudentAttendanceJustificationPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = StudentAttendanceJustificationPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$omClass = StudentAttendanceJustificationPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					StudentAttendanceJustificationPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj5 (StudentAttendanceJustification)
				$obj5->addStudentAttendance($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


	/**
	 * Selects a collection of StudentAttendance objects pre-filled with all related objects except StudentAttendanceJustification.
	 *
	 * @param      Criteria  $c
	 * @param      PropelPDO $con
	 * @param      String    $join_behavior the type of joins to use, defaults to Criteria::LEFT_JOIN
	 * @return     array Array of StudentAttendance objects.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doSelectJoinAllExceptStudentAttendanceJustification(Criteria $c, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$c = clone $c;

		// Set the correct dbName if it has not been overridden
		// $c->getDbName() will return the same object if not set to another value
		// so == check is okay and faster
		if ($c->getDbName() == Propel::getDefaultDB()) {
			$c->setDbName(self::DATABASE_NAME);
		}

		StudentAttendancePeer::addSelectColumns($c);
		$startcol2 = (StudentAttendancePeer::NUM_COLUMNS - StudentAttendancePeer::NUM_LAZY_LOAD_COLUMNS);

		CareerSchoolYearPeer::addSelectColumns($c);
		$startcol3 = $startcol2 + (CareerSchoolYearPeer::NUM_COLUMNS - CareerSchoolYearPeer::NUM_LAZY_LOAD_COLUMNS);

		StudentPeer::addSelectColumns($c);
		$startcol4 = $startcol3 + (StudentPeer::NUM_COLUMNS - StudentPeer::NUM_LAZY_LOAD_COLUMNS);

		AbsenceTypePeer::addSelectColumns($c);
		$startcol5 = $startcol4 + (AbsenceTypePeer::NUM_COLUMNS - AbsenceTypePeer::NUM_LAZY_LOAD_COLUMNS);

		CourseSubjectPeer::addSelectColumns($c);
		$startcol6 = $startcol5 + (CourseSubjectPeer::NUM_COLUMNS - CourseSubjectPeer::NUM_LAZY_LOAD_COLUMNS);

				$c->addJoin(array(StudentAttendancePeer::CAREER_SCHOOL_YEAR_ID,), array(CareerSchoolYearPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::STUDENT_ID,), array(StudentPeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::ABSENCE_TYPE_ID,), array(AbsenceTypePeer::ID,), $join_behavior);
				$c->addJoin(array(StudentAttendancePeer::COURSE_SUBJECT_ID,), array(CourseSubjectPeer::ID,), $join_behavior);

		$stmt = BasePeer::doSelect($c, $con);
		$results = array();

		while ($row = $stmt->fetch(PDO::FETCH_NUM)) {
			$key1 = StudentAttendancePeer::getPrimaryKeyHashFromRow($row, 0);
			if (null !== ($obj1 = StudentAttendancePeer::getInstanceFromPool($key1))) {
				// We no longer rehydrate the object, since this can cause data loss.
				// See http://propel.phpdb.org/trac/ticket/509
				// $obj1->hydrate($row, 0, true); // rehydrate
			} else {
				$omClass = StudentAttendancePeer::getOMClass();

				$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
				$obj1 = new $cls();
				$obj1->hydrate($row);
				StudentAttendancePeer::addInstanceToPool($obj1, $key1);
			} // if obj1 already loaded

				// Add objects for joined CareerSchoolYear rows

				$key2 = CareerSchoolYearPeer::getPrimaryKeyHashFromRow($row, $startcol2);
				if ($key2 !== null) {
					$obj2 = CareerSchoolYearPeer::getInstanceFromPool($key2);
					if (!$obj2) {
	
						$omClass = CareerSchoolYearPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj2 = new $cls();
					$obj2->hydrate($row, $startcol2);
					CareerSchoolYearPeer::addInstanceToPool($obj2, $key2);
				} // if $obj2 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj2 (CareerSchoolYear)
				$obj2->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined Student rows

				$key3 = StudentPeer::getPrimaryKeyHashFromRow($row, $startcol3);
				if ($key3 !== null) {
					$obj3 = StudentPeer::getInstanceFromPool($key3);
					if (!$obj3) {
	
						$omClass = StudentPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj3 = new $cls();
					$obj3->hydrate($row, $startcol3);
					StudentPeer::addInstanceToPool($obj3, $key3);
				} // if $obj3 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj3 (Student)
				$obj3->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined AbsenceType rows

				$key4 = AbsenceTypePeer::getPrimaryKeyHashFromRow($row, $startcol4);
				if ($key4 !== null) {
					$obj4 = AbsenceTypePeer::getInstanceFromPool($key4);
					if (!$obj4) {
	
						$omClass = AbsenceTypePeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj4 = new $cls();
					$obj4->hydrate($row, $startcol4);
					AbsenceTypePeer::addInstanceToPool($obj4, $key4);
				} // if $obj4 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj4 (AbsenceType)
				$obj4->addStudentAttendance($obj1);

			} // if joined row is not null

				// Add objects for joined CourseSubject rows

				$key5 = CourseSubjectPeer::getPrimaryKeyHashFromRow($row, $startcol5);
				if ($key5 !== null) {
					$obj5 = CourseSubjectPeer::getInstanceFromPool($key5);
					if (!$obj5) {
	
						$omClass = CourseSubjectPeer::getOMClass();


					$cls = substr('.'.$omClass, strrpos('.'.$omClass, '.') + 1);
					$obj5 = new $cls();
					$obj5->hydrate($row, $startcol5);
					CourseSubjectPeer::addInstanceToPool($obj5, $key5);
				} // if $obj5 already loaded

				// Add the $obj1 (StudentAttendance) to the collection in $obj5 (CourseSubject)
				$obj5->addStudentAttendance($obj1);

			} // if joined row is not null

			$results[] = $obj1;
		}
		$stmt->closeCursor();
		return $results;
	}


  static public function getUniqueColumnNames()
  {
    return array(array('student_id', 'day', 'course_subject_id'));
  }
	/**
	 * Returns the TableMap related to this peer.
	 * This method is not needed for general use but a specific application could have a need.
	 * @return     TableMap
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function getTableMap()
	{
		return Propel::getDatabaseMap(self::DATABASE_NAME)->getTable(self::TABLE_NAME);
	}

	/**
	 * The class that the Peer will make instances of.
	 *
	 * This uses a dot-path notation which is tranalted into a path
	 * relative to a location on the PHP include_path.
	 * (e.g. path.to.MyClass -> 'path/to/MyClass.php')
	 *
	 * @return     string path.to.ClassName
	 */
	public static function getOMClass()
	{
		return StudentAttendancePeer::CLASS_DEFAULT;
	}

	/**
	 * Method perform an INSERT on the database, given a StudentAttendance or Criteria object.
	 *
	 * @param      mixed $values Criteria or StudentAttendance object containing data that is used to create the INSERT statement.
	 * @param      PropelPDO $con the PropelPDO connection to use
	 * @return     mixed The new primary key.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doInsert($values, PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doInsert:pre') as $callable)
    {
      $ret = call_user_func($callable, 'BaseStudentAttendancePeer', $values, $con);
      if (false !== $ret)
      {
        return $ret;
      }
    }


		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		if ($values instanceof Criteria) {
			$criteria = clone $values; // rename for clarity
		} else {
			$criteria = $values->buildCriteria(); // build Criteria from StudentAttendance object
		}

		if ($criteria->containsKey(StudentAttendancePeer::ID) && $criteria->keyContainsValue(StudentAttendancePeer::ID) ) {
			throw new PropelException('Cannot insert a value for auto-increment primary key ('.StudentAttendancePeer::ID.')');
		}


		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		try {
			// use transaction because $criteria could contain info
			// for more than one table (I guess, conceivably)
			$con->beginTransaction();
			$pk = BasePeer::doInsert($criteria, $con);
			$con->commit();
		} catch(PropelException $e) {
			$con->rollBack();
			throw $e;
		}

		
    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doInsert:post') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $values, $con, $pk);
    }

    return $pk;
	}

	/**
	 * Method perform an UPDATE on the database, given a StudentAttendance or Criteria object.
	 *
	 * @param      mixed $values Criteria or StudentAttendance object containing data that is used to create the UPDATE statement.
	 * @param      PropelPDO $con The connection to use (specify PropelPDO connection object to exert more control over transactions).
	 * @return     int The number of affected rows (if supported by underlying database driver).
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function doUpdate($values, PropelPDO $con = null)
	{

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doUpdate:pre') as $callable)
    {
      $ret = call_user_func($callable, 'BaseStudentAttendancePeer', $values, $con);
      if (false !== $ret)
      {
        return $ret;
      }
    }


		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$selectCriteria = new Criteria(self::DATABASE_NAME);

		if ($values instanceof Criteria) {
			$criteria = clone $values; // rename for clarity

			$comparison = $criteria->getComparison(StudentAttendancePeer::ID);
			$selectCriteria->add(StudentAttendancePeer::ID, $criteria->remove(StudentAttendancePeer::ID), $comparison);

		} else { // $values is StudentAttendance object
			$criteria = $values->buildCriteria(); // gets full criteria
			$selectCriteria = $values->buildPkeyCriteria(); // gets criteria w/ primary key(s)
		}

		// set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		$ret = BasePeer::doUpdate($selectCriteria, $criteria, $con);
	

    foreach (sfMixer::getCallables('BaseStudentAttendancePeer:doUpdate:post') as $callable)
    {
      call_user_func($callable, 'BaseStudentAttendancePeer', $values, $con, $ret);
    }

    return $ret;
  }

	/**
	 * Method to DELETE all rows from the student_attendance table.
	 *
	 * @return     int The number of affected rows (if supported by underlying database driver).
	 */
	public static function doDeleteAll($con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		$affectedRows = 0; // initialize var to track total num of affected rows
		try {
			// use transaction because $criteria could contain info
			// for more than one table or we could emulating ON DELETE CASCADE, etc.
			$con->beginTransaction();
			$affectedRows += BasePeer::doDeleteAll(StudentAttendancePeer::TABLE_NAME, $con);
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Method perform a DELETE on the database, given a StudentAttendance or Criteria object OR a primary key value.
	 *
	 * @param      mixed $values Criteria or StudentAttendance object or primary key or array of primary keys
	 *              which is used to create the DELETE statement
	 * @param      PropelPDO $con the connection to use
	 * @return     int 	The number of affected rows (if supported by underlying database driver).  This includes CASCADE-related rows
	 *				if supported by native driver or if emulated using Propel.
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	 public static function doDelete($values, PropelPDO $con = null)
	 {
		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		if ($values instanceof Criteria) {
			// invalidate the cache for all objects of this type, since we have no
			// way of knowing (without running a query) what objects should be invalidated
			// from the cache based on this Criteria.
			StudentAttendancePeer::clearInstancePool();

			// rename for clarity
			$criteria = clone $values;
		} elseif ($values instanceof StudentAttendance) {
			// invalidate the cache for this single object
			StudentAttendancePeer::removeInstanceFromPool($values);
			// create criteria based on pk values
			$criteria = $values->buildPkeyCriteria();
		} else {
			// it must be the primary key



			$criteria = new Criteria(self::DATABASE_NAME);
			$criteria->add(StudentAttendancePeer::ID, (array) $values, Criteria::IN);

			foreach ((array) $values as $singleval) {
				// we can invalidate the cache for this single object
				StudentAttendancePeer::removeInstanceFromPool($singleval);
			}
		}

		// Set the correct dbName
		$criteria->setDbName(self::DATABASE_NAME);

		$affectedRows = 0; // initialize var to track total num of affected rows

		try {
			// use transaction because $criteria could contain info
			// for more than one table or we could emulating ON DELETE CASCADE, etc.
			$con->beginTransaction();
			
			$affectedRows += BasePeer::doDelete($criteria, $con);

			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Validates all modified columns of given StudentAttendance object.
	 * If parameter $columns is either a single column name or an array of column names
	 * than only those columns are validated.
	 *
	 * NOTICE: This does not apply to primary or foreign keys for now.
	 *
	 * @param      StudentAttendance $obj The object to validate.
	 * @param      mixed $cols Column name or array of column names.
	 *
	 * @return     mixed TRUE if all columns are valid or the error message of the first invalid column.
	 */
	public static function doValidate(StudentAttendance $obj, $cols = null)
	{
		$columns = array();

		if ($cols) {
			$dbMap = Propel::getDatabaseMap(StudentAttendancePeer::DATABASE_NAME);
			$tableMap = $dbMap->getTable(StudentAttendancePeer::TABLE_NAME);

			if (! is_array($cols)) {
				$cols = array($cols);
			}

			foreach ($cols as $colName) {
				if ($tableMap->containsColumn($colName)) {
					$get = 'get' . $tableMap->getColumn($colName)->getPhpName();
					$columns[$colName] = $obj->$get();
				}
			}
		} else {

		}

		$res =  BasePeer::doValidate(StudentAttendancePeer::DATABASE_NAME, StudentAttendancePeer::TABLE_NAME, $columns);
    if ($res !== true) {
        foreach ($res as $failed) {
            $col = StudentAttendancePeer::translateFieldname($failed->getColumn(), BasePeer::TYPE_COLNAME, BasePeer::TYPE_PHPNAME);
        }
    }

    return $res;
	}

	/**
	 * Retrieve a single object by pkey.
	 *
	 * @param      int $pk the primary key.
	 * @param      PropelPDO $con the connection to use
	 * @return     StudentAttendance
	 */
	public static function retrieveByPK($pk, PropelPDO $con = null)
	{

		if (null !== ($obj = StudentAttendancePeer::getInstanceFromPool((string) $pk))) {
			return $obj;
		}

		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$criteria = new Criteria(StudentAttendancePeer::DATABASE_NAME);
		$criteria->add(StudentAttendancePeer::ID, $pk);

		$v = StudentAttendancePeer::doSelect($criteria, $con);

		return !empty($v) > 0 ? $v[0] : null;
	}

	/**
	 * Retrieve multiple objects by pkey.
	 *
	 * @param      array $pks List of primary keys
	 * @param      PropelPDO $con the connection to use
	 * @throws     PropelException Any exceptions caught during processing will be
	 *		 rethrown wrapped into a PropelException.
	 */
	public static function retrieveByPKs($pks, PropelPDO $con = null)
	{
		if ($con === null) {
			$con = Propel::getConnection(StudentAttendancePeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		$objs = null;
		if (empty($pks)) {
			$objs = array();
		} else {
			$criteria = new Criteria(StudentAttendancePeer::DATABASE_NAME);
			$criteria->add(StudentAttendancePeer::ID, $pks, Criteria::IN);
			$objs = StudentAttendancePeer::doSelect($criteria, $con);
		}
		return $objs;
	}

} // BaseStudentAttendancePeer

// This is the static code needed to register the MapBuilder for this table with the main Propel class.
//
// NOTE: This static code cannot call methods on the StudentAttendancePeer class, because it is not defined yet.
// If you need to use overridden methods, you can add this code to the bottom of the StudentAttendancePeer class:
//
// Propel::getDatabaseMap(StudentAttendancePeer::DATABASE_NAME)->addTableBuilder(StudentAttendancePeer::TABLE_NAME, StudentAttendancePeer::getMapBuilder());
//
// Doing so will effectively overwrite the registration below.

Propel::getDatabaseMap(BaseStudentAttendancePeer::DATABASE_NAME)->addTableBuilder(BaseStudentAttendancePeer::TABLE_NAME, BaseStudentAttendancePeer::getMapBuilder());

